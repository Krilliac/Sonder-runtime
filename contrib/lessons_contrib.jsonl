{"id": "lesson-0118616be693b342bff78a64", "text": "Don't assume all cross-DLL reflection breaks together: compiler-intrinsic type hashing (e.g. __FUNCSIG__) can stay stable across images even when service-locator addresses don't; verify each mechanism separately."}
{"id": "lesson-011a16eb5750cf6b408a668b", "text": "Inject real faults, dropped packets, added latency, reordered messages, in tests to expose code that silently assumes the fallacies: reliable network, zero latency, infinite bandwidth."}
{"id": "lesson-01ddb1815cc2cb9a150483d8", "text": "Ground an LLM's self-reported success by actually executing its code in a subprocess with a timeout and checking the exit code, instead of trusting the model's own claim."}
{"id": "lesson-01eeb65902f0559c9de68ac9", "text": "Use `collections.deque` for efficient queue operations in topological sorting."}
{"id": "lesson-021e3883fd70beefc9a8a7d5", "text": "Use a while loop for main game loop, manage events and player movement inside it."}
{"id": "lesson-024a691f704efde3a1b7c5bd", "text": "**Bounded buffers ensure deterministic resource usage and prevent memory leaks.**"}
{"id": "lesson-026da013ee4d8662856c787f", "text": "Use binary search algorithm with two pointers (lo, hi) to achieve O(log n) search time complexity."}
{"id": "lesson-029bab5d97b888288e74c24d", "text": "Make writes atomic by creating the temp file with tempfile.mkstemp(dir=same_directory_as_target), fsync its fd, then os.replace(tmp, target) so readers never observe a partially written file."}
{"id": "lesson-02c230a5eb0c5ae2a42fb5a9", "text": "Wrap a network thread's per-frame parse/dispatch in try/catch so one malformed packet cannot std::terminate the whole host process."}
{"id": "lesson-02cf42c3f6c9e53413680e7b", "text": "Write-Output \"42\" directly outputs the string without needing variables."}
{"id": "lesson-033705aa35fb5fc81e7b541b", "text": "Use `std::endl` for line breaks in C++."}
{"id": "lesson-03573e5d25d9d4994eb91e69", "text": "On Windows, bind listener sockets with SO_EXCLUSIVEADDRUSE, not SO_REUSEADDR, so a second bind on a busy port fails loudly instead of silently co-binding and stealing traffic."}
{"id": "lesson-03d9341b8a3a8b9bf3d78c83", "text": "Use bitwise operations and string indexing for efficient hex conversion and parsing."}
{"id": "lesson-042eb95039e1040fb267fc48", "text": "Use regular expressions with capture groups and backreferences to insert underscores between letter case transitions."}
{"id": "lesson-04301306584a2aa2966c47ed", "text": "Build a trie from nested dicts via node.setdefault(ch, {}) per character, marking word ends with a sentinel key like '$' to distinguish prefixes from full words."}
{"id": "lesson-04348323bfb205d0e765604c", "text": "For 'every element appears 3 times except one', track two accumulator registers ones/twos updated as ones=(ones^n)&~twos, twos=(twos^n)&~ones so each bit cycles 0->1->2->0 per triple."}
{"id": "lesson-047e70a9755434197bafb402", "text": "Use `yield` inside a function to make it a generator that pauses/resumes per next() call; exhausting it raises StopIteration instead of returning a full list."}
{"id": "lesson-04abc69a6f59362ece4bb84a", "text": "In cooperative multiple inheritance, the order of base classes in the class statement changes what super() resolves to at each step -- check __mro__, don't guess."}
{"id": "lesson-050306b3b06e731eff451860", "text": "Use tempfile.NamedTemporaryFile(delete=False) when another handle must reopen the file by name (required on Windows), then os.unlink it yourself; use TemporaryDirectory for scratch dirs that self-clean on context exit."}
{"id": "lesson-051666c72af56ba377a77434", "text": "Embedded fallback shader source must mirror the engine-side constant-buffer struct field-for-field; one missing member silently shifts later fields into zeroed memory with no error, just black output."}
{"id": "lesson-0545ea77eca582208557e4a6", "text": "Use a list to collect decoded characters and join at the end for O(n) string building."}
{"id": "lesson-054cd1c2cfb8d306d883f41d", "text": "Re-validate range/state gates (alive, distance, same-zone) at both action-start and action-resolution, since a target can move, flee, or die during a cast/windup between the two."}
{"id": "lesson-05bb64b82d474029558f8de2", "text": "Test power-of-two with n > 0 and (n & (n - 1)) == 0 instead of looping or using math.log2, which is exact and avoids float rounding."}
{"id": "lesson-05c9b4ef6283702af9b84610", "text": "Bisect a regression with `git bisect run <script>` so each step is decided by the script's exit code, not by eyeballing diffs."}
{"id": "lesson-05dc55771bd528f5514c3548", "text": "After breaking a dependency cycle, add a CI check that walks the module graph and fails the build on any new cross-package cycle, so the fix cannot silently regress."}
{"id": "lesson-05f36ba30ee05c24a4ead340", "text": "Use `std::function` and lambda captures to handle dynamic panel creation safely."}
{"id": "lesson-06e19a4aaec8e34623fd1eb7", "text": "Use functools.reduce with an explicit initial value ([] or 1) to fold nested lists or compute a product, avoiding IndexError on empty input."}
{"id": "lesson-070786c7fe121ac6cc083ea5", "text": "Build an async context manager with @contextlib.asynccontextmanager and a try/finally around yield so cleanup runs in `async with` even when the body raises."}
{"id": "lesson-070a3ab9b61ffb497f726017", "text": "Use try-except to handle conversion errors elegantly."}
{"id": "lesson-077e9720bfacd5e4bded577f", "text": "Use `for...of` loop for iterating arrays in JavaScript."}
{"id": "lesson-07bcdcfbeea31363375b2bdc", "text": "Use `LinkedListNode<T>` for O(1) removal and insertion in an ordered collection."}
{"id": "lesson-083506d50069de95ca67e865", "text": "Use `Array.Reverse()` to efficiently reverse arrays in-place when converting strings to char arrays and back."}
{"id": "lesson-088141380dca55952bc0a140", "text": "Implement itertools' 'grouper' recipe as `zip_longest(*[iter(iterable)]*n, fillvalue=x)` to chunk a flat iterable into fixed-size tuples without manual index slicing."}
{"id": "lesson-094b6ea3e78d4c0cb84548a1", "text": "asyncio.gather(*coros) runs coroutines concurrently and returns results in the SAME order as the input args, regardless of which one finishes first."}
{"id": "lesson-096fce2192a0cdab19ad1780", "text": "Check divisibility up to half of the number for primality testing."}
{"id": "lesson-097fde2cf0ddb7bd7b848814", "text": "Use `System.Collections.Generic.List[int]` for maintaining order efficiently in LRU cache operations."}
{"id": "lesson-0a0e9558ac0308412669395e", "text": "Sum digits with sum(int(d) for d in str(abs(n))): cast to str first so each character iterates as a digit."}
{"id": "lesson-0a517166cfc7d5bee66f1597", "text": "Use string slicing with negative step (`[::-1]`) to reverse strings in Python."}
{"id": "lesson-0a652eae4f3bdadc5d5212a8", "text": "To swallow one specific exception type with cleanup attached, make __exit__(self, exc_type, exc, tb) return True only for that type; for no-cleanup suppression, use contextlib.suppress(ExcType) instead."}
{"id": "lesson-0a99bc8ba0ed64f17bc12127", "text": "Use `std::cout` for quick console output in C++."}
{"id": "lesson-0ad35d15978fe1323fb973b5", "text": "Implement base64 by packing 3 bytes into a 24-bit int, then extracting four 6-bit groups via shifts (>>18,>>12,>>6,&0x3F) and padding short chunks with zero bytes plus '=' markers."}
{"id": "lesson-0ad609d0a33efab843b29325", "text": "For unweighted shortest-path BFS, track distance alongside each queued node as (node, dist) tuples and mark visited at enqueue time, not dequeue, to avoid duplicate re-enqueues."}
{"id": "lesson-0af35288a339e2cb6814e1dc", "text": "Implement a circuit breaker with closed/open/half-open states that trips after N consecutive failures and short-circuits calls during a cooldown, letting the dependency recover."}
{"id": "lesson-0bb2aa4724bfe0bca850a7a4", "text": "Transpose a matrix with [list(r) for r in zip(*matrix)]; the * unpacks rows so zip pairs up columns."}
{"id": "lesson-0bd532887c8252f91d51ec39", "text": "Branch a Union[int, str] parameter with `isinstance(value, int)` first, since bool is also an int and order can flip results."}
{"id": "lesson-0c24f5943f8ec849f24b1dda", "text": "Swap characters manually for string reversal."}
{"id": "lesson-0c28462a688dfb1fbbcb9554", "text": "Matrix-chain DP must iterate the outer loop by increasing interval length (not by i or j directly) so every dp[i][k] and dp[k+1][j] subproblem is already filled before it's read."}
{"id": "lesson-0c5c9a0fd59f5d2a159ade63", "text": "Use floating-point division for accurate averages."}
{"id": "lesson-0c6a220d30660e12f38274bf", "text": "When CI gates third-party version pins via a manifest/lock file, update that lock file in the same change as the dependency bump or automated update PRs fail the gate."}
{"id": "lesson-0d883e692b910b77b6b71aab", "text": "Use `std::condition_variable::wait_until` with a predicate for spurious wakeups and absolute deadlines."}
{"id": "lesson-0dc01a1d60240b56f968db71", "text": "Use `-join` to concatenate characters into a string efficiently."}
{"id": "lesson-0deaa19dd4e8d279bc911af7", "text": "Seed dp[i][0]=i and dp[0][j]=j as base rows/cols, then take 1+min(delete,insert,substitute) for edit distance."}
{"id": "lesson-0df2e167a01dafc6a50a2ff1", "text": "Use authenticated encryption (AEAD) rather than encrypt-then-roll-your-own-MAC; plain CBC without an integrity check is vulnerable to padding-oracle attacks that recover plaintext byte by byte."}
{"id": "lesson-0df652971ae31b87d370a705", "text": "Use os.environ to detect automated testing and adjust input/output accordingly."}
{"id": "lesson-0e42f86646c914c9d57d4a98", "text": "Use a collections.deque holding indices as a monotonic queue, popping smaller trailing values, to get sliding-window maximum in O(n)."}
{"id": "lesson-0e86a02879c11ba607a5b8c6", "text": "Use iterative bottom-up dynamic programming with tuple unpacking to compute Fibonacci numbers in O(n) time and O(1) space."}
{"id": "lesson-0eb3c8386b0fe271a0c6bd2a", "text": "Check if user is None before accessing its 'name' key."}
{"id": "lesson-0f071a999437a286f665164b", "text": "Use string concatenation with modulo and integer division to manually convert decimal to binary."}
{"id": "lesson-0f4654cfdf875324bf680b39", "text": "Use Lamport timestamps to derive a happened-before partial order across nodes without synchronized wall clocks; break ties with node ID for a total order."}
{"id": "lesson-0f6667a9c6158c600f4d59e5", "text": "Fix off-by-one error in condition."}
{"id": "lesson-102a2d2a75361b9ea23e50ae", "text": "Benchmark your KDF's cost parameter against current hardware to target roughly 250ms per hash, and periodically bump it as CPUs get faster, re-hashing on the user's next successful login."}
{"id": "lesson-10581a54dc92e2b034971ba2", "text": "Watch for implicit type coercion in a WHERE clause (e.g. comparing an indexed int column to a string literal) silently disabling index use and forcing a full scan."}
{"id": "lesson-106874c20c62d9f8d1dc3b94", "text": "Use modulo arithmetic to implement conditional logic for leap year calculation."}
{"id": "lesson-10a3229c94187591a00bb167", "text": "Call exec functions with an argv array instead of shell=True plus string interpolation; passing argv bypasses shell parsing entirely and neutralizes command-injection metacharacters."}
{"id": "lesson-10a97697821882966fb93c19", "text": "Use parameterized queries or prepared statements for all SQL; string-concatenating user input stays exploitable even behind an escaping function."}
{"id": "lesson-11118de2f105ede0b70409dd", "text": "Use console.log() for direct string output to stdout in JavaScript."}
{"id": "lesson-11a19fc44a89660e2d66adae", "text": "Implement __getattr__ (not __getattribute__) for lazy/cached fallback lookups since it only fires on attribute-lookup misses, and cache in it to avoid repeated loader calls."}
{"id": "lesson-11d6c1aa212d7938a77fe5af", "text": "On a timeout, treat the outcome as unknown, not failed: the request may have completed after the response was lost, so retries must be idempotent, never assume-and-resend blindly."}
{"id": "lesson-123eede6192df6a97c45bc61", "text": "Use `std::stack` for balanced bracket checking."}
{"id": "lesson-12aec52f77e020aa7717c101", "text": "Use modular arithmetic to check divisibility and conditionally return results."}
{"id": "lesson-12d1f457c365710b2492d6f1", "text": "Never mutate an object's hash-relevant fields after using it as a dict/set key; the bucket is fixed at insertion so lookups silently miss it later."}
{"id": "lesson-12d8730fcc4100e10d8a68c5", "text": "Use -join to explicitly concatenate array elements into a single string when building output from character arrays."}
{"id": "lesson-12dcefd808eadb42424f1960", "text": "Use `-eq` for equality comparison in PowerShell conditions."}
{"id": "lesson-12e034f518dce8dd0f7e28e5", "text": "Distinguish leaks from bloat by diffing two heap snapshots minutes apart: unreachable growth is a leak; reachable-but-unused growth is bloat from caches or listeners never trimmed."}
{"id": "lesson-133c7cad9de07ec4c3cf7e70", "text": "Use `all()` with generator expression for prime checking."}
{"id": "lesson-1343a5300800171936547c34", "text": "Use `collections.Counter` for efficient word frequency counting and retrieval of most common elements."}
{"id": "lesson-13f0db05438210e8a572bf6c", "text": "Use `re.search` for pattern matching in regex within a loop to process lines efficiently."}
{"id": "lesson-141d389c851131a2a45232aa", "text": "Use for loop with Math.Sqrt for efficient prime number checking."}
{"id": "lesson-1434aa9910550952ef12faeb", "text": "Load-balance with least-outstanding-requests or power-of-two-choices instead of round robin when request costs vary, since round robin sends equal load to slow and fast backends"}
{"id": "lesson-144ece5d44d1e08464f43809", "text": "Prototype language semantics with a tree-walking interpreter first; compile to a flat bytecode array only once semantics stabilize, since walkers are easier to debug."}
{"id": "lesson-14d580a8de8e4a4beb38a87e", "text": "Pair SameSite=Lax/Strict cookies with a per-request CSRF synchronizer token; SameSite alone fails for cross-subdomain requests and some top-level GET navigations."}
{"id": "lesson-14d8a215527514591a284ecc", "text": "Dutch national flag needs three pointers lo/mid/hi: on a 2-swap with hi, decrement hi WITHOUT advancing mid, since the swapped-in value from hi is still unexamined."}
{"id": "lesson-14ee80cb874831a5bf1a0858", "text": "Use indexing to reverse string characters explicitly with `-join`."}
{"id": "lesson-14fd4c6f13ddb3b94ca6fc34", "text": "Remember try/else runs only when the try block raised nothing, and finally always runs afterward even when except or else returns a value."}
{"id": "lesson-150815ff6cd708b1b29b3873", "text": "FTS5 MATCH throws on raw punctuation; sanitize queries to alphanumeric tokens joined with OR before passing them to MATCH."}
{"id": "lesson-152fa9ed714440b7b63e0fc8", "text": "Use `LinkedList<T>` for O(1) insertions and deletions from both ends in LRU cache implementation."}
{"id": "lesson-15587643dfa7db7006f1d337", "text": "Use CAS for exactly-once initialization and transition states."}
{"id": "lesson-1577b6b81d03f8af837a8cb3", "text": "Use Kadane's algorithm with two variables to track current and best subarray sums in O(n) time."}
{"id": "lesson-168839bb51b33959b5357ba6", "text": "Implement __aiter__/__anext__ raising StopAsyncIteration for manual async-iterator state; pass return_exceptions=True to gather so one failing coroutine's exception becomes a value instead of cancelling running siblings."}
{"id": "lesson-168ed05cd5bb98d858893bf4", "text": "Blending self-graded benchmark outcomes with human-judged ones into one success rate hides the judged rate; report the two populations separately."}
{"id": "lesson-168f71cb047136188a34429e", "text": "Use a for loop with index-based array access to iterate through elements and accumulate a sum."}
{"id": "lesson-16b3c6adb59f349273069f99", "text": "In binary search use lo,hi=0,len(a)-1 and mid=(lo+hi)//2 with lo<=hi to avoid infinite loops."}
{"id": "lesson-171c4254ae27516654e4594b", "text": "Use `json.dumps` inside a generator expression for efficient string joining."}
{"id": "lesson-17499686b1f3ddeda1696320", "text": "Skip duplicate permutations by sorting first, then at each slot skip nums[i]==nums[i-1] unless the earlier equal value is already used."}
{"id": "lesson-175fcf2d921d76f7c3ed637f", "text": "Iterate string, check alpha chars, convert to upper, use std::toupper."}
{"id": "lesson-179d99e41d79a529e3212121", "text": "Use try-except to handle invalid inputs and ensure game continues smoothly."}
{"id": "lesson-17aa1ecaee096ee91c47ca53", "text": "Guard every untrusted length/count field against buffer-remaining before resize/allocate, or a malicious frame becomes an allocation-DoS."}
{"id": "lesson-17e5e5d04876ad0b55f423f2", "text": "Use `for` loop for sequential iteration."}
{"id": "lesson-17eb57140134dc9216851516", "text": "Use Euclid's algorithm efficiently with modulo operation inside loop."}
{"id": "lesson-17f6db81932640f7e9cd93fa", "text": "Never use a mutable literal (list/dict/set) as a default arg; use default=None and create the mutable inside the function body."}
{"id": "lesson-18239cdd6d4f9ba700adce8f", "text": "When several codebase versions share one core library, put the divergent version's vendored headers earlier on its include path so they shadow the shared headers, scoped to that translation unit only."}
{"id": "lesson-1861aa48cbb5bd8556da146f", "text": "Use `String.ToCharArray()` and `Array.Reverse()` to efficiently reverse a string in C#."}
{"id": "lesson-187d1034f3b01e5f36e85d21", "text": "Use `ROW_NUMBER()` for ranking within partitions."}
{"id": "lesson-18849567c7a764af53fdbe6f", "text": "Use `GdiplusStartup` and `GdiplusShutdown` for proper GDI+ initialization and cleanup."}
{"id": "lesson-18ab978641c40bee09342712", "text": "Build parameterized decorators as decorator(func)-returning outer functions, and apply functools.wraps(func) on the inner wrapper to preserve __name__/__doc__."}
{"id": "lesson-18cc152623322adb9744ae3b", "text": "When an external tool edits a live simulation, mutate it through the sim's existing setters (not a new render path) and drain edits from one queue once per tick to avoid locking the hot path."}
{"id": "lesson-18cfac32d466dbaddae4fdd7", "text": "Use `std::atomic` for shared state to avoid race conditions."}
{"id": "lesson-193cd77fc7e46e2cd1888c00", "text": "Stack multiple `@overload` signatures above one real implementation; the implementation body must itself branch on `isinstance` since only it executes at runtime."}
{"id": "lesson-19620362d43008afc53bc091", "text": "Encode pagination cursors as opaque signed tokens of (sort_key, tiebreaker_id), not raw integer offsets — this survives concurrent writes and stops clients forging arbitrary offsets."}
{"id": "lesson-196ec183ed6deb1a17902359", "text": "Bound fan-out concurrency by acquiring a shared asyncio.Semaphore(n) inside each worker's `async with sem:` block; gather still preserves result order while capping simultaneous work at n."}
{"id": "lesson-19c8386cf9f314270c950ea0", "text": "Enumerate all submasks of a bitmask with sub = (sub - 1) & mask looping from mask down to 0; total work across all masks is O(3^n), not O(4^n)."}
{"id": "lesson-1a3920f8bbbd3f4609b811b8", "text": "Use `sorted()` with a key function to sort numerically when given lexicographically ordered strings."}
{"id": "lesson-1ac51535bf35529255ee76b9", "text": "Use itertools.product(range(...), repeat=n) plus a sum filter to brute-force bounded combination search instead of nested for-loops."}
{"id": "lesson-1acb515110f29d337b1a06ae", "text": "Call asyncio.run(coro()) once at the top level to drive an event loop; await inside async def suspends until the awaited coroutine/future resolves, returning its value directly."}
{"id": "lesson-1ad9195d3fa965b32fc0d0a0", "text": "Add missing cases for Hull and Domain stages in switch statement."}
{"id": "lesson-1b190a0036d0a72fb3cab368", "text": "Use early exit conditions in loops to optimize performance by breaking as soon as a factor is found."}
{"id": "lesson-1bd4a661faf22350d6e3ea8b", "text": "Track column and both diagonals (row-col, row+col) as three sets for O(1) conflict checks instead of rescanning the board each placement."}
{"id": "lesson-1be7e13333e9010f964289d7", "text": "Use `std::unordered_map` and `std::list` for O(1) access and efficient order management in LRU cache implementation."}
{"id": "lesson-1c310d2d201c215ed64a27c6", "text": "Convert string to char array with `String.ToCharArray()`, reverse array with `Array.Reverse()`, then construct new string from reversed array."}
{"id": "lesson-1c3ed0ce60272cba949664b1", "text": "Split strings with `str.split()` and validate each part separately to check complex string formats like IP addresses."}
{"id": "lesson-1c647dcf98f07e4da24a07d2", "text": "Recover a Generic[T] subclass's concrete type argument at runtime via `cls.__orig_bases__` plus `typing.get_args`, since normal instances erase T entirely."}
{"id": "lesson-1d1d231a53d0f789c910b004", "text": "Use modular arithmetic to handle wrap-around in Caesar cipher shifts."}
{"id": "lesson-1d2878751d319f38043e79f1", "text": "Use modular arithmetic and iterate through values/symbols in reverse order for correct Roman numeral construction."}
{"id": "lesson-1d3ddf2ffd4abb4b792bd5a7", "text": "Wrap a slow coroutine in asyncio.wait_for(coro, timeout=t) and catch asyncio.TimeoutError; wait_for cancels the inner coroutine/task automatically on timeout."}
{"id": "lesson-1d69a00ada3c90030293dbbf", "text": "Check for false sharing: two threads writing adjacent fields on the same cache line serialize through cache-coherence traffic even with zero logical contention; pad or reorder fields to separate cache lines."}
{"id": "lesson-1e87e860b803cc2eafc6c5fb", "text": "Store a data descriptor's per-instance value under a name-mangled key in obj.__dict__ (never the plain attribute name) so __get__/__set__ don't recurse and instance __dict__ writes can't shadow it."}
{"id": "lesson-1e8c076df388aaddb21dda87", "text": "Never distil a lesson from a constraint the caller stated in the prompt: that records the instruction, not anything learned from the answer."}
{"id": "lesson-1f2f94dbfefab783b1257370", "text": "Reject non-finite (NaN/Inf) values in authoritative input (movement, physics) before they poison server state and replicate to every observer."}
{"id": "lesson-1f446d234f9170735abdcc5f", "text": "Check address length, validate each part as digit, ensure no negative/over 255."}
{"id": "lesson-1f5ebf03081db951f5cf053a", "text": "Extend game context to include new subsystem pointers for easier integration."}
{"id": "lesson-1f86a9010fd881d22000ce27", "text": "Use a list to accumulate decoded characters and join at the end for O(n) string concatenation."}
{"id": "lesson-1fdf9a610c61e292e8c3ee15", "text": "Design queue consumers for at-least-once delivery plus idempotent processing rather than chasing broker-guaranteed exactly-once, which no distributed queue truly delivers end-to-end"}
{"id": "lesson-1fefd64baeb22997519caf37", "text": "Use dynamic programming with a 2D table to solve longest common subsequence problems in O(m*n) time."}
{"id": "lesson-20a0a141a069bd4b0f25fa55", "text": "Measure a proposed lint or filter against the existing corpus before enabling it: a rule that matches 10 rows and misjudges 8 of them is worse than no rule."}
{"id": "lesson-21099778720bc685cc688cbd", "text": "Use `sorted()` with `key=int` for numeric sorting of string IDs."}
{"id": "lesson-212d7826a394aa9c8510b8bb", "text": "Use `Map` for O(1) access and `Array` to maintain order efficiently."}
{"id": "lesson-212eae2444c43d3723994fb8", "text": "Delete stale build artifacts before invoking a compiler so a post-run existence check genuinely proves this run produced them, not a leftover prior build."}
{"id": "lesson-219786cb082ff77f867b14b2", "text": "Ensure all reliable control-channel bunches are added to `pendingReliable` for retransmission."}
{"id": "lesson-21c03b6ce88c0a9050927236", "text": "Use `os.walk` with sorting for deterministic directory traversal."}
{"id": "lesson-221bcb55179ce6d20df9c53a", "text": "**NONE**"}
{"id": "lesson-22284fb9d7aa6d4fb21c1881", "text": "For interval scheduling (max non-overlapping intervals), sort by end time not start time or duration; greedily keep an interval whenever start >= last_end."}
{"id": "lesson-224afe066684e5a799207e6b", "text": "Use `pygame.draw.rect` and `pygame.draw.circle` for drawing paddles and ball respectively."}
{"id": "lesson-226714aa97a6357fbf807fad", "text": "Attach else: to a for loop to run code only when the loop finishes without break, replacing a manual found-flag variable."}
{"id": "lesson-226db698a1010e4684a9095e", "text": "Check divisibility using a loop with optimized step size for prime number verification."}
{"id": "lesson-22aa8dab0f2e5bfe291161e0", "text": "Use for-loop with index to reverse string characters explicitly."}
{"id": "lesson-22d78f0281d6e1078726e064", "text": "For expensive per-item UI previews, never render on request in the UI thread; queue as pending and process a fixed budget per frame to avoid stutter."}
{"id": "lesson-236b095ae1a3a3c5d302d375", "text": "Use Console.WriteLine() for simple, sequential output of individual values."}
{"id": "lesson-239b66111fd46c45590236ff", "text": "When digit d maps to bit d (1-indexed), extract candidates via `low = cand & -cand; d = low.bit_length()-1; cand &= cand-1` — do not shift cand while incrementing d separately, that desyncs the digit-to-bit mapping."}
{"id": "lesson-24496686f2468e94744fb1df", "text": "Use manual string reversal for compatibility when `std::reverse` is unavailable."}
{"id": "lesson-24759a96c3eafa42f9c930c7", "text": "Use string slicing with `[::-1]` to reverse strings for palindrome checks."}
{"id": "lesson-249d9379d0e8142afe6d05c0", "text": "Use a generator expression with `sum()` for efficient single-pass counting of elements matching a condition."}
{"id": "lesson-2522cde8ec6722fb5e12f040", "text": "Use match/case with type-capture patterns (`int(dx)`), mapping/sequence destructuring, and `if` guards to replace a chain of isinstance/key checks with one ordered, exhaustive dispatch."}
{"id": "lesson-2529b044cb5f2e6e4f2d9e36", "text": "Delete a BST node with two children by copying in the inorder successor's value (leftmost node of the right subtree) then recursively deleting that successor from the right subtree, never swapping node objects."}
{"id": "lesson-2557195c9854f14c2c916e6c", "text": "Use `GdiRgnIntersect`, `GdiRgnSubtract`, `GdiRgnUnion`, and `GdiRgnXor` for region operations to combine rectangles efficiently."}
{"id": "lesson-2574a6c3fe993889863c6194", "text": "Run git rebase -i HEAD~n to squash, reorder, reword, or drop commits before pushing, keeping shared history clean without altering the remote."}
{"id": "lesson-257dc115e77c3a6616f3fdcf", "text": "Use a list of tuples ordered from highest to lowest value for greedy algorithm implementation."}
{"id": "lesson-25c11697787993accf470388", "text": "Use generator expression for concise even number filtering and sum calculation."}
{"id": "lesson-26329a367c03b1e5d33db123", "text": "Cap metric label cardinality - never use raw user IDs, full URLs, or UUIDs as tags - high-cardinality labels blow up time-series storage and crash metrics backends."}
{"id": "lesson-2645c674b61b33e87aec9697", "text": "Never compare floats with ==; use math.isclose(a, b, rel_tol=..., abs_tol=...) since 0.1+0.2 is a different binary64 value than 0.3."}
{"id": "lesson-2678cdfd5e62398f941ee9c0", "text": "Write to disk before running scripts for validation."}
{"id": "lesson-26c086ae6e3c2f4793bf907c", "text": "Manually reverse string using loop for simplicity and brevity."}
{"id": "lesson-26f5f5e3ba6eae35d0b6a399", "text": "Review logs for anomalies and missing expected events to diagnose system behavior."}
{"id": "lesson-2716df0a6d1afa4705a13a47", "text": "Initialize bucket lists before appending words."}
{"id": "lesson-27bf0f91b06a39958ac593eb", "text": "Decide AP vs CP per operation, not per system: a checkout's inventory decrement stays CP during a partition while its product-view page stays AP for availability"}
{"id": "lesson-27e050fe4fcb70d7be7368c5", "text": "Scope a review to the diff itself; if it bundles an unrelated refactor, ask the author to split it into a separate commit or PR first."}
{"id": "lesson-27f0adf0bea1bc603e070ea1", "text": "Read a traceback from the bottom exception upward, not top-down — the root cause and failing line sit at the bottom, imports and dispatch fill the top."}
{"id": "lesson-280584ee1da0b9f9f86195e2", "text": "Profile with a sampling profiler (perf, VTune, py-spy) before touching code; the hot path is rarely where source-reading intuition points."}
{"id": "lesson-280cf60bf8268d8181507a2f", "text": "Handle NULL pointers explicitly in all functions."}
{"id": "lesson-286bbb9048ff14ace9d7283e", "text": "Include `<cstddef>` for `size_t` and `<iostream>` for `std::cout`. Use `std::accumulate` from `<numeric>`."}
{"id": "lesson-28ab9e13d86f353660dde838", "text": "Use String.prototype.split('') to convert a string into an array of characters, then apply Array.prototype.reverse() to reverse the array in place, and finally use Array.prototype.join('') to concatenate the reversed array back into a string."}
{"id": "lesson-28f61935930841ddb76b77e0", "text": "Use itertools.chain.from_iterable(list_of_lists) to flatten one level without a nested loop."}
{"id": "lesson-2a3ae03073dd92b2b4def724", "text": "Modular arithmetic is key to handling list rotation sizes larger than list length."}
{"id": "lesson-2aef5fb193b6db441d744023", "text": "Use ExitStack.enter_context in a loop for a runtime-sized set of context managers; on mid-loop failure it unwinds only entered resources in reverse order, and callbacks also run LIFO."}
{"id": "lesson-2b2457105cfee22d0e47d47f", "text": "Open a large fixed-width-record file with mmap.mmap(fileno, 0, access=ACCESS_READ) and slice by byte offset to random-access records; the OS pages in only the touched bytes instead of loading the whole file."}
{"id": "lesson-2b4470b9beb16dbeb3563ad4", "text": "Use `std::from_chars` for safe numeric parsing in configuration files."}
{"id": "lesson-2b9a71438f3d8b04709794bb", "text": "Use `sorted` with a key function to sort based on integer values for lexicographically ordered string IDs."}
{"id": "lesson-2c27352a2fcabf4ea40477d9", "text": "Pin a heisenbug for reproduction: fix the random seed, force single-threaded execution, and record/replay the exact input sequence — most 'random' failures are actually timing- or order-dependent."}
{"id": "lesson-2c75ad818eb6f705c0e17cdf", "text": "Use `std::sqrt` for efficient prime checking."}
{"id": "lesson-2c844c46a838449a3b22386b", "text": "Check constraints, validate each individually, ensure edge cases are covered precisely."}
{"id": "lesson-2cbcc731b9865ba6a3ffd340", "text": "Use `std::function<void(const Packet&)>` for flexible and safe callback handling in C++."}
{"id": "lesson-2cc7f894e92bf4fb5f5a490f", "text": "Use find_if with isspace to trim whitespace efficiently in C++."}
{"id": "lesson-2d123c1916f6949847406bf4", "text": "Generated files that a CI contract-checker regenerates and diffs must never be hand-edited; edit the source-of-truth document and rerun the generator."}
{"id": "lesson-2d4545b7e8ba5884c440376f", "text": "**Lesson:** Use two-phase combat model to eliminate sequential processing bias and ensure fair kill credit resolution."}
{"id": "lesson-2dd5c88eb03a2bf5acd5264b", "text": "Use Write-Output to print exact string literals in PowerShell scripts."}
{"id": "lesson-2e1b68b3a132a303878aa133", "text": "Use `sizeof` to determine array length instead of `std::ssize`."}
{"id": "lesson-2e56cd1f9734fc283563c3bd", "text": "Use generator expressions with built-in functions like `sum()` for memory-efficient computation."}
{"id": "lesson-2e9959e611d82e63caaa3193", "text": "Use list(dict.fromkeys(seq)) to dedupe while preserving first-seen order, unlike set() which loses order."}
{"id": "lesson-2ed5faaed9560cc19e32b4b4", "text": "Mouse-capture delta math must use one consistent coordinate space (client vs screen); mixing them produces a huge fake delta that instantly saturates camera pitch/yaw clamps."}
{"id": "lesson-2f6d8d4b3c57ccb30f0aed61", "text": "Give a pytest yield-fixture scope='module' to share one setup/teardown across every test in the file instead of re-running it per test."}
{"id": "lesson-2fa1ceba43aefaef61602504", "text": "Ensure script tags are placed correctly before any JavaScript code that uses them."}
{"id": "lesson-2fd2ef4fcd2fe384989c1332", "text": "Benchmark an inference server with the caller's own options, not the API defaults: measuring Ollama without num_gpu shows a conservative auto-offload and a phantom context cliff that the real caller never hits."}
{"id": "lesson-2fe27056bac7dffc5ad9f25d", "text": "**Check index validity against current PackageMap before using it.**"}
{"id": "lesson-300ba8e0580a4d7821ee67df", "text": "Split \"key=value;...\" with str.partition('=') per segment, not str.split('='), so values may contain extra '=' signs."}
{"id": "lesson-3037d6b3ebce10dc4246444e", "text": "Use `-join` and `iex` to evaluate expressions in PowerShell strings."}
{"id": "lesson-3040678cbc72c04e964938c3", "text": "Use `defaultdict` for streaming group-by aggregation to avoid key errors and simplify initialization."}
{"id": "lesson-3061a4a60d769252b33e9c60", "text": "Catch the N+1 problem by asserting query counts in integration tests, then fix it with a JOIN or a single batched IN-list fetch instead of one query per row."}
{"id": "lesson-30bb6feda68de84796927f17", "text": "Use heapq.nsmallest(k, data)[-1] to get the kth smallest without a full O(n log n) sort."}
{"id": "lesson-31245e0c8879dfc786caca86", "text": "Use `raise NewError(...) from original` to set __cause__ and __suppress_context__=True explicitly; omitting 'from' still links the error via __context__ but leaves __cause__ None and __suppress_context__ False."}
{"id": "lesson-316ca5869f884fd3dde48f96", "text": "Build JSONL with json.dumps(record, sort_keys=True) per line and '\\n'.join, so key order never breaks byte-for-byte comparisons downstream."}
{"id": "lesson-3211680e9ee89956eec11151", "text": "Use `str.split()` to decompose strings into components for validation, then check each component's format and range."}
{"id": "lesson-326610a654eaa846cfa6b09b", "text": "Use a visited-set guard after heapq.heappop in Dijkstra since heapq has no decrease-key; skip stale (dist,node) pairs left in the queue."}
{"id": "lesson-32678a8060297d420716b605", "text": "Use `console.log` to print text immediately and terminate the program."}
{"id": "lesson-3269a80e4a174fc8f7d9c1dd", "text": "Exploit the bottleneck-path theorem: the minimax edge on a path equals the max edge on that pair's MST path, built via Kruskal with union-by-rank/path-compression."}
{"id": "lesson-3272ea3c03be4f4819a0c981", "text": "Merge two already-sorted iterables in O(n+m) with list(heapq.merge(a, b)) instead of concatenating and re-sorting."}
{"id": "lesson-327d89b665af0f668dc2f825", "text": "Build a set of target characters (e.g. set('aeiouAEIOU')) for O(1) membership checks inside a sum() generator."}
{"id": "lesson-328267d1ce7c8920caad4cae", "text": "Use `csv.DictReader` for streaming CSV rows efficiently."}
{"id": "lesson-32a7259d9fddce3bd35e2657", "text": "Sort input vector, check size for median calculation, return average of middle elements or single middle element if even-sized."}
{"id": "lesson-32bcba763462e9a0317b3494", "text": "Use `std::vector` for dynamic arrays and ensure O(1) operations with swap-and-pop."}
{"id": "lesson-337a678a3ef6d97d4ad816cf", "text": "For in-place array reversal, swap arr[lo]/arr[hi] and step lo+=1,hi-=1 while lo<hi; the function mutates and can still return the same object."}
{"id": "lesson-33d28bcd9a5a7197366ee7f1", "text": "Convert array elements to integers using `Convert.ToInt32` in a generic sum function."}
{"id": "lesson-3413753a47f0e15694108b06", "text": "Use array slicing and `-join` for string reversal in PowerShell."}
{"id": "lesson-342cddd3561ef70bec372943", "text": "Measure elapsed durations with time.monotonic(), not time.time(), because wall-clock reads can jump backward from NTP/manual corrections and produce negative or nonsensical diffs."}
{"id": "lesson-348680f01a628ab95c5f9619", "text": "Assign through a full slice (`lst[:] = ...`) instead of rebinding `lst = ...` to rotate/replace contents in place, keeping every existing alias to that list object seeing the update."}
{"id": "lesson-34903e7b44e1d5818ad99cce", "text": "Apply amortized analysis, not worst-case-per-op: a doubling dynamic array gives O(1) amortized append despite occasional O(n) resizes; measure total time over N ops, not one op."}
{"id": "lesson-34ea078a38fd7aa0a003aa02", "text": "For coin-change minimum-coins DP seed dp with float('inf') sentinels (not 0) so unreachable amounts stay distinguishable from a true zero-coin base case."}
{"id": "lesson-34f70a824d4fd2b5d582dd82", "text": "A stack frame over 4 KB on the Windows ABI emits a __chkstk probe, which a freestanding -nostdlib module cannot resolve; make the large buffer static or shrink it."}
{"id": "lesson-3557c7b73914050a640f7c2b", "text": "Use std::cout with std::endl to print each number on a separate line in C++."}
{"id": "lesson-360781d58f4e19140d83c3f1", "text": "Use LEFT JOIN to include all characters, even those with zero sessions."}
{"id": "lesson-36361484a5eea71375834943", "text": "Use Write-Output to explicitly print values in PowerShell scripts for predictable console output."}
{"id": "lesson-363b5d885f787903b146e12c", "text": "Use a dictionary for mapping integers to Roman numerals for cleaner code."}
{"id": "lesson-365d1be325a502d70baf3ee5", "text": "Use heapq.nsmallest() for efficient partial sorting when you only need the k smallest elements."}
{"id": "lesson-368d8e05b4fb980cc2fed227", "text": "To move zeros to the end preserving order in one pass, keep a write pointer and swap nums[write]/nums[read] only on non-zero reads instead of overwriting-then-backfilling zeros."}
{"id": "lesson-36f1369001b6b837229dea32", "text": "Ensure inclusive conditions in if-statements for accurate logic execution."}
{"id": "lesson-36fc063bebfbfcf3dc2c82c0", "text": "Check leap years with y % 4 == 0 and (y % 100 != 0 or y % 400 == 0), not just divisible-by-4."}
{"id": "lesson-37251d975a467e4d05e22e01", "text": "Enforce read/write quorums with R + W > N so every read set overlaps at least one up-to-date replica, trading some availability for consistency without full consensus."}
{"id": "lesson-37a973e468c7969e0de46b8c", "text": "Use `int(n**0.5)` for efficient prime checking."}
{"id": "lesson-38612b2a0a03414d429bca3d", "text": "Replicate a log via majority-quorum consensus like Raft or Paxos so committed entries survive any minority-node failure and split-brain dual-leader writes are structurally impossible."}
{"id": "lesson-388b2575f70e575d0554c7fa", "text": "Use find and substr to split string efficiently handling delimiters including empty tokens."}
{"id": "lesson-38a5068591e8125d44af0c3a", "text": "When bumping a wire/ABI protocol version, update the exporter, the consumer's static asserts, and the test's export list together in one change, or version skew fails silently at runtime."}
{"id": "lesson-38dd5850f3fdf4ead1cfb720", "text": "Use `Console.WriteLine` for printing each number on its own line."}
{"id": "lesson-3911a23f8830cf9f179d2ee3", "text": "Wrap a resource pool access in `with threading.Semaphore(n):` to cap concurrent holders at n regardless of how many threads race for it."}
{"id": "lesson-3924d7f7b707ee546717a94a", "text": "On a multi-threaded core dump, run `thread apply all bt full` in gdb (or `~*kb` in windbg/cdb) to find which thread held the lock at crash time, not just the faulting thread."}
{"id": "lesson-394d03fe1b75c97637402cb7", "text": "Write CSV with csv.DictWriter(quoting=csv.QUOTE_MINIMAL, restval=\"\") so commas/quotes/embedded newlines round-trip safely through DictReader."}
{"id": "lesson-396a3dc093fcac6ddac5d2ac", "text": "Use explicit loop termination and conditional checks to avoid syntax errors and ensure correct prime number detection logic."}
{"id": "lesson-3994f774a5317d97afa81e52", "text": "Use `console.log` for printing to the console in JavaScript."}
{"id": "lesson-3a2f576831b1dfd1157dbec5", "text": "Override metaclass __prepare__ to return a dict subclass whose __setitem__ rejects re-assigning the same class-body name, catching silent method-shadowing bugs at class-creation time."}
{"id": "lesson-3adf12fc25d5a3ec626fc434", "text": "Treat any non-zero exit code from a sandboxed untrusted child as a crash, not just timeouts, since it can't be trusted to signal failure any other way."}
{"id": "lesson-3aef58c96c7fec5f5b24aa67", "text": "**Immutable Data Structures for Consistency and Testability**"}
{"id": "lesson-3b335aba4cd1b7add937c77b", "text": "In recursive BST insert, always reassign the return value back to node.left/node.right (node.left = insert(node.left, val)) so new nodes actually attach to the tree."}
{"id": "lesson-3b49370dc10d038541073ad9", "text": "Check bipartiteness with a weighted Union-Find carrying an XOR parity bit per node (relative to its parent): union two endpoints as 'must differ', and if find() shows they already share a root with equal parity, the edge closes an odd cycle -> not bipartite."}
{"id": "lesson-3b7532041d54269f846f77e9", "text": "Use `std::unordered_map` for efficient participant-to-channel mapping and state management."}
{"id": "lesson-3b9d53c1fe71754412d84311", "text": "Use console.log() for direct output when exact string formatting is required."}
{"id": "lesson-3bc6617c09c30ace419acfcc", "text": "Use Console.WriteLine() for simple output formatting in C# console applications."}
{"id": "lesson-3bf36c57899d409fcbc77525", "text": "Add explicit surface parameters to drawing functions to support rendering on different surfaces like minimaps."}
{"id": "lesson-3bfc36d5c86550c8d1b5c124", "text": "Kadane's algorithm: at each step set cur=max(x, cur+x) then best=max(best,cur) to find max subarray sum in one linear pass."}
{"id": "lesson-3c5d73c94d349cd6f48a604e", "text": "Sort by value then slice, use lambda for concise sorting."}
{"id": "lesson-3c7ffcfc50e5b4599c5d46a1", "text": "Use generator.throw(exc) to inject an exception at the suspended yield point and wrap the loop body in try/finally so cleanup (e.g. appending a 'closed' marker) runs on close() too."}
{"id": "lesson-3cb49e7d53cf534ed0ab1759", "text": "Maintain a fixed-size min-heap with heapq.heappush then heapq.heappushpop once full to track the k largest elements in O(n log k)."}
{"id": "lesson-3cba3cdc6f539be0d2d171f0", "text": "Pass func=min (or max) and initial= to itertools.accumulate to get a running extreme (e.g. lowest balance reached), not just a running sum."}
{"id": "lesson-3d758c3ba60cc263e03a0855", "text": "Compute lcm as abs(a*b)//math.gcd(a,b), but special-case a==0 or b==0 first since gcd(0,0)==0 makes the division blow up."}
{"id": "lesson-3d8d2c5e103a21f172444845", "text": "Treat every released response field as a permanent contract: add new fields as optional, and never remove or repurpose one without a Sunset header and a full deprecation window."}
{"id": "lesson-3d92b87062087ddc9338f3f4", "text": "Use `Write-Output` for quick console printing in PowerShell scripts."}
{"id": "lesson-3da95d646ae4bce35727addc", "text": "Require an Idempotency-Key header on POST-create endpoints; store (key, request hash, response) so a retried request replays the cached response instead of double-creating the resource."}
{"id": "lesson-3e01986136719df3638bcf9b", "text": "Use Doxygen comments for clear, structured documentation."}
{"id": "lesson-3e899aabe0be064d4eb95b08", "text": "Push a sentinel object through queue.Queue so the consumer thread knows to exit, instead of guessing when the producer is done."}
{"id": "lesson-3e8d624fa0d3badaf6c75f79", "text": "For 'minimize the max/min capacity' problems, binary search capacity in [max(nums), sum(nums)] and use a linear greedy feasibility check (can_ship) as the monotonic predicate."}
{"id": "lesson-3efc8a51fbe4483215de12d7", "text": "Build an O(1) LRU cache with OrderedDict.move_to_end(key) on access/insert and popitem(last=False) to evict the least-recently-used entry."}
{"id": "lesson-3efd923cb9be79a5fa1c4757", "text": "Initialize dictionary keys before incrementing their values to avoid `KeyError`."}
{"id": "lesson-3f4a1df589d83d64daf853a5", "text": "Use recursion with string concatenation to flatten nested dictionaries into dotted paths."}
{"id": "lesson-3f91576c74c2400939e8419b", "text": "Use regex alternation with the float branch first, r'-?\\d+\\.\\d+|-?\\d+', so findall grabs whole decimals instead of splitting on the dot."}
{"id": "lesson-400b6d29c77d27bad4b1e483", "text": "Standardize every error response on one envelope, e.g. RFC 7807 (type, title, status, detail, instance), so clients write one error handler instead of one per route."}
{"id": "lesson-4021683d571465efa3a40add", "text": "Use `std::array` for fixed-size collections to ensure consistent access patterns and avoid runtime bounds checking."}
{"id": "lesson-40da0a2102d5d286e54faf98", "text": "For trapping rain water with two inward pointers, advance whichever side has the smaller running max, since that side's max is the true water ceiling there."}
{"id": "lesson-40f7fce9ab18c93e270011b7", "text": "Build a trie from nested dict nodes with an explicit is_end flag, not just leaf-presence, so 'car' inserted doesn't falsely make search('ca') true."}
{"id": "lesson-41289a7d25961aa2e9ae3133", "text": "Use a list of tuples mapping Roman numerals to values in descending order for greedy algorithm conversion."}
{"id": "lesson-4166f53df0eb678aa91bd66b", "text": "Use `Write-Output` for exact string output in PowerShell."}
{"id": "lesson-4183da9d57bc6f2b0c70008c", "text": "When cloning production databases into a parallel unified environment, only bump version/schema-check rows to satisfy the new code's checks; never alter schema, so the originals stay untouched rollback points."}
{"id": "lesson-41cfc708d39d274991698d6a", "text": "An index on a low-cardinality column (e.g. boolean, status flag) can make the planner slower via random I/O than a sequential full scan."}
{"id": "lesson-41f255356e30ca7e1b880df1", "text": "Use binary search algorithm with O(log n) time complexity for sorted lists."}
{"id": "lesson-423a8b5d19e9c1894446e820", "text": "Check if a number is prime using a loop up to its square root."}
{"id": "lesson-423bc9994d33d7c074723f7d", "text": "**Split built-in combat into two phases: planning and resolution to ensure fairness and determinism.**"}
{"id": "lesson-4254ec9a4e1d6083d4793df1", "text": "Convert integers to binary, handling edge cases with simple logic and checks."}
{"id": "lesson-43502fb6071e1d7feec528b9", "text": "On a parse error, synchronize by skipping tokens until a statement boundary (semicolon, block keyword) so one typo doesn't cascade into hundreds of spurious errors."}
{"id": "lesson-43acd3d5de165ac9dbe07b5f", "text": "Precompute an O(n^2) is-palindrome table by increasing substring length before the min-cut interval DP so each is_pal[i][j] check is O(1) instead of rescanning the substring."}
{"id": "lesson-43ae8db552c70e4b054681a8", "text": "Give a reusable descriptor __set_name__(self, owner, name) so it derives its private storage key from the attribute name it's bound to."}
{"id": "lesson-4468f91c13a82d660793cf81", "text": "Use `std::vector<std::string>` for storing and manipulating tokenized command line arguments."}
{"id": "lesson-44b8168e4b4f2a1d9f437b1e", "text": "Use Set.size to verify uniqueness of elements when checking divisibility conditions."}
{"id": "lesson-45701ea5078c856573825e66", "text": "Build a lexer by alternating named groups in one compiled pattern and dispatch on match.lastgroup in finditer, instead of testing each group for None."}
{"id": "lesson-45d335cf7593431b16ce028e", "text": "heapq has no decrease-key: implement update/remove by keeping an entry_finder dict from item to its [priority, count, item] list, mutating the item slot to a REMOVED sentinel and skipping sentinel entries lazily on pop."}
{"id": "lesson-4622f81820fbdac6bd2756a1", "text": "Force a decimal point on codegen'd C++ float literals (e.g. std::showpoint) — a bare digit-sequence plus an f suffix like 2f is not a valid floating literal and fails to compile."}
{"id": "lesson-4705884735cc78b7ee204cf8", "text": "A syscall number added to the enum but not to the ABI document has no declared capability gate, which is exactly what the ABI exists to pin."}
{"id": "lesson-478c411a224dcfd663c2a49f", "text": "Use substr to check prefix/suffix match efficiently avoiding unnecessary copies."}
{"id": "lesson-47a53e7d76c95f3b47c76428", "text": "Fuse lexical (FTS) and semantic (embedding) search with reciprocal rank fusion, then apply a cosine similarity floor on the fused candidates so irrelevant hits are dropped rather than merely re-ranked."}
{"id": "lesson-480b8b34829af7683202fb75", "text": "For iterative preorder traversal with an explicit stack, push the right child before the left child so popping processes left first, matching root-left-right order."}
{"id": "lesson-486186cf91da2ce4028e9595", "text": "Normalize rotation with k %= len(seq) first, then split as seq[-k:] + seq[:-k] to handle k >= len safely."}
{"id": "lesson-486f3019c528f5c6e73ab22f", "text": "Flag mutable default arguments like def f(x=[]) — the default object is created once at definition time and shared across every call, leaking state silently."}
{"id": "lesson-48c1ba7616c8a155f050fd30", "text": "Convert underscores to camelCase efficiently using string iterators and conditions."}
{"id": "lesson-48d0cca6f53f827654a12411", "text": "Use `GROUP BY` to merge duplicate rows by summing their counts."}
{"id": "lesson-48e2f6a3ab561edfd473a358", "text": "Use dict.get(key, 0) + 1 to tally counts in one pass without checking key existence first."}
{"id": "lesson-48eba311c5ed638a5a7a57cd", "text": "Reduce gcd across a whole list using functools.reduce(math.gcd, nums) instead of a hand-rolled pairwise loop."}
{"id": "lesson-4906fad4cbd23be7b1fb53d1", "text": "Use iterative approach for Fibonacci to avoid recursion overhead and potential stack overflow."}
{"id": "lesson-4966cb1a20bee98855485f4d", "text": "When reading length-prefixed binary results from an untrusted subprocess, bounds-check the declared count before allocating to avoid a crash on corrupt or truncated output."}
{"id": "lesson-49990f3e18ead112d1ae15fe", "text": "Use string interpolation with double quotes to explicitly convert and print numeric results in PowerShell."}
{"id": "lesson-499ebc388d60a68ca376201d", "text": "Pair a json.JSONEncoder.default() tagging objects with __type__ and a matching object_hook= in json.loads to round-trip custom classes through JSON."}
{"id": "lesson-4a317bbf0ffa78e7d3139ffc", "text": "Order composite index columns to match equality predicates first, then range/sort columns, so the index also satisfies ORDER BY without an extra sort step."}
{"id": "lesson-4a403261e0df60057bdd26d9", "text": "Add full jitter to exponential backoff (delay = random(0, base*2^n)) rather than fixed exponential, so retrying clients desynchronize instead of hammering in lockstep"}
{"id": "lesson-4a5fce3139ff5cc05dd005fb", "text": "Use pygame.key.get_pressed() for smooth, continuous input handling in game loops."}
{"id": "lesson-4abeac535adad11c43d03cbf", "text": "Save HTML code to an `.html` file and open in a web browser."}
{"id": "lesson-4ace9390f4b83fc9cfcc4f8c", "text": "Replace recursive Fibonacci with a bottom-up loop using tuple reassignment `a, b = b, a + b` for O(1) space, no recursion limit risk."}
{"id": "lesson-4af5975348194ba956e6fbb6", "text": "Check object-level authorization on every request by verifying the authenticated user can access the specific resource ID requested, not just that a valid session exists — this is what stops IDOR."}
{"id": "lesson-4afd27b31162fa9611495bad", "text": "Define functions before using them in Python."}
{"id": "lesson-4b19232f42a31d2147e7f4c3", "text": "Use tuple unpacking for simultaneous variable updates to avoid temporary variables in iterative calculations."}
{"id": "lesson-4b1ec9fbb319a17833427652", "text": "Identify edge cases and optimize loop to check divisibility up to sqrt(n)."}
{"id": "lesson-4bd951b46b9a9832eb4f2d08", "text": "Switch to QUIC over UDP when TCP head-of-line blocking across multiplexed streams dominates latency, since QUIC gives each stream independent loss recovery"}
{"id": "lesson-4c5be2a323fe41bc53dcd0fa", "text": "Apply Kahan summation, tracking compensation c=(t-total)-y each step, when summing floats spanning huge magnitude gaps (1e16 mixed with ~1.0 terms); naive sequential sum loses the small terms entirely."}
{"id": "lesson-4c60a23a6b8b3c77852d691b", "text": "Use two variables to track first and second largest values in a single pass through the list."}
{"id": "lesson-4cace1527c414509881ce6d3", "text": "Use `str.split()` to break strings into lists and `list.count()` with `set()` for efficient word frequency counting."}
{"id": "lesson-4cccfd4084a258791e50ea43", "text": "Use Counter subtraction/&/|/+ for multiset shortfall/overlap/union math; subtraction clamps at zero unlike plain dict subtraction."}
{"id": "lesson-4d038e12dda2263a8efcea85", "text": "Use indexing and -join to reverse strings in PowerShell."}
{"id": "lesson-4d07998bd31ae2434172ac96", "text": "Use cursor-based pagination (opaque token of last sort key + id) instead of offset/limit — offsets skip or duplicate rows when items are inserted or deleted mid-scroll."}
{"id": "lesson-4d084cb08d364d4874bf55b9", "text": "Sort candidates so a candidate exceeding remaining lets you `break` (not `continue`) to prune the whole remaining branch in one step."}
{"id": "lesson-4d10e926d58c41e6123fc033", "text": "Use a for loop to iterate through array elements and accumulate their sum in a variable."}
{"id": "lesson-4d128af1671d988927658f55", "text": "Use (?P<name>...) named groups and match.groupdict() to pull fields out by label instead of brittle numeric group(1), group(2) indices."}
{"id": "lesson-4e0b48f7db37d0555377cb45", "text": "Use `foreach` loop for iterating arrays in C#."}
{"id": "lesson-4e1beaf02ba8d1dc596dbb54", "text": "Structure each test as Arrange (build state), Act (one call), Assert (check outcomes only) so failures show a single cause."}
{"id": "lesson-4e7b7d9bb1eec9e7e290d17d", "text": "Catch use-after-free and double-free with AddressSanitizer's poisoned redzones and quarantine: freed memory is marked unaddressable and delayed from reuse so an errant access faults immediately at the culprit line."}
{"id": "lesson-4eb530cbccac99c8ccec8211", "text": "Clamp frameTime before using it in calculations to prevent negative values and ensure consistent behavior."}
{"id": "lesson-4f529bdcb10a40ea7f60e904", "text": "To find all occurrences of a pattern in text with the Z-algorithm, concatenate pattern + sentinel + text (sentinel absent from both) and scan for z[i] >= len(pattern), avoiding false matches that could span the join."}
{"id": "lesson-4f923b24063726506212a311", "text": "Use `sizeof` correctly in loops to iterate over arrays."}
{"id": "lesson-4fb1e2290c9f335a9e0502c9", "text": "Use `defaultdict` for efficient counting and tracking first occurrences."}
{"id": "lesson-502e5282e7a6591030833b0d", "text": "Use `TypeVar(\"C\", bound=\"Comparable\")` to require only that T support `__lt__`, letting a generic min-finder accept any class implementing that one method."}
{"id": "lesson-50591dcc9ee8b52358c45595", "text": "Use `print()` function for output in Python."}
{"id": "lesson-513f3236cc62b1461fc2d1cb", "text": "Use Write-Output for direct string printing in PowerShell scripts."}
{"id": "lesson-515379f6bba3487a43e28b92", "text": "Use `foreach` loop for iterating through a range of numbers and `Write-Host` to print each number on a new line."}
{"id": "lesson-51be5629f32c90c5abcfdcfe", "text": "Avoid mutable default arguments in function parameters. Use `None` as a default and initialize inside the function if necessary."}
{"id": "lesson-51d8ac212443e1a6169c736d", "text": "Key a memoizing closure with (args, frozenset(kwargs.items())) to hash keyword calls, and expose mutable closure state (hit/miss stats) via functions assigned as wrapper attributes."}
{"id": "lesson-5229bd84dd72c51d98cef3df", "text": "Separate internal fragmentation (oversized blocks from size-class rounding) from external fragmentation (free memory scattered in unusable slivers): fix the first with tighter size classes, the second with compaction or pooling."}
{"id": "lesson-5236b0d51711b6255f935a09", "text": "Handle EOFError gracefully with try-except block."}
{"id": "lesson-52527b75dba864f5dbe025b2", "text": "Compute Levenshtein distance with a rolling two-row DP (prev/curr lists sized len(b)+1) instead of a full O(n*m) matrix to save memory."}
{"id": "lesson-528a4cbd5f031a382ccdcf00", "text": "Include `<numeric>` for `std::accumulate`."}
{"id": "lesson-52ab754dc06cc99b94a10e74", "text": "Rate-limit with a token bucket sharded per (API key, endpoint), returning Retry-After and X-RateLimit-Remaining headers, so one noisy client can't starve others on shared infrastructure."}
{"id": "lesson-52b20b2538a6602be15aade0", "text": "Use `enumerate` with `start=1` for 1-based line numbering."}
{"id": "lesson-52c4d2a736d7526428b9b8e6", "text": "For prod-only crashes, symbolize addresses against the exact release build's archived debug info (PDB/dSYM) versioned per build; dev-build offsets never match."}
{"id": "lesson-52e085154bcbbcabebf13936", "text": "Use clear naming, leverage built-ins like ** for simplicity in power operations."}
{"id": "lesson-536a156b4c9fba9552e35951", "text": "Every subclass of a __slots__ class must redeclare __slots__ (even empty) or it silently regains a __dict__ and loses the memory/attribute-restriction benefit."}
{"id": "lesson-53a3fc7d10840607cd5f1de3", "text": "Use `console.log()` for simple output formatting when each value needs to be printed on a separate line."}
{"id": "lesson-540766f0f8a5d7b001c549c2", "text": "Use `pygame.time.Clock` to control frame rate for consistent gameplay across different hardware."}
{"id": "lesson-546eff6a226c2ce0cd6edfea", "text": "Flag a clever one-liner that needs a second read to parse even when it is correct — the readability tax compounds across every future reader."}
{"id": "lesson-5482cda3699ff69cd8cbb835", "text": "Review a test by mentally reverting the fix it covers; if the test would still pass without the code change, it isn't actually testing the bug."}
{"id": "lesson-553a0180592fa2ae8b8af2f9", "text": "Check vertical distance using `std::abs` for tolerance comparison."}
{"id": "lesson-55800e9895b5b7d88e9c27b2", "text": "Use list comprehension and join for efficient string concatenation with JSON.dumps."}
{"id": "lesson-5593d2851930123c0c9f9800", "text": "Use `reduce` from `functools` to apply a function of two arguments cumulatively to the items of an iterable."}
{"id": "lesson-5606453c55fcc51531bd8cef", "text": "Use threading.Event with event.wait(timeout) inside a worker's loop condition so a setter thread can cleanly stop it instead of killing it."}
{"id": "lesson-5748471a273bb57773e9832a", "text": "Read hardware counters (perf stat: cache-misses, branch-misses, IPC) to classify a hot function as memory-bound vs compute-bound before choosing an optimization strategy."}
{"id": "lesson-5765e8e390b486ef528938f9", "text": "Apply Extract Method whenever a comment is needed to explain a code block: the extracted method's name replaces the comment and becomes a seam you can test in isolation."}
{"id": "lesson-57c8ca8bde07f396763d3d43", "text": "Use dynamic programming to efficiently compute Levenshtein distance with optimal substructure."}
{"id": "lesson-57e10870a363964b442181bd", "text": "Use console.log() for simple output instead of complex string concatenation or formatting."}
{"id": "lesson-57ecd1b3fb17d0ae577d989f", "text": "Use `asyncio.Semaphore` for worker slot management to limit concurrent workers safely."}
{"id": "lesson-58378c4e643b8012bb89c4ef", "text": "Give every recursive function a base case checked before the recursive call, e.g. `if n == 0: return 1`, or it never terminates."}
{"id": "lesson-583f379f58bd1f41416a151e", "text": "Run binary search directly over an mmap'd sorted fixed-width record file using struct.unpack_from at computed byte offsets, giving O(log n) disk touches with zero full-file load for on-disk index lookups."}
{"id": "lesson-587d3a08795b8e7b5626b942", "text": "Stamp a version field on serialized payloads and chain small per-version migrate_vN_to_vN+1() functions keyed by dict, applied in a loop until target_version."}
{"id": "lesson-58a1ef060966ec8a87077a42", "text": "Use bitwise operations to efficiently count set bits, loop until all bits are processed."}
{"id": "lesson-58cf0e04d863acbd58b5d329", "text": "Reuse keep-alive connections via a pooled HTTP client instead of opening one socket per request, avoiding repeated TCP handshake plus TLS negotiation cost"}
{"id": "lesson-594b00e690564df4fe651c12", "text": "Enforce interface contracts in type.__new__ by checking inspect.signature(namespace[method]).parameters against a required param list before the subclass is even created."}
{"id": "lesson-59af7285d3580cd4cd0617a9", "text": "Choose decimal.ROUND_HALF_EVEN (banker's rounding, Decimal's default) over ROUND_HALF_UP when rounding many .5-boundary values in a batch, since HALF_UP systematically biases the aggregate total upward while HALF_EVEN does not."}
{"id": "lesson-59d7b5d5466a7d6d07b7db5e", "text": "Use `zip(*matrix)` to transpose a matrix efficiently in Python."}
{"id": "lesson-5a473a66c7389eb3ca24539e", "text": "Ensure `if` condition includes equality for inclusive checks."}
{"id": "lesson-5a5a4722ee96101875593d6b", "text": "Capture a heisenbug's one true execution with a record-and-replay debugger (rr), then replay it deterministically with reverse-continue and reverse-watchpoints."}
{"id": "lesson-5a6329a014485e3c5b4cbd44", "text": "Use regular division (`/`) for floating-point results instead of integer division (`//`)."}
{"id": "lesson-5a6cd59e7d9d414c7784b40e", "text": "Chain `for row in matrix for n in row` left-to-right in one comprehension to flatten nested iterables while filtering, instead of nesting comprehensions or building a temp list."}
{"id": "lesson-5af562cae5e8d6c068cecd2d", "text": "Mark a visited grid cell in place with a sentinel char and restore it after recursion instead of allocating a separate visited-set per DFS path."}
{"id": "lesson-5afc9938de7f4a3a77106b1f", "text": "Use `ForEach-Object` with `-join` for string concatenation in PowerShell."}
{"id": "lesson-5b0f29c7575200ef7482d402", "text": "Diagnose HTTP/1.1 head-of-line blocking by counting stalled requests behind one slow response on a shared connection, then fix with pipelining-safe HTTP/2 multiplexed streams"}
{"id": "lesson-5b39c319517406a81f492c99", "text": "Use `[math]::sqrt()` for square root calculations in PowerShell instead of undefined `sqrt()` function."}
{"id": "lesson-5b478c0ffa1fc41fc7603610", "text": "Use Write-Output to print each item on its own line in PowerShell."}
{"id": "lesson-5b5456bf8937b964fc931fae", "text": "Use `collections.Counter` for frequency counting and `.most_common()` to find the most frequent item efficiently."}
{"id": "lesson-5ba5ac57dbb5624cef1ef22d", "text": "Use `std::atomic` for thread-safe state transitions in concurrent systems."}
{"id": "lesson-5bad052c3679b4360f32ca6d", "text": "Use list comprehension for concise table generation."}
{"id": "lesson-5c3512076058a2c902e84e1f", "text": "Use collections.deque for O(1) pops from both ends of a queue."}
{"id": "lesson-5c6fa938ee90fcbfca851f00", "text": "Use heapq.nlargest(n, data, key=...) to get top-n by a key in O(n log k) instead of sorting everything."}
{"id": "lesson-5dc65e362be979871dc6ff72", "text": "Use `-join '+' | Invoke-Expression` to compute the sum of array elements in PowerShell."}
{"id": "lesson-5de40a569277c3471e28f474", "text": "Use `__declspec(dllimport)` for importing Windows APIs in freestanding C code."}
{"id": "lesson-5e33a9b5fc1313b69e79e539", "text": "Ensure `end` is inclusive in range by adding 1 to `end`."}
{"id": "lesson-5f2e189417a4c66c037e2f86", "text": "Isolate each downstream dependency into its own bulkhead (dedicated thread pool or connection pool) so one slow dependency can't starve capacity needed by unrelated requests."}
{"id": "lesson-5f57c401bd79ab97bd0c8db9", "text": "The KMP failure function doubles as a periodicity detector: n - pi[n-1] gives the smallest additive period, and n % period == 0 tells you if the string is an exact whole-number repetition of that period."}
{"id": "lesson-5f970b21ec8e833d1681cccb", "text": "Manually reverse string using two-pointer technique."}
{"id": "lesson-5fe31e0a22da1ec5d4fd8828", "text": "Share mutable state across Pool workers with mp.Value/mp.Array plus Pool(initializer=..., initargs=...); plain objects passed as task args are pickled per-worker copies, mutations never propagate back."}
{"id": "lesson-5ff70962077ccee37fff0ca6", "text": "Use floating-point division (`/`) instead of integer division (`//`) for accurate averages."}
{"id": "lesson-600b5ac7297a7164f708e286", "text": "Use `pygame.draw.polygon` for ship rotation and movement."}
{"id": "lesson-601d135b7e667c06389f075d", "text": "Use `json.loads` for parsing JSON strings and handle exceptions to skip malformed lines."}
{"id": "lesson-602ab6bc0177bd16a07e838a", "text": "Use split to separate words, capitalize each, then join back ensuring proper capitalization."}
{"id": "lesson-609166d62058ac0d12af33cf", "text": "Use correct constants and iterate over data efficiently in a loop."}
{"id": "lesson-60b821ec9cb07898185ce5ef", "text": "Use Kahn's algorithm for topological sorting with a queue to process nodes with zero in-degree first."}
{"id": "lesson-6111cdb3bafaca7cd2acb373", "text": "Use string indexing and concatenation to reverse a string manually when standard methods fail."}
{"id": "lesson-6119434a47ca5829fdac99fb", "text": "For binary-search-on-the-answer, use lo,hi=0,n with mid=(lo+hi+1)//2 and lo=mid on success to converge on the largest feasible value without an off-by-one infinite loop."}
{"id": "lesson-613886f94b706fdc6c5eac18", "text": "Use `console.log` for immediate output in scripts."}
{"id": "lesson-615d02e299dea8ab90db9d80", "text": "Precompute row/col/3x3-box used-digit sets once, then maintain them incrementally during backtracking instead of rescanning the board per candidate check."}
{"id": "lesson-615dce221268704439512958", "text": "Use `str.split()` and `str.join()` with generator expressions for efficient string transformation pipelines."}
{"id": "lesson-61cb786cff7e710e6cb1e252", "text": "Use `argparse` with custom validation and precedence handling for CLI options."}
{"id": "lesson-61e8302d86573b62834923ba", "text": "Use `str.split()` to break strings into lists and `list.count()` with `set()` for efficient unique element counting."}
{"id": "lesson-621d8e269eceb8b4e63431bc", "text": "Use Array.reduce() to accumulate values from an array into a single result."}
{"id": "lesson-62219bbf45c7f22199b1676d", "text": "Use slicing for concise string reversal in Python."}
{"id": "lesson-62629d0a687be5022f0a12e3", "text": "Read the query plan for a seq scan on a filtered large table before adding an index; every new index adds write-amplification on INSERT/UPDATE/DELETE."}
{"id": "lesson-62b8c8fa9850c0f319812671", "text": "Use pool.imap_unordered when task order doesn't matter: results arrive in completion order, letting downstream consumers start on fast-finishing items sooner than imap allows."}
{"id": "lesson-63160a9d89d28a4aff809c94", "text": "Use rfind to find last '/' or '\\', then extract substring efficiently."}
{"id": "lesson-63570571a9a2ab891f766f8b", "text": "Remove redundant direct broadcasts to avoid duplicate notifications."}
{"id": "lesson-6368c42b886ce5131550a7df", "text": "Sandbox untrusted generated code on Windows: launch CREATE_SUSPENDED, assign to a Job Object with a memory limit, then resume — never let it run unconfined."}
{"id": "lesson-63838e0f8bbc25276ca647ae", "text": "Prefix non-blocking style comments with 'nit:' to separate must-fix issues from preference, so trivial feedback never stalls a mergeable change."}
{"id": "lesson-6385343448dddfa7c282ff96", "text": "Encode the algorithm, version, and cost parameters inside the stored hash string itself (e.g. $argon2id$v=19$m=...) so verification is self-describing and old hashes upgrade without breaking."}
{"id": "lesson-639586e70e1c663ba0c4eb07", "text": "Use simple heuristics for AI, check corners and center first; handle user input gracefully."}
{"id": "lesson-63bb93fe2235b2ad11a861e9", "text": "Use `struct.pack` for binary data formatting in BMP file generation."}
{"id": "lesson-63e80810440b35147f469463", "text": "Use `std::ifstream` to read binary files and validate their contents for game initialization."}
{"id": "lesson-640cf41a3fa4f7b5fe75d18f", "text": "Give a base Exception subclass a .code attribute and derive specific error types from it, so callers can catch the whole family via the base class."}
{"id": "lesson-64519d20e6cd754ad6afdb90", "text": "Use a 2D list (list of lists) to represent a grid-based game board for easy row/column access and modification."}
{"id": "lesson-6460e0b66559cbc91adbae57", "text": "Use `eval` judiciously; validate inputs and handle exceptions."}
{"id": "lesson-6481b1c9d0f96d0e10adf228", "text": "Use 3-way (Dutch national flag) partitioning in quicksort to split into <, ==, and > pivot regions and recurse only on the outer two; this keeps duplicate-heavy arrays at O(n log n) instead of degrading toward O(n^2) with 2-way partitioning."}
{"id": "lesson-648f572ad84b7d5b02899a8a", "text": "Handle exceptions gracefully with try-except blocks to ensure robustness."}
{"id": "lesson-654a199ad5dd20718225e583", "text": "Closest pair of points in O(n log n): recurse on x-sorted halves, then only rescan a strip of width d re-sorted by y, and break the inner j-loop once dy >= best since the strip is y-sorted."}
{"id": "lesson-6575a10b3c694602d54ff743", "text": "Use `iter()` and `all()` with generator expressions to efficiently check subsequence containment in linear time."}
{"id": "lesson-65a0dd72d99a6062435afbe6", "text": "Inline Method to remove indirection that has only one caller and adds no clarity; a seam nobody uses for testing or extension is just a maintenance cost."}
{"id": "lesson-65cac68797264de61f3bb3f2", "text": "A 'no module specified' fallback that auto-loads every matching plugin DLL in the output directory can silently load mutually-incompatible plugins together; force single-mode by pointing it at a deliberately invalid-but-existing path."}
{"id": "lesson-6665740138a4378cb9796eed", "text": "Use collections.defaultdict(list) to group items by a computed key without checking `if key in dict` first."}
{"id": "lesson-66ab274e7b7935b0a2bfd0fc", "text": "Iterators are efficient for checking subsequences ensuring O(n) time complexity."}
{"id": "lesson-66c251de3ace52de559213a1", "text": "Use a list of tuples with descending values to implement greedy algorithm for conversion problems."}
{"id": "lesson-6709739f79510a262c7429be", "text": "Use exact pack names for assets to avoid confusion and ensure correct file loading."}
{"id": "lesson-67178520f24a5be7ecde6450", "text": "Include `<iostream>` and `<vector>`. Use `std::cout` for output."}
{"id": "lesson-671cc475f6b79ffcb4a01698", "text": "In Rabin-Karp, always re-verify a rolling-hash hit with a direct text[i:i+m]==pattern slice comparison before accepting the match, since equal hashes under a fixed modulus can still collide."}
{"id": "lesson-674e9f8ed6fd480bb9683f07", "text": "Generate diff-only UPDATE statements (changed columns vs a snapshot) for single-row edits, and atomic DELETE+bulk-INSERT for multi-row entities so they never half-apply."}
{"id": "lesson-677734b2f3ff006411b9428f", "text": "Use cache-aside with a TTL, not write-through alone: on write, invalidate the key rather than update it, bounding staleness if the invalidation message itself is lost"}
{"id": "lesson-67a39a61bdf9fa78bc89b200", "text": "Use a dictionary for O(1) lookups when mapping characters to values in string processing tasks."}
{"id": "lesson-67db964e54c0c799370aba65", "text": "Use git rebase --onto <newbase> <oldbase> <branch> to transplant only a range of commits when an upstream dependency branch moved beneath you."}
{"id": "lesson-68295e86132af08a560418b6", "text": "Use a loop to check divisibility up to half of the number for primality testing."}
{"id": "lesson-684f8495cfd40b8df8ad99b4", "text": "Never reuse a nonce or IV with the same key under AES-GCM or CTR mode; reuse lets an attacker XOR two ciphertexts to recover plaintext or forge valid auth tags."}
{"id": "lesson-685ba9a79bdf128dbd3aee06", "text": "Ensure no trailing spaces in output by using `-join ':'` instead of `:` directly."}
{"id": "lesson-68935a2cd2cbc72901504bf0", "text": "Attach a concrete suggested fix to every review comment instead of just flagging the problem, so the author can act without a follow-up round-trip."}
{"id": "lesson-68b147853b4d668a43a3a498", "text": "Use `iter()` and `all()` with generator expressions to efficiently check subsequence membership in linear time."}
{"id": "lesson-68bfc3ec9d5bce5f40c9e5ae", "text": "Attach line/column spans to every token during lexing, not just AST nodes, so error messages survive later tree transforms."}
{"id": "lesson-68ffc2ed129123e6738a33f4", "text": "Use `pygame.event.get()` to handle events like quitting the game."}
{"id": "lesson-69bff037e853538eb07b2c07", "text": "Composition over inheritance: prefer a field holding a swappable object over subclassing, so behavior changes without touching a class hierarchy."}
{"id": "lesson-6a17f333b13de1ce0e186464", "text": "Use dictionary unpacking with `{**a, **b}` to merge dictionaries where the second dict's values override the first's."}
{"id": "lesson-6a56f91fa7b6de5d8bcaff49", "text": "To count subarrays summing to k, keep a running prefix sum and a Counter of prefix values seen so far (seeded with {0:1}); add counts[running-k] before updating counts[running]."}
{"id": "lesson-6a64bf68aaf40acc660b6737", "text": "Use Condition.wait_for(predicate) instead of a bare wait()+notify() to implement a bounded buffer safely against spurious wakeups and lost wakeups."}
{"id": "lesson-6aa332ae2810fb93f8547758", "text": "Relax every edge n-1 times in Bellman-Ford (not just neighbors of visited nodes) so negative weights that Dijkstra can't handle are still resolved."}
{"id": "lesson-6ae8714774a6c36ab325e44d", "text": "Build Roman numerals greedily from a high-to-low value-symbol table with subtractive pairs like (900,'CM'), consuming num via divmod per tier."}
{"id": "lesson-6ae94851189a4bbf59ae0fea", "text": "Check for divisibility up to the square root of the number."}
{"id": "lesson-6aefb18841fadfa71f860bf3", "text": "Open a fresh per-call SQLite connection with WAL journal mode and a busy_timeout rather than sharing one long-lived connection, to avoid lock contention across concurrent MCP tool calls."}
{"id": "lesson-6afc3ca88fd479b6ccf1f6d3", "text": "Use dynamic programming with a 2D array to store intermediate results for grid path counting problems."}
{"id": "lesson-6b6e19b6759642ac04ccecd8", "text": "Use `std::size` for array size to avoid magic numbers and improve readability."}
{"id": "lesson-6bd9fe7df092fddc60fd0b22", "text": "Use `sizeof(array)/sizeof(array[0])` to safely compute the number of elements in a C-style array at compile time."}
{"id": "lesson-6bea5b6f5278c54de06eeaa1", "text": "Find the kth largest by keeping a size-k min-heap: push each element, pop when len(heap)>k; heap[0] is the kth largest in O(n log k) without sorting."}
{"id": "lesson-6c0db699d8c63e1dedbe3133", "text": "Use `heapq.nsmallest` with custom key for efficient top-N selection."}
{"id": "lesson-6c4909896571a8b23524d187", "text": "Use `std::sort` with custom comparator for stable sorting by ParticipantId."}
{"id": "lesson-6c82807b86c2c94fdb6e7a1e", "text": "Compile with ThreadSanitizer (-fsanitize=thread) to catch a data race at the exact unsynchronized access, before it silently corrupts state elsewhere."}
{"id": "lesson-6ca034c7571583bdb15361a3", "text": "Use bitwise operations to efficiently find next power of two."}
{"id": "lesson-6ca4fa75fc4eeca1bc110b75", "text": "Iterate merging arrays efficiently, ensuring no extra space used; use two pointers for O(1) space complexity."}
{"id": "lesson-6cf18bb6d64f08c6028e6a95", "text": "Group anagrams with defaultdict(list) keyed on ''.join(sorted(word)) as the canonical signature."}
{"id": "lesson-6cf72e8c2ccb877390f12f81", "text": "**Lesson**: Always ensure full state cleanup and rollback in critical operations to prevent partial state inconsistencies."}
{"id": "lesson-6d762f5253f1abd1ce4cf89a", "text": "Normalize text with re.findall(r\"[a-z0-9']+\", text.lower()) before Counter to strip punctuation and case noise."}
{"id": "lesson-6ddbff35f8022839424fd0b5", "text": "Use trial division with optimized factor checking (check 2 separately, then odd numbers up to sqrt(n)) for prime factorization."}
{"id": "lesson-6e332d5fb73ba02d0c104329", "text": "Use itertools.groupby(s) to collapse consecutive repeated characters, then len(list(g)) for each run's count."}
{"id": "lesson-6e3ec92e2a5a78fdd128fe70", "text": "Use list comprehension for concise flattening of lists by one level."}
{"id": "lesson-6e5bb9ca3f183b87335522e2", "text": "Ensure server decodes and commits role selection, then replicates PRI properties to owner channel."}
{"id": "lesson-6e5ccfd12b5d893729d877ff", "text": "When deserializing a preorder-with-sentinel string, wrap tokens in a single shared iter() and call next(tokens) inside the recursive helper, not an index counter, so nested recursive calls consume the stream in the right order without manual bookkeeping."}
{"id": "lesson-6e609ecbbe6af8cabf417e00", "text": "Find a seam, a point where behavior can change without editing that line, such as a virtual call or a swappable collaborator, to insert a test double around legacy code."}
{"id": "lesson-6ee4e24b02335db26cb53d19", "text": "Use string concatenation with modulo and integer division to implement manual binary conversion when built-in functions are not allowed."}
{"id": "lesson-6ee7619567a58f935c436156", "text": "Track engagement with a boolean flag."}
{"id": "lesson-6f20b4bb416df0fa6079ffb7", "text": "Push all items onto a plain list with heapq.heappush then heapq.heappop repeatedly for a min-ordering; use heapq.nsmallest(n, iterable) when you only need the top n without draining the heap."}
{"id": "lesson-6f5f53749f0865b00f298e6d", "text": "Probe a laptop's discrete GPU with a timeout over ~5s and one retry: switchable graphics idle powered down, so the first nvidia-smi wakes the card and a tight timeout fails only when nothing else has warmed it."}
{"id": "lesson-6fe8beec86af489898d7a4d3", "text": "Use a generator expression with `sum()` for efficient counting without storing intermediate results."}
{"id": "lesson-701fd6234339b6e943a95db8", "text": "When a parallel agent is interrupted mid-edit, check for a symbol that is called but never defined before trusting the tree; that shape hides in a clean-looking diff."}
{"id": "lesson-705f9c7db1942605bdbfdcb6", "text": "Use `ON CONFLICT` for upserts in SQLite."}
{"id": "lesson-70a35105fe7223f5857c7666", "text": "Pin task launch to ensure it begins only after critical initialization phases complete."}
{"id": "lesson-70c218121a15c6a09026d9d9", "text": "Use a stack (list) to track opening brackets and match them with closing brackets in reverse order for balanced bracket validation."}
{"id": "lesson-70cf893b586ed2fef44928d9", "text": "Use bisect.bisect_left(sorted_list, x) to find the sorted insertion index in O(log n), avoiding manual loops."}
{"id": "lesson-70e75ea636c695e070c193f6", "text": "When recursively flattening JSON, treat empty dict/list as a leaf too or its key silently vanishes from the result."}
{"id": "lesson-711ce35ab4fe36ef2437cc69", "text": "Document functions with Doxygen tags for clarity and safety."}
{"id": "lesson-712ef391ad61c49585afa866", "text": "Implement a no-eval arithmetic parser as three mutually recursive functions (expr/term/factor) so */ binds tighter than +- without a precedence table."}
{"id": "lesson-714fd54fb0945c1ec953e2f2", "text": "Rely on sorted()/list.sort() being stable (Timsort) so ties keep input order; sort once by the tiebreaker key to layer a secondary ordering without a tuple key."}
{"id": "lesson-71552e13ab317a306d181ab9", "text": "Rewrite left-recursive grammar rules like expr := expr '+' term into a loop that folds left operands; naive recursive descent infinite-loops on them."}
{"id": "lesson-71aac3664b6f65cfa5f5fccf", "text": "For longest palindromic substring, expand around every one of the 2n-1 centers (n single chars plus n-1 adjacent pairs) so odd- and even-length palindromes are both covered in O(n^2) with O(1) extra space."}
{"id": "lesson-71fa7430cdd3c1c13ec969fb", "text": "Remember timedelta normalizes to 0<=seconds<86400 and 0<=microseconds<1e6 with days absorbing sign, so a negative timedelta like -1 hour shows as days=-1, seconds=82800, not seconds=-3600."}
{"id": "lesson-725aedf6615031f9dac96ea6", "text": "Use Write-Output for simple sequential output of discrete values in PowerShell scripts."}
{"id": "lesson-72eb085c75cde2f5c2771a81", "text": "Version APIs via the URL path or an Accept header, never a bare query parameter — proxies and CDNs cache and strip query params, silently breaking version routing."}
{"id": "lesson-730e6fe973e8cc5a25909583", "text": "For cooldown-constrained scheduling (e.g. task scheduler), pop the most-frequent task off a max-heap (negated counts) each tick and stash it in a deque keyed by its next-available tick, re-pushing to the heap only when that tick arrives."}
{"id": "lesson-733f609f3a104b43fdf5bcef", "text": "Use bit manipulation and a lookup table to manually encode data in base64 without built-in libraries."}
{"id": "lesson-73894ac9d05660bfada123b0", "text": "Use functools.cmp_to_key(compare) with a compare(a,b) that returns sign(int(a+b)-int(b+a)) to sort strings by concatenation order (e.g. largest-number problem) when no single-value key() can express it."}
{"id": "lesson-73bc45eff0f3e5bad87807c9", "text": "To grow a memory-mapped file, first f.truncate(new_size) on the underlying file object, then remap with a fresh mmap.mmap call; also call mm.flush() explicitly since ACCESS_WRITE mmap edits aren't durable until flushed."}
{"id": "lesson-73c0452bd7e9127092a6395a", "text": "Isolate allocation cost from algorithm cost: swap in an arena or pool allocator and re-measure; a tight loop of malloc/free can dominate over the logic it wraps."}
{"id": "lesson-73c6ec055b547b2b3d0eee57", "text": "Use `max()` and `min()` functions to clamp values within a range in a single expression."}
{"id": "lesson-73dbf6dc1d8bb7d750de1466", "text": "When multiple threads share one DB connection, put the mutex inside the connection object itself and hold it across a whole transaction span, not per-call, or BEGIN...COMMIT races corrupt rows."}
{"id": "lesson-744779e80497243702ed5f32", "text": "Use the square root optimization (i * i <= number) in primality testing to reduce time complexity from O(n) to O(√n)."}
{"id": "lesson-745aec0de65efc71b63297f0", "text": "Use `split()` to get words and `max()` with `key=len` for longest word."}
{"id": "lesson-7489a406d7d99fe8e5393fcb", "text": "Use two-pointer technique for string reversal."}
{"id": "lesson-7490c04f67f90cfe0126cea7", "text": "Make embedding calls soft-fail to None on network/timeout errors and have every downstream consumer degrade gracefully (lexical-only search, skip recall) rather than raising."}
{"id": "lesson-74d83bd652ea454eb2807f39", "text": "Use `ON CONFLICT` with `DO UPDATE` for conditional inserts and updates in SQL."}
{"id": "lesson-751a1dc04b57238c6ee663dc", "text": "Prefer re.finditer over re.findall when you need match.start()/end() offsets; findall silently discards position info and collapses multi-group matches to tuples."}
{"id": "lesson-75ab51f0366cecee5e5ba4e6", "text": "Fuzz a function against a trusted oracle (e.g. sorted()) over many random.Random(seed)-generated inputs, asserting invariants instead of one fixed example."}
{"id": "lesson-75abd468bc7dbe08e1605b17", "text": "Check inclusive conditions for range checks."}
{"id": "lesson-75b9fa3977bd19a8ff41b4b3", "text": "Test immediate spawn after close ACK to identify channel reuse race conditions."}
{"id": "lesson-75ba7ff1b1c43eb17aa5aae4", "text": "Add assertions in critical points to ensure state synchronization between components."}
{"id": "lesson-75c12708aaa2a74e6c0ace04", "text": "Migrate with a Strangler Fig facade: route one call path at a time from the old system to the new implementation, and delete the old path only after traffic fully shifts."}
{"id": "lesson-75cd942a5f39c9a0c5be3d41", "text": "Use `Array.prototype.reduce` for summing array elements."}
{"id": "lesson-76c704e89c1a47d82bd41816", "text": "Use a set to track seen items for O(1) duplicate detection while building output list."}
{"id": "lesson-76f2018216586bdba21587ed", "text": "Use pool.imap(func, iterable) instead of pool.map for streaming: it returns a lazy iterator immediately and preserves input order, unlike map's full-list block."}
{"id": "lesson-773b4650a572a9ddd427d6e3", "text": "Use a stack (list) to track opening brackets and match them with closing brackets in LIFO order."}
{"id": "lesson-77ee3349402951789f3656f9", "text": "Write recursive-descent parsers as one function per grammar rule, using single-token lookahead (peek) to choose which production to take."}
{"id": "lesson-77f3318c1f300eeaf57f9166", "text": "Encode visited-city subsets as integer bitmasks indexing dp[mask][last_city] arrays in TSP DP, not tuples or frozensets, so state lookups stay O(1) array access instead of hashing overhead."}
{"id": "lesson-7853038811cbf50b1c0b0b1c", "text": "Manacher's algorithm needs the string transformed with interleaved sentinels ('^#a#b#$') so every palindrome becomes odd-length, letting one radius array p[i] and a mirror-reuse of p[2*center-i] give true O(n) time."}
{"id": "lesson-78fae3328ad18bf9794b3647", "text": "Use `pygame.event.get()` to handle events and `pygame.key.get_pressed()` for input detection in games."}
{"id": "lesson-791a00c6b6b82f3f0846cf15", "text": "Use collections.Counter to efficiently count item occurrences in a list."}
{"id": "lesson-794fb47c7e5f9eb06de55166", "text": "When unifying divergent codebase variants behind a shared engine, pick one variant as the byte-identical reference and gate every shared-code commit with an automated golden/identity test, not a policy note."}
{"id": "lesson-79d74f5e2483fa281b81d481", "text": "Never let a @contextmanager generator catch an exception and yield again in the same except -- gen.throw() forbids it, raising RuntimeError('didn't stop after throw()'); retry outside the with."}
{"id": "lesson-79e86ee44b439985845b2f5e", "text": "Initialize mutable default arguments with `None` and check inside function."}
{"id": "lesson-7a1e2f0f25f8d99b0602f258", "text": "Precompute factorials and inverse factorials once (O(n)) with a single pow(fact[n], mod-2, mod) call, then answer each nCr mod p query in O(1) instead of recomputing an inverse per query."}
{"id": "lesson-7a3e5ac1bc94aeaf501ee8af", "text": "Ensure validation before writing to prevent invalid state transitions and maintain consistent object behavior."}
{"id": "lesson-7a8e314296e9b9402987ce1c", "text": "Back a @property with a private _attr and validate in the setter; assigning through the property (even in __init__) enforces the invariant everywhere."}
{"id": "lesson-7ab1c101f5fd4bf9705660be", "text": "Test primality by trial division only up to int(n**0.5), skipping even divisors after checking 2 to halve iterations."}
{"id": "lesson-7ad970de1968b39455f58b12", "text": "Use Measure-Object with -Sum parameter to calculate the sum of array elements in PowerShell."}
{"id": "lesson-7b0757751d1b333b95eb4bc7", "text": "Compare package GUIDs before reconciliation to prevent protocol errors."}
{"id": "lesson-7ba0c6c5b99ad77bc7c8f18b", "text": "Use iterative computation with tuple unpacking for O(n) time and O(1) space Fibonacci calculation."}
{"id": "lesson-7ba87b41a09bd018886973ec", "text": "Use `std::unordered_set` for fast lookup and membership checks of loaded assets."}
{"id": "lesson-7bc50a4a84ec1b44ed907ea0", "text": "Count set bits with Brian Kernighan's trick n &= n - 1 in a loop; each iteration clears exactly the lowest set bit, giving O(popcount) time."}
{"id": "lesson-7bd047eebc23fac78b769c81", "text": "Use Write-Output for explicit console output in PowerShell scripts."}
{"id": "lesson-7c02432386c73dc1988fe68f", "text": "Break reference cycles in refcounted graphs by making back-pointers (child-to-parent) weak/non-owning while forward pointers stay strong, or cycles never hit zero."}
{"id": "lesson-7c0ef43974959e8f96ff1da5", "text": "Use `int(s, base)` to convert strings in any base to integers directly."}
{"id": "lesson-7c4c343300de7a199babd037", "text": "Use `LinkedList<T>` for O(1) insertions and deletions from both ends to efficiently manage cache eviction order."}
{"id": "lesson-7ca8b0e3b77861429b1b3c6e", "text": "Define recursive functions with clear base case for concise solutions."}
{"id": "lesson-7cac38aced25b6b5ac7cd7a3", "text": "Use LINQ's `Sum()` method for quick array summation."}
{"id": "lesson-7cf2f40332820e395b77d77d", "text": "Install a pre-commit hook running lint and fast tests so bad commits never leave the machine, but keep it fast or developers bypass it with --no-verify."}
{"id": "lesson-7d1faca06517ac5213db7206", "text": "Use collections.defaultdict(int) so counts[key] += 1 works without a KeyError on first sight of a key."}
{"id": "lesson-7d47e5893132f778b2053d8a", "text": "Two network services that both default to the same port can silently co-bind via SO_REUSEADDR; kill stale listeners before starting a new one or clients connect to the wrong process."}
{"id": "lesson-7d52aa63c9ecf0ed496eb208", "text": "Use the modulo operator (%) to implement divisibility checks for calendar calculations."}
{"id": "lesson-7d581b4335c9546e7cc27b5a", "text": "Use fractions.Fraction for exact rational splits (e.g. dividing a bill n ways); the fractions re-sum to the exact original total, unlike float division which only approximates it."}
{"id": "lesson-7d78212e436217545522813a", "text": "Wrap a nested-quantifier subpattern like (a+)+ in an atomic group (?>a+)+ (Python 3.11+ re) to stop the engine from re-backtracking into it, killing exponential catastrophic-backtracking blowups."}
{"id": "lesson-7d9826ff9165f8415cf44c7c", "text": "Pass both rel_tol and abs_tol to math.isclose: rel_tol alone fails near zero, abs_tol alone fails at large magnitudes, so combine them across scales."}
{"id": "lesson-7dc7298d8b6ca61a89286dc7", "text": "Handle command-line arguments safely with `sys.argv`."}
{"id": "lesson-7e01b5685ef40ba580c29b08", "text": "Apply dependency inversion: have high-level modules depend on abstractions they own, not ones defined by the low-level implementation, so it stays swappable in tests."}
{"id": "lesson-7e2e799e28669e8e536b4eee", "text": "Use fail-closed logic for critical configuration validation."}
{"id": "lesson-7e3970d3671ffa595c6c9c38", "text": "Use a set to track seen non-zero digits for quick duplicate checking."}
{"id": "lesson-7e51afe3e821d3bff90f295b", "text": "Bound buffer overruns with guard/redzone pages surrounding heap allocations so an out-of-bounds write segfaults at the fault site instead of silently corrupting adjacent allocator metadata."}
{"id": "lesson-7efac95f8b64e58492f59367", "text": "**Lesson:** Atomic state transitions and lock protection are crucial for managing concurrent access in distributed systems to prevent race conditions and ensure data integrity."}
{"id": "lesson-7f00c5f43154574ffb656fc0", "text": "Use heapq.nlargest() for efficient top-k selection with O(n log k) time complexity."}
{"id": "lesson-7f67281ce33410e39f8b7e41", "text": "Fix missing closing ')' in expression."}
{"id": "lesson-7fe7df49b774c79b80a0089f", "text": "Build chunks with [seq[i:i+size] for i in range(0, len(seq), size)]; slicing past the end just truncates safely."}
{"id": "lesson-80a301c1438167c90b68a2b1", "text": "Implement union-find find() with path halving (parent[x]=parent[parent[x]]) so Kruskal's cycle check stays near O(1) after sorting edges by weight."}
{"id": "lesson-80ed0f31cf919cb2f86d1bbc", "text": "Track first/second max in one pass with two variables instead of sorting the whole list."}
{"id": "lesson-810f995ec8c787bc331ac88f", "text": "Compare two strings as multisets with collections.Counter(a) == collections.Counter(b) to test anagrams ignoring case/punctuation."}
{"id": "lesson-8157f089c80534f8e23e341b", "text": "Convert integers using `[int]` for arithmetic operations."}
{"id": "lesson-818f8b26560dcfc1d6ded714", "text": "Use string slicing with `[::-1]` to reverse strings in O(n) time."}
{"id": "lesson-81b68703b2f8e03c637c7ebe", "text": "Implement Prim's MST with heapq by pushing duplicate (weight,to) entries instead of decrease-key, and skip an edge if its target is already visited (lazy deletion)."}
{"id": "lesson-821239566d8ccf8dd28f6ebf", "text": "Use float division for averages to avoid integer truncation."}
{"id": "lesson-8213121561602ab75624f518", "text": "Construct aware datetimes with zoneinfo.ZoneInfo(tzinfo=...) and convert between zones with astimezone(), never by hand-shifting hours."}
{"id": "lesson-8216f3e0576b5a83a788ac91", "text": "Don't trust the GIL for compound ops: `x = x + 1` is load/add/store, not one bytecode, so it can still race; insert time.sleep(0) between the read and write to force/prove the interleaving, then fix it with an explicit Lock."}
{"id": "lesson-8235e2e9f4fcb0598bd8bd5b", "text": "Support @deco and @deco(kw=v) on one function by giving the outer function a _func=None first positional param and dispatching on whether it is callable."}
{"id": "lesson-824f85096b3d5632ce034ed1", "text": "READ COMMITTED permits non-repeatable reads across a transaction; use SELECT ... FOR UPDATE or SERIALIZABLE isolation to stop lost updates in read-modify-write cycles."}
{"id": "lesson-8279ee8bfbebe43a06af0ceb", "text": "Use procedural drawing and simple AI for paddle movement in Pygame games."}
{"id": "lesson-828be0709f8456ff9839d5be", "text": "During a DST fall-back transition, disambiguate a repeated local hour with fold=0/1 (first/second occurrence) -- same naive fields, but utcoffset() and timestamp() differ by an hour."}
{"id": "lesson-82a3ff4d91b40971e17fc174", "text": "Implement recursive tree traversals with an accumulator list passed by reference (out=None then out=[] on first call) instead of returning and concatenating lists at each level."}
{"id": "lesson-82b4bd73aa83059a8b09b31e", "text": "Use a fixed-width negative lookbehind like (?<!\\\\) before a delimiter to split on it only when not backslash-escaped, then unescape the survivors."}
{"id": "lesson-82eb89401eb886c36cbe24dc", "text": "Pair dependency inversion with a single composition root: wire all concrete implementations in one bootstrap file so business logic never calls 'new ConcreteClass' directly."}
{"id": "lesson-836542f5cb8c289692289a24", "text": "Prime a generator-based coroutine with next() before calling send(), since the first yield expression's value is only reachable once execution has advanced to that point."}
{"id": "lesson-8365aebc3c9f00746090865c", "text": "Solve two_sum in O(n) with a value-to-index hashmap: check target-n in seen before inserting n."}
{"id": "lesson-83a619ded2814816bedddb38", "text": "Frame-count-based test scheduling is unreliable under variable frame rate (vsync, occlusion); give smoke-test harnesses a wall-clock scheduling mode instead."}
{"id": "lesson-83c5e64053b58120e7c186bc", "text": "Read JSON as plain text with <fstream> and validate binary signatures manually."}
{"id": "lesson-83e79d35d84885512bfefa26", "text": "Validate IPv4 by splitting string, check each part, ensuring valid range and format."}
{"id": "lesson-84d9d8528029dfb8b8451881", "text": "Defend pickle against __reduce__ RCE with two layers: HMAC-sign and verify bytes before pickle.loads, and restrict find_class to a whitelist so signed payloads can't resolve dangerous callables."}
{"id": "lesson-84dc6dea1d953a55e51d7783", "text": "Use os.system('cls' if os.name == 'nt' else 'clear') to clear the terminal screen portably across operating systems."}
{"id": "lesson-84e9164384afaa04ec6cad5f", "text": "After `if x is None: return`, later code sees `x` narrowed from Optional[T] to T without an isinstance check or cast."}
{"id": "lesson-859bd6edc4ccd133a1079371", "text": "After a user's write, pin their next reads to the primary (read-your-writes) instead of the replica pool, or async replication lag will show them stale data"}
{"id": "lesson-85f07e583fa18ec1ca58c489", "text": "Use `for value in values` instead of `for index in range(len(values) - 1)` to iterate over all elements in the list."}
{"id": "lesson-863767dbe14c436edfcb0016", "text": "Use the pipeline operator `|` with `ForEach-Object` to process each item in an array individually."}
{"id": "lesson-8668774eba85b7161918f5d2", "text": "For variable-size sliding window on 'sum >= target', expand hi in a for-loop but shrink lo in an inner while as long as the window still qualifies."}
{"id": "lesson-867d8a05dd74b7232dade7f1", "text": "Use '=' (standard, unaligned) not '@' (native) struct format prefixes for on-disk/wire binary formats; native mode silently inserts platform-dependent padding that breaks portability."}
{"id": "lesson-87155b17e8dce05785cd29b1", "text": "Use `${}` for variable references in strings when they contain special characters like `:`."}
{"id": "lesson-8762a0a945d055f77d36fdae", "text": "For a fixed-size sliding window, seed window_sum=sum(nums[:k]) then each step add nums[i] and subtract nums[i-k] instead of re-summing the slice."}
{"id": "lesson-877f68f38b55d4597d0270ff", "text": "Use collections.Counter to efficiently count element frequencies and find the most common element in O(n) time."}
{"id": "lesson-87964d87f60a22758e518175", "text": "Use `Console.WriteLine` for printing to the console in C#."}
{"id": "lesson-87f7e078b5e38fdcbfd042f9", "text": "Use modulo operator `%` to check divisibility for even/odd numbers efficiently."}
{"id": "lesson-8823928fa2d811901d99f386", "text": "Tie every resource's lifetime to a stack-scoped object's destructor (RAII) so exceptions, early returns, and goto can't skip the matching free or close."}
{"id": "lesson-8877dc010a5a32d7b03d2ee3", "text": "Decouple publisher from subscribers with an observer/event-bus callback list, but explicitly unregister on teardown or dangling references leak memory."}
{"id": "lesson-88c98377469f05a5162d780d", "text": "Use `try/catch` blocks in network thread's per-frame parse/dispatch to handle exceptions safely."}
{"id": "lesson-88fcaa4ea0ffbad2e70ab7c7", "text": "Identify common characters and use generator expressions for concise counting."}
{"id": "lesson-8936f7db7c1bc3933466ed2d", "text": "Use a string containing all vowels and check membership with `in` for O(1) case-insensitive vowel detection."}
{"id": "lesson-894d66911cbb1e354093c199", "text": "Wrap a single try/except/finally around one yield in a @contextlib.contextmanager generator to get commit-on-success, rollback-on-exception, and unconditional cleanup in one reusable context manager."}
{"id": "lesson-8967c8d8585e2ab504b4e4cc", "text": "Distinguish hashing (one-way, for integrity/passwords), encryption (two-way, needs a key), and encoding like Base64 (no secrecy at all) before choosing a primitive."}
{"id": "lesson-898cdb9c07a4ef596320f0d2", "text": "Use pygame.Rect for efficient collision detection with collidepoint() and colliderect() methods."}
{"id": "lesson-899b0471eea8443c6e281946", "text": "Tag disciplined print-debugging output with a unique marker string and the enclosing function name so grep can isolate one execution path from interleaved log noise."}
{"id": "lesson-89e1218ae2ca2c410b35c3dd", "text": "Use `pygame.key.get_pressed()` for efficient key detection in game loops."}
{"id": "lesson-8a1dccf7fc09a3a443365b81", "text": "Ensure conditions are inclusive for edge cases."}
{"id": "lesson-8a5ef5ecd95cd5c154a3d294", "text": "Use switch statements for mapping enum values to strings in C."}
{"id": "lesson-8a8e167fbd399e9a7978d2db", "text": "Convert camelCase/PascalCase to snake_case with two regex passes: r'([a-z0-9])([A-Z])' then r'([A-Z]+)([A-Z][a-z])', inserting underscores before lowering."}
{"id": "lesson-8ac904354a0bb23a3c7749e5", "text": "Model non-CRUD operations as RPC-style action endpoints (POST /orders/{id}/cancel) instead of forcing them into PATCH — ambiguous partial-update semantics invite silent state corruption."}
{"id": "lesson-8ad0cb292ba3f1a0a2736c3a", "text": "Use `sizeof` correctly to calculate array length in C++."}
{"id": "lesson-8adc2728cbc44cf3a2936f86", "text": "Use multiprocessing.shared_memory.SharedMemory with Pool(initializer=...) so each worker attaches to the same named OS buffer by name -- zero-copy, no pickling of array contents each task."}
{"id": "lesson-8b21442b2ca83b1a5f2e8079", "text": "When a subsystem can't be runtime-verified in the current environment, isolate its unverified assumptions behind named constants so one later real-data run fixes them without touching working code."}
{"id": "lesson-8b6476a35574cc538897a0ec", "text": "An unsigned char shifted in C promotes to int first, so the result assigned back to an unsigned type trips -Wsign-conversion; cast the operand before shifting."}
{"id": "lesson-8becdfeaaa7f0bec3278108a", "text": "Use `split('')`, `.reverse()`, and `join('')` to reverse strings in JavaScript."}
{"id": "lesson-8bf19488087943df97139fac", "text": "Use __init_subclass__(cls, *, name=None) with a class-level dict to auto-register subclasses without a decorator."}
{"id": "lesson-8cc461b1f7f93a299f8b3166", "text": "Never invent a cipher, mode, or padding scheme; use audited libraries (libsodium, OpenSSL) and standard constructions like AES-GCM or ChaCha20-Poly1305 instead of rolling your own crypto."}
{"id": "lesson-8ce47cda51699268dfd2ed11", "text": "Implement modular exponentiation as a binary-exponent loop (square base, halve exp via bit shift) to make O(log exp) cost explicit instead of relying only on pow(base,exp,mod)."}
{"id": "lesson-8d1e278a58c452a5a2a06658", "text": "Flatten nested dict/list JSON by recursing with a growing prefix key ('a.b' for dicts, 'a[0]' for lists) and special-casing empty dict/list as leaf values."}
{"id": "lesson-8d3527b09184a148b11fabaf", "text": "Use `pygame.draw.rect` for drawing game elements procedurally."}
{"id": "lesson-8d597b130e4f3bb504a50842", "text": "Capture retail actor-open payloads and channel lifecycle for dynamic identity management."}
{"id": "lesson-8da7566be43897c68cce3b25", "text": "Grant standing admin roles rarely; instead use time-boxed just-in-time privilege elevation that auto-revokes after minutes, so a compromised account has a narrow blast-radius window."}
{"id": "lesson-8db22587541dac4915a02165", "text": "Use a dictionary for O(1) lookups and process the string in reverse to handle subtraction cases in Roman numerals."}
{"id": "lesson-8e185b22068ff0d487ef02b3", "text": "Use `json.dumps()` with `sort_keys=True` to ensure consistent JSON serialization order."}
{"id": "lesson-8edb537c92249e26ab089495", "text": "Use `try-except` to handle end-of-input gracefully."}
{"id": "lesson-8f447d1e99755717440c26f1", "text": "Never SELECT * in production queries: fetch only needed columns so a covering index can satisfy the query without a heap/table lookup."}
{"id": "lesson-8f52b64e730ca66e2dacb4ba", "text": "Fall back to valgrind --tool=memcheck when ASan can't be built into the target (embedded, closed toolchain), accepting a 10-30x slowdown for exact invalid-access traces."}
{"id": "lesson-8f808297903fc80a3e14d2ce", "text": "Enforce idempotency-key uniqueness with a database unique constraint on (client_id, key), not just a cache lookup — otherwise a check-then-insert race lets duplicate requests both succeed."}
{"id": "lesson-8fb2d8f8a290e73144830f8a", "text": "Use dictionary unpacking to safely override values in dicts without mutation."}
{"id": "lesson-8fdaab32270b3c93f0b6fdc4", "text": "Use inspect.signature(func).bind(*a,**kw) plus apply_defaults() to validate arguments against parameter annotations before dispatching a dynamic call."}
{"id": "lesson-8fef906a5cbbfa19b2638133", "text": "Before packing a Python int into ctypes.c_int32 for a binary protocol, bounds-check against INT32_MIN/INT32_MAX; ctypes silently wraps overflow via two's-complement instead of raising."}
{"id": "lesson-905ade1a0273e82f3b167f12", "text": "Use `for value in values` to iterate over all elements in the list."}
{"id": "lesson-90eb822366cccd342a17309f", "text": "Use heapq.nlargest() for efficient retrieval of top n elements without full sorting."}
{"id": "lesson-9120f48f8a922c1eeaf4922d", "text": "Use string multiplication with boolean expressions to conditionally build strings without explicit if-else branches."}
{"id": "lesson-914cdbab114967d952614edb", "text": "Use `std::string::find_first_of` to check for multiple characters in a string efficiently."}
{"id": "lesson-91b5bfef1c7df662e02bec78", "text": "Use a hash map to store previously seen values and their indices for O(1) lookup when searching for complements."}
{"id": "lesson-9216c84ce537174881f6dd75", "text": "Check frame order before subtracting to avoid underflow."}
{"id": "lesson-921ad2cce126f12af0599f6e", "text": "Use CSS animations for creating spinning effects."}
{"id": "lesson-925e420a067550244ef45ccb", "text": "Simulate decrease-key in Dijkstra by pushing a new (dist, node) tuple whenever you find a shorter path instead of mutating the heap, and skip a popped node if it's already in the visited set — the stale, larger-distance duplicate is discarded for free."}
{"id": "lesson-9287414d4c62ad2d34eff1c5", "text": "Use collections.OrderedDict with move_to_end(key) on access and popitem(last=False) on overflow to build an O(1) LRU cache."}
{"id": "lesson-928c8aece61e91977f3773b8", "text": "Never store passwords with a fast general-purpose hash like SHA-256 or MD5 alone; use a deliberately slow KDF such as argon2id or bcrypt so brute-forcing costs attackers real time."}
{"id": "lesson-929727e59280f7aa30189b20", "text": "Compute Euler's totient for every n up to N in O(N) with a linear sieve that tracks each composite's smallest prime factor, applying phi[i*p]=phi[i]*p when p divides i and phi[i]*(p-1) otherwise."}
{"id": "lesson-92e101ec850102e52957f888", "text": "When distilling free-text lessons from an LLM, filter output with a vague-language blocklist plus a concrete-anchor allowlist (backticked code, dotted calls, CamelCase APIs, Big-O) so platitudes never pollute retrieval."}
{"id": "lesson-92e12d428d483b166bcd4702", "text": "Use concise formula for Celsius to Fahrenheit conversion in Python."}
{"id": "lesson-92e3e3ef49a051953c8f1890", "text": "Choose UDP over TCP when occasional packet loss is cheaper than head-of-line-blocked retransmit delay, e.g. real-time voice or game state"}
{"id": "lesson-932554fef2927357c964b873", "text": "For meet-in-the-middle subset sum, split into two halves, generate all 2^(n/2) subset sums per half via sums+[s+x for s in sums], then check target-a in the other half's set."}
{"id": "lesson-9370f06c80c21c3d009bd4b7", "text": "Compile with AddressSanitizer (-fsanitize=address) to catch use-after-free and heap-buffer-overflow at the faulting instruction instead of a delayed unrelated crash."}
{"id": "lesson-9373e2750d8682f2062eb0ff", "text": "task.cancel() only raises asyncio.CancelledError inside the task at its next await point; re-raise it after cleanup in a try/except so task.cancelled() reports True instead of swallowing the cancellation."}
{"id": "lesson-937c27386d741f60088844dd", "text": "Sort intervals by start, then merge by comparing each interval's start to the last merged interval's end to collapse overlaps in one pass."}
{"id": "lesson-939d18b2197cbbb0fdb17358", "text": "Use `str.split()` to tokenize strings and `collections.Counter` for efficient frequency counting."}
{"id": "lesson-943807bfe0774f03029d3074", "text": "Use collections.Counter to compare character frequencies for anagram detection."}
{"id": "lesson-943d86856993f100063261d2", "text": "Use dynamic programming with a 2D array to store intermediate results and avoid redundant calculations."}
{"id": "lesson-94462553a2d669aac6883af7", "text": "Use dictionary unpacking (`**`) to merge dictionaries with later values overriding earlier ones."}
{"id": "lesson-95206db1c4d2cb32b0273202", "text": "Avoid full-heap GC pauses by exploiting the generational hypothesis: collect the young generation frequently and cheaply, promote survivors, and scan the old generation rarely using write barriers to track cross-generation pointers."}
{"id": "lesson-9565c40a453147503fa998ad", "text": "Replace near-duplicate test functions with @pytest.mark.parametrize over a list of (input, expected) tuples for one table-driven test."}
{"id": "lesson-95e686c46ad1cd7f782e2816", "text": "Break a two-module import cycle by extracting the interface both sides actually need into a third module, then point both originals at it instead of each other."}
{"id": "lesson-961fc9cf85513b5b4ad22cbf", "text": "Use a range-based for loop to iterate through an array and accumulate values into a sum variable."}
{"id": "lesson-963287fae69cd1548237d97b", "text": "Use static_cast<size_t> for type matching in EXPECT_EQ assertions."}
{"id": "lesson-96a08dc8d7513d87ca0a1b10", "text": "Reduce logging level from DEBUG to TRACE for performance optimization without observable regressions."}
{"id": "lesson-96ca8663e83875c7de459fa8", "text": "except* returns matched leaves still wrapped in the original nested ExceptionGroup shape, not a flat tuple; recursively flatten by walking .exceptions before treating results as plain exceptions."}
{"id": "lesson-9731a0c219368e1393d47a3a", "text": "When a plugin DLL statically links the host engine, exe and DLL get separate copies of every global/singleton/type-id; keep shared state exe-side only."}
{"id": "lesson-9731bbb100d1a64b143e8c52", "text": "Set separate connect, TLS-handshake, and read timeouts per request; a single blanket timeout masks whether DNS, the socket, or a slow server stalled"}
{"id": "lesson-978567b76aa1cdbb7bab3ffb", "text": "Transpose the second matrix once with b_cols = list(zip(*b)) so each output cell is sum(x*y for x,y in zip(row, col))."}
{"id": "lesson-97c4e706fdff86488e96c73b", "text": "Use {**d1, **d2} to merge dicts where later dict's values overwrite earlier ones on key collision."}
{"id": "lesson-97f8f8040d0e67ad97774d3b", "text": "itertools.chain.from_iterable over an unbounded generator plus itertools.islice pulls only N items lazily; islice-ing the same chain object again resumes mid-stream instead of restarting."}
{"id": "lesson-9929ebcc0ee4662ece000de8", "text": "Use `pygame.display.flip()` to update the display after drawing."}
{"id": "lesson-99b110e818cab95d79c4c18b", "text": "Use collections.Counter(iterable).most_common(n) for top-n frequency counts; missing keys return 0, never KeyError."}
{"id": "lesson-99b355b453389b91ceef364a", "text": "datetime equality across differing tzinfo compares by absolute instant automatically, but comparing a naive datetime against an aware one raises TypeError -- normalize first with astimezone()."}
{"id": "lesson-99b9c1768ad44e31f463a28c", "text": "Implement quickselect with Lomuto partition and recurse only into the side containing index k, giving O(n) average time to find the kth smallest without fully sorting."}
{"id": "lesson-99f977d1685c696189e2aafd", "text": "Always sorted(data, key=fn) immediately before itertools.groupby(data, key=fn); groupby only merges equal-key items that are already contiguous, silently splitting/overwriting otherwise."}
{"id": "lesson-9a149eaac9999e8652c7344c", "text": "Check if number is prime by testing divisibility up to its square root and skipping even numbers after 2."}
{"id": "lesson-9a1bbc9ddf4aab92a9c96647", "text": "Denormalize only after measuring join cost with EXPLAIN; when you duplicate a column, add a trigger or dual-write path so the copy can't silently drift."}
{"id": "lesson-9a59a1435826e8bb796f1fb7", "text": "Check divisibility using modulo operator for prime number verification."}
{"id": "lesson-9ac6592da2da0ed4ae29c4b5", "text": "Avoid multi-lock deadlock (transfer(a,b) vs transfer(b,a) racing) by always acquiring locks in a fixed global order, e.g. sorted by object id, never per-call argument order."}
{"id": "lesson-9affc5728a0ce7291e7f568e", "text": "Maintain a running median with a negated max-heap for the lower half and a min-heap for the upper half; after every push, cross-balance one element between them so sizes differ by at most 1 and the median is O(log n) per insert."}
{"id": "lesson-9b19d035cb6fdd170ef87e6b", "text": "cmd /c strips the leading and trailing quote from a quoted argument; double-wrap quoted compiler paths as cmd /c \"\"path\\exe\" args\" to avoid mangled redirection."}
{"id": "lesson-9b25f6b22bcea19ddcf43260", "text": "Compute gcd via Euclidean algorithm: repeat a, b = b, a % b until b is 0, then return abs(a)."}
{"id": "lesson-9c36556c38648258846ec619", "text": "Prefer a generator expression `(x for x in ...)` over a list comprehension for single-pass iteration: sys.getsizeof shows ~200 bytes of fixed state versus O(n) list storage."}
{"id": "lesson-9c75aed57fa0f20b0fadbf7d", "text": "Use conditional and watchpoint breakpoints (break if x==N, watch on a memory address/variable) to stop only at the Nth iteration or the exact write that corrupts state, instead of stepping through thousands of benign iterations."}
{"id": "lesson-9c8690cf8b2170193198e770", "text": "Fix late-binding closures in a loop by defaulting the loop var (lambda i=i: ...) or wrapping in a factory function to capture it by value."}
{"id": "lesson-9cc59d60bb8fa38fa53a5327", "text": "Key a content cache by hash(path+size) so a repacked source file auto-invalidates its cache entry, and negative-cache failed loads to stop per-frame retry storms."}
{"id": "lesson-9ccf1674de78306e4caad839", "text": "Ensure JSON objects are formatted with consistent indentation for readability and proper nesting."}
{"id": "lesson-9d2b57fd069bc4bf0f5a4c07", "text": "A rule true only for one platform, word size, or language version must carry that condition in its text, or it will be applied where it silently corrupts."}
{"id": "lesson-9d7010df00ab1b1c3da6ffde", "text": "Cap retry attempts with a deadline budget, not just a count, so retries inside a timed request never outlive the caller's own timeout"}
{"id": "lesson-9ddc519685850c776ed3c016", "text": "Check divisibility up to sqrt(num) for prime testing."}
{"id": "lesson-9e417e4719380b5ef0c9c8b4", "text": "Use manual swapping for string reversal when `<algorithm>` is not available."}
{"id": "lesson-9e6ba85544f52e7dcc59bf00", "text": "Use Kadane's algorithm with two variables to track current and best sums for maximum subarray problems."}
{"id": "lesson-9e81a410fc7d4d7623fcc8d8", "text": "Implement topological sort with Kahn's algorithm (BFS on zero-in-degree nodes via collections.deque) so cycle detection is free: if len(output_order) != n after the queue drains, the graph has a cycle and no valid ordering exists."}
{"id": "lesson-9ec9e71b59e5a3f807425a08", "text": "Use collections.Counter(items).most_common(1)[0][0] to get the single most frequent element in one pass."}
{"id": "lesson-9f36752f6cc5fd64ed07684b", "text": "Use `std::unordered_map::find` to safely access existing keys without inserting new ones."}
{"id": "lesson-9f4fd5639802d8cae14df816", "text": "Use set operators &, -, |, ^ (intersection/difference/union/symmetric-diff) to compare two collections' overlap in one pass each."}
{"id": "lesson-9fe7fab2ec152eeecf8e1826", "text": "Use Python's built-in `int(string, base)` function to convert number strings from any base to decimal integers."}
{"id": "lesson-a02501b83aaf06e3b651ac0e", "text": "A smoke test asserting a fixed error code for 'device absent' passes forever in emulation and fails the day real hardware appears; derive the expectation from the probed state."}
{"id": "lesson-a05a6900a9e0896600dcb06c", "text": "Use float('-inf') as initial values for tracking first and second largest elements in a single pass."}
{"id": "lesson-a1584cda6b27239ce0f11202", "text": "Iterate over all elements in the list to include every value in calculations."}
{"id": "lesson-a1658eab301631c634fe35ad", "text": "Sort intervals by start time before merging."}
{"id": "lesson-a1e9cb6b255a66f1c18cfaef", "text": "Count inversions in O(n log n) by adding len(left)-i to the inversion total inside merge sort's merge step exactly when an element from the right half is taken."}
{"id": "lesson-a35391e45679469aec2034eb", "text": "Reverse a string in one step with slicing s[::-1] instead of a manual loop or list reversal."}
{"id": "lesson-a38726e7e74e42c31e53157a", "text": "Drop into pdb.set_trace()/breakpoint() at the failure site and inspect live locals with p/pp instead of guessing values by re-reading source."}
{"id": "lesson-a3bd311f7107d278169990da", "text": "Never force-push a shared branch: it rewrites commits other clones already built on, orphaning their work and breaking their reflog recovery."}
{"id": "lesson-a3c0336b72eb6c00711984fd", "text": "Back a descriptor's per-instance state with weakref.WeakKeyDictionary (never a plain dict) when the owner uses __slots__, so instances without a __dict__ still get storage that doesn't leak on deletion."}
{"id": "lesson-a400e2732922c0518bd24fe4", "text": "Use `pygame.Rect` for collision detection and drawing rectangles."}
{"id": "lesson-a4be55830140a1d500bfa18d", "text": "Use modulo operator (%) to normalize rotation size when implementing list rotation to handle cases where k exceeds list length."}
{"id": "lesson-a57546969a36a0ec40e05da3", "text": "Wrap a naive recursive Fibonacci with @functools.lru_cache(maxsize=None) to turn exponential calls into linear memoized ones."}
{"id": "lesson-a592fc7f95d980b3472a3a38", "text": "Pass a function to re.sub so the replacement can inspect the Match object (group values, position, external counters) instead of only static backreference text."}
{"id": "lesson-a5fae4183eceb776e4209d0f", "text": "Use explicit type casting for integer literals to avoid overflow and ensure correct comparisons."}
{"id": "lesson-a69c1f8b37f3726328ed5f2e", "text": "Add clear, concise Markdown for documentation updates."}
{"id": "lesson-a74706114b2cc23ec4ab53e5", "text": "Use PowerShell's array slicing and -join operator to reverse strings without relying on non-existent cmdlets."}
{"id": "lesson-a74a4e17c58f1acd717ac1f5", "text": "Before assuming a shortcut refactor exists across N codebase variants, run an empirical pairwise duplication study first; the real overlap can turn out to be zero identical files."}
{"id": "lesson-a7660196deb4fd1860f4ac0a", "text": "Iterate over all elements in the list to include every value in the sum calculation."}
{"id": "lesson-a7675a8452ba36e98eb57861", "text": "Use the built-in `sum()` function to efficiently calculate the total of numeric sequences."}
{"id": "lesson-a7d7930e8aa536f58b5fc768", "text": "Use `BeginDepletionNotificationBatch` and `EndDepletionNotificationBatch` for nested and reentrant callback handling in `TicketSystem`."}
{"id": "lesson-a7da5c66b45fa27b702eef34", "text": "A non-content FTS5 table has no delete triggers; deleting the source row leaves an orphaned FTS row unless you explicitly DELETE from the mirror too."}
{"id": "lesson-a809949a6ea7f0c97fd4addb", "text": "Sort intervals by start time first, then merge overlapping ones in a single pass."}
{"id": "lesson-a889178b5d1aab2971e4cfb5", "text": "Use bit manipulation efficiently with bitwise operations and loops."}
{"id": "lesson-a8979ffa174f3b62f776db3a", "text": "Build a (n+1)x(m+1) DP grid where dp[i][j]=dp[i-1][j-1]+1 on a char match else max(dp[i-1][j],dp[i][j-1]) for LCS length."}
{"id": "lesson-a8ac48e60456070872ed1d38", "text": "Forward all caller args untouched with def wrapper(func, *args, **kwargs): return func(*args, **kwargs) to wrap any-signature callables."}
{"id": "lesson-a8ac674b1bea3cbad8fb342e", "text": "Use modulo operator (%) to normalize rotation indices and avoid unnecessary full rotations."}
{"id": "lesson-a8b886c88237c3415cc34425", "text": "A detection probe that fails closed to 'absent' inverts every downstream sizing decision silently; distinguish 'not present' from 'did not answer in time'."}
{"id": "lesson-a8cbaaff4dbc903de894cb6a", "text": "Use `max()` with `key=len` to find the longest string in a list of strings."}
{"id": "lesson-a93594d32b8e6b75c4ad799b", "text": "Use modular arithmetic for indices to handle wrap-around in FIFO buffers."}
{"id": "lesson-a9752bb3311f0f4ec3d9c7a3", "text": "Parse Roman numerals left-to-right: subtract the current symbol's value when it's less than the next symbol's value, else add it."}
{"id": "lesson-a99606ecd7aebbf9fabbefe8", "text": "Use bounded retry with a guaranteed fallback for mutation/crossover operators: attempt N candidates, validate each, and always return an unchanged deep copy rather than risk an invalid result."}
{"id": "lesson-aa371adfe2a53e9671f69674", "text": "Attach a client-generated idempotency key to every mutating request and dedupe server-side, so a client retry after a timeout can't double-charge or double-insert"}
{"id": "lesson-aa713e52003f4ffb6d61bf25", "text": "Use `bool` for boolean return values in functions."}
{"id": "lesson-aa729d386b631c4a8891d573", "text": "Use foreach loop with arrays for simple iteration and summation operations."}
{"id": "lesson-aa97afaadba692a6d833066b", "text": "Check `key in config` or compare `is None` before relying on bare truthiness, since 0, '', and [] are falsy but legitimately present values."}
{"id": "lesson-aaa60d9831077027c28d3c54", "text": "Two structs describing one wire format in different languages need a test comparing them field by field: a size assertion on each side still passes when fields are reordered."}
{"id": "lesson-aac3dc9ff710dd032ef52754", "text": "Use vector clocks, one counter per replica, to detect concurrent conflicting writes that wall-clock timestamps would falsely order or silently overwrite."}
{"id": "lesson-ab3a2b12c82c44fa868d0a45", "text": "Use mutexes to prevent concurrent restarts and ensure thread safety."}
{"id": "lesson-aba7230c7d61d23713c6d675", "text": "When a dataclass subclass adds a required field after a parent field with a default, mark the new field field(kw_only=True) (3.10+) instead of reordering, since kw-only fields are exempt from the positional default-ordering check."}
{"id": "lesson-abbb2d22497062ae2d9f7c8b", "text": "Use Stack<char> for balanced bracket checking."}
{"id": "lesson-abcf0d3b90330378b3323d72", "text": "Use a while loop with manual index control to process consecutive character groups in a string."}
{"id": "lesson-abefda1391e4a5617854f230", "text": "XOR-reduce an entire list into one accumulator to find the single non-duplicated element when every other value appears exactly twice."}
{"id": "lesson-ac5d8bf348cc1caed31b869f", "text": "Use `tempfile.NamedTemporaryFile` for safe file operations during processing."}
{"id": "lesson-ac728b1ebee8f054a96e5d73", "text": "Filter non-alphanumeric chars, ignore case, compare remaining valid chars."}
{"id": "lesson-ac85fbc23e7a84f3ad62d9f1", "text": "Use starred unpacking `first, *middle, last = seq` to peel off the head and tail of a sequence in one assignment instead of slicing with [0] and [-1] separately."}
{"id": "lesson-aca57d5c742c5d24b613d5e4", "text": "Use pygame.Rect for collision detection and platform boundaries."}
{"id": "lesson-adcf74cb1dc994820860516e", "text": "Use slicing to check if string reads the same backward, simplify with direct comparison."}
{"id": "lesson-ade30b2f55cd67885ac4035e", "text": "Use `sorted` with a key function to sort based on integer values."}
{"id": "lesson-aecbab034ab631034c6f876d", "text": "Prevent cache stampede on a hot key's expiry with request coalescing (single-flight) or probabilistic early recomputation, so one expiry doesn't trigger thousands of simultaneous origin fetches"}
{"id": "lesson-af496d4258f195bcc2d876d1", "text": "Use float division for accurate averages."}
{"id": "lesson-afafedec706fc22a9075b022", "text": "Mark commits that won't build or can't be tested with `git bisect skip`, and have the run-script return 125 so bisect knows to route around them."}
{"id": "lesson-afe2bf2beb500b3af88ea6f2", "text": "Wrap a shared counter's increment in `with threading.Lock():` so concurrent read-modify-write from multiple threads can't lose updates."}
{"id": "lesson-b019f172206b0e8b47a86125", "text": "Use `re.findall()` with a regex pattern to extract all integers from a string in one line."}
{"id": "lesson-b05cb946797d76232c674f06", "text": "Use `LinkedListNode<T>` for efficient insertion and deletion in an ordered list."}
{"id": "lesson-b0a07e49b84a6dc552dc182d", "text": "Build a sieve of Eratosthenes with a bytearray and slice-assignment (is_prime[i*i::i] = bytearray(...)) to mark composites in bulk instead of a Python-level inner loop."}
{"id": "lesson-b1175eec9a5d3ed3f6d08373", "text": "Use console.log() for simple output formatting with automatic line breaks."}
{"id": "lesson-b194b1327283773cde1271ee", "text": "Check for divisibility up to the square root of n for prime number verification."}
{"id": "lesson-b215f643053a5d6f3ffdd542", "text": "Use `bool` for boolean results to simplify conditional logic."}
{"id": "lesson-b21ca74179b66fed86c415d5", "text": "Use `-join` to concatenate array elements into a string."}
{"id": "lesson-b254bbad6c9e46177952b0f4", "text": "Keep transactions short: never hold a transaction open across a network call or external I/O, since held row and table locks block every competing writer."}
{"id": "lesson-b260fab2bf392cef17774fd1", "text": "Use optimized prime-checking algorithm for quick results."}
{"id": "lesson-b28e2e70d4eb7d4f861b97b5", "text": "Use mp.Pool(processes=N).map(func, iterable) to parallelize CPU-bound work; func must be a module-level, picklable function, not a closure or lambda."}
{"id": "lesson-b291bc61ebed29256a7635cb", "text": "Allocate short-lived batches from a bump/arena allocator and free the whole arena in one O(1) reset instead of walking objects for individual deallocation."}
{"id": "lesson-b2eb6476e24ce196081a7bfe", "text": "Use `std::filesystem` for path handling and validation to ensure cross-platform compatibility."}
{"id": "lesson-b32d3a80afa5e8edb45f40da", "text": "In C++ on Windows, pair GetIfTable2 with FreeMibTable via an RAII guard, and check result != NO_ERROR and a non-null table before iterating - the guard guarantees FreeMibTable runs on every exit path."}
{"id": "lesson-b34150f512952f82f2b14ce5", "text": "Use for-loop with index to reverse string in PowerShell."}
{"id": "lesson-b34afb9b942375f574e9a084", "text": "Use bitwise operations for manual serialization of multi-byte fields."}
{"id": "lesson-b35eb3717b0a30ddb78b7b81", "text": "Use `None` as default value for mutable parameters to avoid shared state between function calls."}
{"id": "lesson-b416fdbcade7449c3b5b6c88", "text": "Add explicit zero-checks before division operations to prevent ZeroDivisionError in movement calculations."}
{"id": "lesson-b42700893aadec8231e599fb", "text": "Use collections.deque for efficient O(1) pops from the front of the queue in topological sort."}
{"id": "lesson-b50d172fa05579e00fed74ba", "text": "Use `Map` for O(1) access and `Array` to maintain order for LRU cache."}
{"id": "lesson-b50e1e064a5af81f777f12af", "text": "Use OrderedDict for efficient key ordering and eviction in LRU cache implementation."}
{"id": "lesson-b528e75d14748cee7d729e03", "text": "Stack cross-cutting behavior with decorators wrapping the same interface, but note wrapping order changes semantics: caching-then-logging differs from logging-then-caching."}
{"id": "lesson-b570ac9ee27d993911966682", "text": "Acquire locks and rows in a fixed, consistent order across every code path so the database's deadlock detector rarely fires; retry the killed transaction rather than hand-serializing call sites."}
{"id": "lesson-b5d5fe7ddcb782eab33d4397", "text": "Initialize buckets with empty lists before appending words."}
{"id": "lesson-b5e3cb4e3901fcecdd8f6054", "text": "Validate string length, first character type, then check each char ensuring valid set of characters."}
{"id": "lesson-b606b3ce0f83a450b9014a6e", "text": "Use `std::vector` for storing collections of elements and range-based for loops for iteration."}
{"id": "lesson-b61819200bac72398a522a10", "text": "Use Python's built-in `sum()` function for quick and efficient summation of elements in a list."}
{"id": "lesson-b61c12f59425a9cd51f402b5", "text": "Use `print` function to output text directly."}
{"id": "lesson-b68dd8d989d57c5a4a608aac", "text": "Use a list as a stack, pushing openers and popping+matching against a bracket-pair dict, to check balance in one O(n) pass."}
{"id": "lesson-b6b8d2c12585071d22d4af6d", "text": "Implement Tarjan's SCC iteratively by pushing (node, iter(neighbors)) frames onto an explicit work_stack and resuming the same iterator object on re-entry, so deep graphs (thousands of nodes) never hit Python's recursion limit like a recursive Tarjan's would."}
{"id": "lesson-b70bd2dea53b7b2ae1daf778", "text": "Quickselect: partition around a random pivot and recurse only into the side containing the kth index for average O(n)."}
{"id": "lesson-b715abb78052a3df773f7bc6", "text": "Flatten arbitrary nesting with a recursive generator using 'yield from _walk(item)' and isinstance checks for list/tuple, not a fixed loop depth."}
{"id": "lesson-b75f75e7ad6c2906704f1355", "text": "Use pygame.time.Clock to control frame rate and ensure smooth animation loops."}
{"id": "lesson-b7aa531f47bef529f5095d99", "text": "Build push-based coroutine pipelines with a @coroutine decorator that primes each generator via next(), then chain stages with target.send(x); one stage can broadcast by calling send() on multiple downstream targets."}
{"id": "lesson-b7b2e6233c27a3ed5939dfe1", "text": "Use `None` as default mutable argument and initialize inside function to avoid shared state issues."}
{"id": "lesson-b7d3fb20f6f17c1be682433e", "text": "Use Array.reduce() with an initial value of 0 to sum array elements."}
{"id": "lesson-b7dba170297ab6b7b4ed9c5f", "text": "Use bitwise AND for prime checking."}
{"id": "lesson-b81d99417668321701f1621f", "text": "Replace static long-lived API keys with short-lived tokens minted via workload-identity or instance-metadata services, so a leaked credential expires in minutes instead of indefinitely."}
{"id": "lesson-b83daf73b7949a5c7e74ba69", "text": "Prefer try/except KeyError (EAFP) over `if key in d` (LBYL) for read-modify-write dict updates, since LBYL's check and act are two separate operations that a concurrent mutation can invalidate between them."}
{"id": "lesson-b873d39fa9775f66238edd95", "text": "Compute KMP's prefix table with the fallback loop 'while k>0 and pattern[i]!=pattern[k]: k=pi[k-1]' so the search loop reuses it for O(n+m) matching with zero backtracking in text."}
{"id": "lesson-b8849d8818fe54d9fd6f4830", "text": "Find LCA in a BST iteratively in O(h) by walking from root and moving left only when both targets are less than node.val, right only when both are greater, else returning the current node."}
{"id": "lesson-b88de48fda1b8bc25613ab40", "text": "Include `<iterator>` for `std::begin` and `std::end`."}
{"id": "lesson-b91bca9d605db5227941d29b", "text": "Represent occupied columns/diagonals as integer bitmasks and shift diag1<<1/diag2>>1 per row descent so each queen placement is O(1) instead of O(n) set lookups."}
{"id": "lesson-b92ea035e1197278d7646273", "text": "Implement MinHeap using sift-up and sift-down manually for efficient priority queue operations."}
{"id": "lesson-ba1acf52117e5b7872776733", "text": "Use `Object.entries` to iterate over key-value pairs in an object for finding the most frequent word."}
{"id": "lesson-ba66553436903cee7e4af318", "text": "Use Kahn's algorithm for topological sorting in directed acyclic graphs (DAGs)."}
{"id": "lesson-bb821e9327d2d65212d4b507", "text": "Use a for loop with condition `$i * $i -le $number` to efficiently check primality."}
{"id": "lesson-bba7d89fa11d863db9827693", "text": "Use a dictionary to map numbers to their indices for quick lookup in two-pointer or hash-based solutions."}
{"id": "lesson-bbb3a1530fa1275479054980", "text": "Use linear interpolation for smooth transitions between values based on a distance or threshold parameter."}
{"id": "lesson-bbbc6ab4c8c6f834bb05a865", "text": "Top-down memoization with functools.lru_cache still recurses on Python's call stack, so an uncached O(n) chain (edit distance on 2000-char strings) hits RecursionError; switch to bottom-up tabulation instead of raising sys.setrecursionlimit."}
{"id": "lesson-bbe7baa3ea46f7ae21364651", "text": "Implement topological sort with Kahn's algorithm: seed a deque with zero in-degree nodes, decrement neighbors' in-degree on pop, and flag a cycle if the output length is short of the node count."}
{"id": "lesson-bc0bc1ced34fe1698a6a20f7", "text": "Use abc.ABC with @abstractmethod to make a class uninstantiable until subclassed, then favor composition (store a strategy object) over inheriting from it so behavior can be swapped at runtime."}
{"id": "lesson-bc6dc8b3f07f8103f9e61b58", "text": "Run red-green-refactor as three real pytest passes: an incomplete implementation must fail before pytest.raises(...) passes, then keep tests green after restructuring internals."}
{"id": "lesson-bd2c3c27c3d9a78c00fe2254", "text": "Use `foreach` loop for iterating arrays in PowerShell."}
{"id": "lesson-bd4ecddcf9e92037191f9219", "text": "When you .throw() into a generator suspended inside `yield from subgen()`, Python forwards the exception into subgen itself, so subgen's own try/except handles it and delegation continues transparently, yielding subgen's response."}
{"id": "lesson-bd596860316f1f0ff0012e4b", "text": "Wrap each pool.imap next() call in try/except: worker exceptions re-raise only at that item's fetch, preserving already-computed results -- unlike pool.map, which discards everything on first failure."}
{"id": "lesson-bd6d8e7634b1b594a500135f", "text": "Use CSS animations for smooth transitions between states."}
{"id": "lesson-bd81041c7c6a6dce2db5ed53", "text": "Dedup newly distilled lessons by embedding cosine similarity against all stored lessons before inserting, so near-duplicate phrasings don't bloat the memory store."}
{"id": "lesson-bd863f62e152a2a528408c22", "text": "Use `ForEach-Object` to reverse string indexing."}
{"id": "lesson-bda0a601a8651ce10df63709", "text": "Bind `(y := expensive(x))` in a comprehension's if-clause to reuse the computed value in the output expression, avoiding calling the same expensive function twice per element."}
{"id": "lesson-bdda5e6aa9a77b93c869ebca", "text": "Use `pygame.draw.circle` for enemy visuals in tower defense games."}
{"id": "lesson-be9ffdd3dd626006c3891741", "text": "For minimum window substring, track one 'missing' counter (chars still needed) instead of dict equality checks, and only shrink lo while need[s[lo]] stays negative."}
{"id": "lesson-becddee012a64114257735eb", "text": "Use `heapq.merge` for k-way merging of sorted inputs efficiently."}
{"id": "lesson-bf16e73c06e5e5714af3a125", "text": "Reproduce a heisenbug by pinning nondeterminism: fixed random seed, single thread, and recorded inputs — most 'random' bugs are order-dependent."}
{"id": "lesson-bf61516b476b05d027c072ba", "text": "Use Fraction(*x.as_integer_ratio()) to extract the exact binary64 rational a float stores, then diff two such Fractions to prove the precise rounding error behind failures like 0.1+0.2 != 0.3."}
{"id": "lesson-bf6e25f3a6cc86cf65c85004", "text": "Use `result = yield from subgen()` to delegate to a subgenerator transparently and capture its StopIteration.value (from `return 'x'` in the subgenerator) as the expression's result."}
{"id": "lesson-bf971676d1a3b56bef2f3fd7", "text": "Use try/except blocks to handle conversion errors when parsing strings to integers."}
{"id": "lesson-c00968771930b4cde0706d24", "text": "Track shot sequence IDs to prevent retransmit ammo deduction."}
{"id": "lesson-c010fdbd354f519e345333d5", "text": "Initialize edit-distance DP's first row/column to range(n+1)/range(m+1), the cost of pure insertions or deletions, before applying the min(delete, insert, replace) recurrence."}
{"id": "lesson-c013f864dd9d99b6c75df920", "text": "Iterative BST search needs no stack: loop while node is not None, compare target to node.val, and step into node.left or node.right based on the comparison for O(h) time O(1) space."}
{"id": "lesson-c0616a54643ff36ed1fdd4c3", "text": "Use Write-Output for explicit string output formatting in PowerShell scripts."}
{"id": "lesson-c0d9c386e010b105e1d794e9", "text": "Use `BEGIN IMMEDIATE` transactions for atomic operations in SQLite."}
{"id": "lesson-c139d6733b68c07501bb207f", "text": "Use a set to track seen elements for O(1) duplicate detection in validation problems."}
{"id": "lesson-c14f20b72bc73a2ad853e395", "text": "Normalize text for comparison by filtering with ch.isalnum() and ch.lower() before checking cleaned == cleaned[::-1]."}
{"id": "lesson-c1cee79d0a328c4c9db37c09", "text": "Use `std::reverse` for string reversal in C++."}
{"id": "lesson-c2383537242fcfa804f03753", "text": "Set an explicit timeout on every outbound call (network, DB, lock); an unbounded wait turns one slow dependency into total thread-pool exhaustion."}
{"id": "lesson-c2fe2a5e3920fbf26a2cef40", "text": "Use parameterized queries to prevent SQL injection and ensure atomic updates in concurrent environments."}
{"id": "lesson-c35be7b8d1bb4d2660dbbee1", "text": "Alert on user-facing symptoms (error-rate or latency-percentile SLO burn) not causes (CPU%, disk%); cause-based alerts fire on noise without confirming real user impact."}
{"id": "lesson-c403baf97e6644520a8764b9", "text": "Don't trust big-O alone: an O(n log n) algorithm with scattered memory access can lose to a cache-friendly O(n^2) one for small-to-medium n."}
{"id": "lesson-c4185debb6e3ce451ef29153", "text": "Use in-degree counts and a queue to process nodes with zero dependencies first ensuring no cycles are missed."}
{"id": "lesson-c43a3ec6921634515ae0f00d", "text": "For strictly-increasing LIS via patience sorting use bisect_left on the tails array; bisect_right would compute the longest non-decreasing subsequence instead."}
{"id": "lesson-c4514b79bf3ead199f74e101", "text": "Build bytecode VM dispatch as a loop over an opcode array with an explicit operand stack; measure whether computed-goto/threaded dispatch beats switch on mispredicted branches."}
{"id": "lesson-c47887da0093a0ebc9ae9cd7", "text": "Set Mock.side_effect to a list mixing exceptions and return values to script retry/error-path behavior across successive calls in one test."}
{"id": "lesson-c4a1ff46ed77ee47a28cfe98", "text": "For sliding-window median, don't rebuild heaps on eviction: tombstone the outgoing value in a delayed-removal counter dict, adjust logical small/large sizes immediately, and only pop the tombstoned value from a heap once it actually surfaces at heap[0]."}
{"id": "lesson-c4c2f093d99ed6915ed1d4c8", "text": "Write characterization tests that record current output, bugs included, as the baseline before refactoring, so changes are measured against real behavior, not assumed intent."}
{"id": "lesson-c4ce062b92599f5194ad0f17", "text": "Use `try-except` to handle invalid input gracefully."}
{"id": "lesson-c4f727ab6059c9d4ede8998c", "text": "Parametrize a container class with `Generic[T]` and a TypeVar so `Stack[int]` and `Stack[str]` share one implementation with distinct static types."}
{"id": "lesson-c512113cefcc2c85d5b25a7e", "text": "Replace a hard-coded `new ConcreteDependency()` with constructor or parameter injection so a test can substitute a fake without editing the class under test's internals."}
{"id": "lesson-c52f9e7b83dabb62c7eeae79", "text": "Implement CRC16-CCITT-FALSE using bitwise operations and a loop for polynomial division."}
{"id": "lesson-c576cf173cec1fb27ae9cb9c", "text": "Combine binary-search-on-the-answer with a greedy feasibility partitioner (parametric search) for 'split array to minimize largest subarray sum': bisect the answer, greedily pack until overflow, check partitions <= m."}
{"id": "lesson-c5ba27ae8d87d7091b7ddda1", "text": "Calibrate a semantic relevance threshold empirically: measure cosine scores of known-irrelevant probes vs known-relevant pairs against the real corpus, then pick a cutoff that clears the noise ceiling."}
{"id": "lesson-c5d0712c375e6a5ce2c57d46", "text": "Shrink a failing case by deleting or simplifying inputs/code while the bug still reproduces, until nothing more can be removed."}
{"id": "lesson-c6094495dccc1b77b6fbfe5e", "text": "Use array slicing with `$s[$s.Length..0]` to reverse a string in PowerShell."}
{"id": "lesson-c6ada42b02b233415bb0bd15", "text": "Use `foreach` loop for iterating arrays efficiently."}
{"id": "lesson-c728f71669c3dac557a52b87", "text": "Resolve conflicts with git checkout --ours/--theirs <path> when one side should win wholesale, or hand-edit <<<<<<< markers when both sides changed meaningfully."}
{"id": "lesson-c74bc055538e7aeffd379883", "text": "Capture HRESULT for error checking before using COM objects."}
{"id": "lesson-c7f80fe27fc496cf877722d6", "text": "Use git cherry-pick -x <sha> to port a single fix across branches; the -x flag records the origin SHA in the message for later traceability."}
{"id": "lesson-c7fcb0ac34e63c3be0e5936b", "text": "Use `os.scandir` for efficient directory traversal with easy filtering."}
{"id": "lesson-c81a534f8934b1c94545557a", "text": "Bisect corrupted or drifting data (not just code) by binary-searching the dataset/config itself — comment out half the migrations, rows, or feature flags per step — when the regression isn't in a commit."}
{"id": "lesson-c830de6e46f4b6b2e3ab4391", "text": "Use bitwise shifting and masking to manually convert values to big-endian byte order."}
{"id": "lesson-c8485310b572b2ae22369e53", "text": "Separate metrics (cheap aggregated time series for dashboards and alerts) from traces (expensive per-request causal spans for root-causing one slow request) - don't conflate them."}
{"id": "lesson-c857b8fd1e8f24c07a5d4686", "text": "Model AST nodes as a tagged union/sum type per kind so an exhaustive switch flags every visitor that forgot to handle a new node."}
{"id": "lesson-c868c5692f88e09f86e2a8bf", "text": "Use Path.with_suffix to swap a file extension in one call instead of string-slicing the path; it operates on the final suffix only, so '.log.bak' replaces just '.log'."}
{"id": "lesson-c898cc38a52e2039adcdfd2d", "text": "Use console.log() for direct output when no complex processing is required."}
{"id": "lesson-c8ba8ae429414029b9cffc65", "text": "Use `str(abs(n))` to convert integers to strings for digit manipulation, enabling easy iteration over individual digits."}
{"id": "lesson-c9215952bf403cc664dc4404", "text": "Use clamping to ensure output doesn't exceed max value efficiently."}
{"id": "lesson-c97eb9351fb23b515e044398", "text": "Use the square root optimization (i * i <= n) in prime checking loops to reduce time complexity from O(n) to O(√n)."}
{"id": "lesson-c9c9186f1e014549bc1139aa", "text": "Use path compression and union by rank in DisjointSet for efficient operations."}
{"id": "lesson-c9ce9cd2bd184eae42c27156", "text": "Detect stale handles into a recycled pool slot with generation counters: reuse bumps the generation so old (index, generation) handles fail comparison instead of aliasing the new occupant."}
{"id": "lesson-c9d8e2abbd24e09219fdb5b2", "text": "Implement right-to-left function composition with functools.reduce(lambda acc,f: f(acc), reversed(funcs), x); zero funcs must reduce to identity."}
{"id": "lesson-c9dfebbcdf5651d77824fe8c", "text": "Use manual string reversal with a loop for simple cases."}
{"id": "lesson-ca01fb1a1097a360ef0a44fd", "text": "Use for-loop to reverse string by index/order and join explicitly with -join."}
{"id": "lesson-ca190d46787440397630b788", "text": "Use `-join` to concatenate characters into a string in PowerShell."}
{"id": "lesson-ca4a27c7f6ed6e457d077a0b", "text": "If a vendored header emits an auto-link pragma with a name that never matches your build's output, define its own suppression macro PUBLIC so all consumers skip it."}
{"id": "lesson-ca4b4dca002a2337600e44ea", "text": "Init the first row/col of the grid DP to 1 (only one path along an edge), then dp[i][j]=dp[i-1][j]+dp[i][j-1] for path counts."}
{"id": "lesson-ca9cdc3c5be0ca3159dcf4c6", "text": "Include `<algorithm>` for `std::reverse`."}
{"id": "lesson-caf5d673a764dc058f3226aa", "text": "Check that test assertions target observable behavior, not internal call sequences — over-mocked tests break on valid refactors that leave output unchanged."}
{"id": "lesson-cb5a4ff8ba807c436aca873c", "text": "Include `<iterator>` for `std::begin` and `std::end`. Use `<iostream>` for `std::cout`."}
{"id": "lesson-cc97076d5dc363a16e431d95", "text": "After bulk loads or large deletes, re-run ANALYZE (or equivalent) manually — autovacuum/auto-stats lag can leave the planner using outdated cardinality estimates for hours."}
{"id": "lesson-cca772af13076afadd33232d", "text": "A health check that only alarms when a success rate is too LOW cannot see a rate inflated by self-generated tests."}
{"id": "lesson-ccad1459a00e67c577db524b", "text": "Force the compiler to keep benchmarked work: use a blackbox/DoNotOptimize sink or write results to a volatile/escaped variable so dead-code elimination can't erase the loop you're timing."}
{"id": "lesson-ccb1b4d58660eb02f96f71bd", "text": "Morris inorder traversal achieves O(1) space by threading each node's inorder predecessor's right pointer to itself on first visit, then detecting and unthreading (restoring None) on the second visit before descending right."}
{"id": "lesson-ccc2ffe26abd224a1dc522eb", "text": "Use foreach loops with range operators (2..n/2) to efficiently check for prime factors in PowerShell."}
{"id": "lesson-ccff88e5e68e1a7b0faa1eca", "text": "Diff flamegraphs from before/after profiles on identical input instead of comparing single wall-clock numbers; the diff isolates the actual delta from run-to-run noise."}
{"id": "lesson-cd685439bb5fb620191fe96a", "text": "Distinguish a constrained TypeVar (`TypeVar('N', int, float)`, exposing `.__constraints__`) from a bound one (`.__bound__`); Python enforces neither, so isinstance-check `.__constraints__` yourself, watching for bool passing as int."}
{"id": "lesson-cd6cc1aa8ae9a3fbd437bf9c", "text": "Compose map() then filter() with a helper that returns None on failure (not raise) so filter can drop bad-parse sentinels in one pass."}
{"id": "lesson-cdfee30c443a41fcb1fe54ca", "text": "Use dictionary unpacking to merge dictionaries with b's values taking precedence on conflicts."}
{"id": "lesson-ce2cf55583e04bf0c4bc23e6", "text": "Use functools.reduce with math.gcd to compute the GCD of multiple numbers efficiently."}
{"id": "lesson-ce3076659c43889ea4eae965", "text": "Ensure inclusive conditions in if-statements for all possible input values."}
{"id": "lesson-ceb082f007852f7bf956ac21", "text": "After n-1 Bellman-Ford passes, run one more relaxation pass to flag vertices still improvable, then flood-fill successors to mark the whole negative-cycle-reachable set."}
{"id": "lesson-cffc61dab123dca519381ae9", "text": "Identify and use the shortest string to reduce comparisons, optimize prefix search."}
{"id": "lesson-d04ee1090e016aeb564e90e4", "text": "Validate IPv4 octets with str.isdigit() plus an explicit leading-zero check (len>1 and p[0]=='0'), since int('0.0.0.0'.split('.')) alone accepts '01' as 1."}
{"id": "lesson-d04f61cbc070d0e303400c3d", "text": "Use reverse iteration and subtractive rules for RomanToInt efficiently."}
{"id": "lesson-d0d9d3e34a5b579f1535713f", "text": "Use modulo operation for prime checking."}
{"id": "lesson-d110dc57f134b352af616b52", "text": "Wrap an incompatible third-party interface in a thin adapter implementing your own interface, so the rest of the codebase never imports the vendor type directly."}
{"id": "lesson-d118769a3871dd4ce3012f43", "text": "`New-Object System.Collections.Generic.List[System.Tuple[string, string]]` should be initialized with `@()` instead of `New-Object`."}
{"id": "lesson-d125a3cf91213fdb4fcad16a", "text": "Use `Where-Object` for filtering collections in PowerShell."}
{"id": "lesson-d15e361cff3100944d24bbe4", "text": "Shard with consistent hashing instead of key mod N: adding or removing a shard remaps only ~1/N of keys instead of nearly all of them"}
{"id": "lesson-d16fb5c9bf99c40deb7331b0", "text": "Use std::stack for LIFO bracket matching."}
{"id": "lesson-d18276eed83eb706b3cdcb1b", "text": "Wrap a pattern in a lookahead capturing group (?=(...)) and scan with finditer to get overlapping matches, since normal matching always consumes and skips past what it just matched."}
{"id": "lesson-d2514e85aa74c0360e13c548", "text": "In a diamond hierarchy, every override must call super().method() cooperatively -- C3 linearization (__mro__) guarantees each class runs once, but skipping one super() call breaks the chain and silently drops remaining classes."}
{"id": "lesson-d34175a3b0ebbfaef3dc5658", "text": "Wrap a task in asyncio.shield() before wait_for so an outer timeout only abandons the wait, not the task; await it again for the real result."}
{"id": "lesson-d45eb30656b56aa2b16375a3", "text": "Use compound indexes to optimize query performance by covering multiple conditions and sorting requirements."}
{"id": "lesson-d4d5ab549b467e80aa4904bd", "text": "Trace every return and exception path from a resource acquisition to its release; leaks hide in early returns or caught-exception branches that skip cleanup."}
{"id": "lesson-d4d8efbde1ef42d9890d800c", "text": "Compare secrets like password hashes, HMACs, or API tokens with a constant-time function (hmac.compare_digest, crypto.timingSafeEqual), never `==`, to close timing side-channels."}
{"id": "lesson-d502e8393d1c092088cdb731", "text": "Subclass json.JSONEncoder and override default() to serialize datetime/Decimal as tagged dicts, then call super().default(obj) for anything unsupported."}
{"id": "lesson-d50844f7aee0468b36dc02d1", "text": "Never monkeypatch.setattr(datetime.datetime, 'now', ...): it's an immutable C type. Instead subclass datetime and monkeypatch the module's imported name to the subclass."}
{"id": "lesson-d513a6c17417f9fc43ace193", "text": "Prefer hash joins for large unsorted equi-joins and nested-loop joins only when one side is tiny or already indexed; force a plan hint if the optimizer picks nested-loop on a big table."}
{"id": "lesson-d5efa78d35e9ace8a70e634f", "text": "Pair exponential backoff with a retry budget (token bucket per client) so retries drain gracefully instead of amplifying a downstream outage into a bigger overload spike."}
{"id": "lesson-d64077d28f88543876d9778b", "text": "Use `str(n)` to convert integers to strings for digit manipulation, then `int(d)` to convert back."}
{"id": "lesson-d64a0927cd3c0200e42fe399", "text": "Destroy engine-owned ECS registries and pools before FreeLibrary on a module DLL, not after — dangling vtables from unloaded code crash or hang at exit."}
{"id": "lesson-d6bd04e2fbf76dc271e7c367", "text": "When porting a pointer-holding integer cast to i386, use `unsigned long` (32-bit pointers); `unsigned long long` is only correct on x86_64."}
{"id": "lesson-d6eba3888d5061209454aa2d", "text": "Size shard count from peak QPS times a 2-3x headroom multiplier divided by per-node throughput, not from average traffic, so the design survives traffic spikes without a fire-drill resplit"}
{"id": "lesson-d733d466dfb01d7714aa4dae", "text": "Chain multiple zero-width (?=...) lookaheads at the start of a pattern to require several independent conditions (uppercase, digit, min length) regardless of their order in the string."}
{"id": "lesson-d73939d47d01c0ccb3c579fe", "text": "Use `std::vector<int>` to collect indices for `ForEachSetBit` callback."}
{"id": "lesson-d783ffe684b97f5d93254e0f", "text": "Use the fixed witness set [2,3,...,37] for Miller-Rabin to get a deterministic (not probabilistic) primality test for any 64-bit n, verified against a known strong pseudoprime like 341550071728321."}
{"id": "lesson-d7a1333265ee40fa8b04d70f", "text": "Load and generate basic navmesh or waypoint data for bot navigation."}
{"id": "lesson-d7b8737261f3a9faf64fb1ed", "text": "Form exactly one falsifiable hypothesis before adding a print or breakpoint, then design the check so only that hypothesis survives or dies — don't scattergun edits."}
{"id": "lesson-d7e0229d8b2ad2edd1ac84b6", "text": "Use `yield from` to recursively flatten nested iterables into a single flat list."}
{"id": "lesson-d8640402da6cb96637371e63", "text": "Use std::cout with std::endl for immediate output flushing in C++ console applications."}
{"id": "lesson-d8b9ac9499ea6e3470d4c553", "text": "Build LSD radix sort as repeated passes of a stable per-digit bucket pass (list-of-lists by digit, then concatenate); each pass must itself be stable or the final ordering corrupts across digit positions."}
{"id": "lesson-d8d46314a1abfaaa4f4878e9", "text": "Use String.prototype.split(''), Array.prototype.reverse(), and Array.prototype.join('') to reverse a string in JavaScript."}
{"id": "lesson-d920ac10e9a22eeb0fdc06e2", "text": "Never pickle.loads() untrusted data; override Unpickler.find_class() with a (module,name) whitelist to raise UnpicklingError on anything not explicitly allowed."}
{"id": "lesson-d9225589bfa0d53b1182e94d", "text": "Use `break` to exit loop early when condition met."}
{"id": "lesson-d9a91a2b90a4df842c40a798", "text": "Iterate over entire range for inclusive calculations."}
{"id": "lesson-da57407c97b8f8d26aea5035", "text": "Use heapq.nsmallest() to efficiently find the k-th smallest element in O(n log k) time."}
{"id": "lesson-da6832b9cb0ab132e82213a3", "text": "To split two unique elements hidden among duplicates, XOR the whole array then isolate one differing bit via x & -x, and use that bit to bucket elements into two XOR-reduce groups."}
{"id": "lesson-da794b40dec96e2a49576d84", "text": "Use threading.RLock (not Lock) when a locked method calls another locked method on the same object from the same thread, or it self-deadlocks."}
{"id": "lesson-daa5dd323adc476e284ea7c1", "text": "Adjust range end by +1 to include endpoint."}
{"id": "lesson-dab0f6d1c326b61651f0c7cc", "text": "For an untestable constructor doing I/O, extract the offending call into a protected method and subclass in the test to override it with a fixture, a stepping stone toward full dependency injection."}
{"id": "lesson-daee61452ad099d838e80e01", "text": "Match output encoding to context: HTML-entity-encode inside tag bodies, JS-string-escape inside script blocks, URL-encode in query params — one encoder does not cover every sink."}
{"id": "lesson-daf85feab1ba81e5ccf25e65", "text": "Store all relevant flags to maintain state across events and frames."}
{"id": "lesson-daffd9847f4b044ab4e33cf2", "text": "Build Huffman codes with heapq on [weight, [symbol, code]] lists, prefixing '0'/'1' onto every trailing pair when merging two heap nodes, so ties break via list comparison not custom classes."}
{"id": "lesson-db0edceb5b0cada48d193068", "text": "Build a FIFO queue from two stacks: push onto in_stack always, only flip into out_stack when it's empty, giving O(1) amortized dequeue."}
{"id": "lesson-db35cc518171c2d1d6651f3a", "text": "Check input bounds before processing to prevent resource exhaustion and crashes."}
{"id": "lesson-db484699cec4ffdc693320b2", "text": "Use a list to collect intermediate results then join at the end for O(n) string concatenation."}
{"id": "lesson-dbbf5450d2cc36f95e4bdacc", "text": "Wrap modular arithmetic in (a op b) % m; Python's % always yields a non-negative result for a positive modulus, so no manual sign-fix is needed."}
{"id": "lesson-dbe60ead0a5539ac726985c2", "text": "Use modulo for wrapping around alphabet, keep non-alphabetic chars unchanged."}
{"id": "lesson-dbeb84121c80aaf6bb85d6d3", "text": "Use Gosper's hack (c=v&-v; r=v+c; return ((r^v)>>2)//c | r) to jump directly to the next integer with the same popcount, avoiding an O(2^n) brute-force scan."}
{"id": "lesson-dbecd8e04b85deb09e2720f1", "text": "Use heapq.merge() to efficiently merge multiple sorted iterables into a single sorted iterator."}
{"id": "lesson-dc2630bedfb84f06f0c073e5", "text": "Normalize text with TextUnformatted for consistent parsing across clients."}
{"id": "lesson-dc52b4f2d8b96627903fa4e5", "text": "Merge two sorted lists with a two-pointer walk, appending the smaller head each step, then the leftover tail."}
{"id": "lesson-dcd987e0a4f5e74a417e77cb", "text": "condition_variable wait_until/wait_for keyed on steady_clock can hang forever if the OS steps the wall clock backward; use relative-timeout waits for anything that must not freeze."}
{"id": "lesson-dd0a42a23ead3151730ea782", "text": "Replace OFFSET-based pagination with keyset (seek) pagination: WHERE (sort_col, id) > (last_val, last_id) LIMIT n, so deep pages don't scan and discard skipped rows."}
{"id": "lesson-dde92826365c2088b4ea343e", "text": "Validate file signatures using `std::ifstream` and binary read operations."}
{"id": "lesson-de8dd30fe8c163e22ece7d7f", "text": "For grid flood-fill/island counting, mark cells visited at push time (not pop time) in the DFS stack to prevent the same cell being pushed multiple times by different neighbors."}
{"id": "lesson-de9421dfe307bee506bcdd3a", "text": "Use CSS3 `transform` with `perspective` and `rotate` for creating 3D effects."}
{"id": "lesson-dfeaa237a52bc2940caa08e4", "text": "Use string multiplication with boolean values (not x % y) to conditionally build FizzBuzz strings in a list comprehension."}
{"id": "lesson-e05924642548d8461ac83c7f", "text": "Use collections.OrderedDict to implement LRU cache with O(1) get/put operations and automatic ordering."}
{"id": "lesson-e05f7dd6553f447ffe891da3", "text": "Use maximal munch when lexing: at each position match the longest possible token (e.g. '==' before '=') to avoid splitting multi-char operators."}
{"id": "lesson-e0876eccb2ab029b7cd4e4cd", "text": "Use early termination in loops to avoid unnecessary iterations when a condition is met."}
{"id": "lesson-e08982e1b46a5bfac126d6d0", "text": "Generate a unique random salt per password via a CSPRNG and store it with the hash; a shared or missing salt lets attackers precompute one rainbow table for every user."}
{"id": "lesson-e14b66b17b4c5c3b86897abb", "text": "Manually reverse string using two-pointer technique for O(n) time complexity."}
{"id": "lesson-e18946b939dba68b4b492b74", "text": "Use `New-Object PSObject` to create custom objects for intervals."}
{"id": "lesson-e18b5f824bedef50dc9ed857", "text": "Use `std::cout` for printing to standard output in C++."}
{"id": "lesson-e20b27d53071f56a636f497e", "text": "Detect a cycle in an undirected graph by passing the DFS parent into recursion and only flagging a cycle when a visited neighbor is NOT the immediate parent, since undirected edges naturally back-reference the parent."}
{"id": "lesson-e217e57d2360d94902b2c8f4", "text": "Apply backpressure with bounded queues and explicit load-shedding (fast 503/429) instead of unbounded buffering, which only delays an OOM crash further downstream."}
{"id": "lesson-e233cdf1f0f4a1686f85df83", "text": "Use CSS animations for simple webpage effects."}
{"id": "lesson-e2678dc6a7637c96daf2d71f", "text": "Use a for loop with a condition to check divisibility up to the square root of the number for prime verification."}
{"id": "lesson-e26a84220d7e2bb12e06538d", "text": "Reconstruct Floyd-Warshall paths with a next-hop matrix: set nxt[i][j]=nxt[i][k] on relaxation, then walk nxt[u][v] repeatedly until reaching v."}
{"id": "lesson-e2742eadf44dc2bc29d6965b", "text": "Deploy a nonce-based Content-Security-Policy with strict-dynamic instead of a host allowlist, since allowlisted domains are routinely bypassed via their own JSONP or reflected-script endpoints."}
{"id": "lesson-e2a04088023d3011c20b7d9d", "text": "Use `Spark::Json::Value` methods like `HasKey`, `IsArray`, and `AsNumber` for robust JSON parsing."}
{"id": "lesson-e2c97ef724da612ee6a4eb19", "text": "Use a dictionary to map Roman numerals, iterate from end to start for subtraction rule, and use one variable for previous value."}
{"id": "lesson-e2f959ffe74bd2f6ba5153c6", "text": "Use `max()` and `min()` functions to clamp values efficiently without explicit conditionals."}
{"id": "lesson-e32c3ccaa0b028c3d9c0b424", "text": "Wrap int(str(s).strip()) in try/except (TypeError, ValueError) to return a caller-supplied default on bad input."}
{"id": "lesson-e33a231dbce78c086f43a0d2", "text": "Replace precedence-level parser cascades with Pratt/operator-precedence parsing: give each operator a binding power so adding one is a one-line table edit."}
{"id": "lesson-e39f935db53989c71fecc639", "text": "Use foreach loops with arrays to iterate through elements and accumulate sums reliably."}
{"id": "lesson-e3ec434c1f9ac31789e8a2a6", "text": "Mark a Protocol `@runtime_checkable` to let `isinstance(obj, MyProtocol)` structurally match any object with the right methods, no inheritance or registration needed."}
{"id": "lesson-e3fa7a0e026b4433625afe3c", "text": "Detect a cycle in a directed graph with 3-color DFS (white/gray/black): a back-edge to a GRAY (on the current recursion stack) node means a cycle, but an edge to a BLACK node is a safe cross/forward edge, not a cycle."}
{"id": "lesson-e4330468ed2d09747b5916bc", "text": "Use `str.split()` without arguments to split on any whitespace and handle empty strings gracefully."}
{"id": "lesson-e44ffabe04b7d5dd1dfa6767", "text": "Use `console.log` to print numbers on separate lines."}
{"id": "lesson-e488dddb9e457952eaf5629c", "text": "Emit structured JSON logs with a correlation/request ID field propagated through every hop, header, and async job so one request's path is reconstructable from logs alone."}
{"id": "lesson-e4f3d3325c0d6e2db91237a1", "text": "Use Stack for LIFO bracket matching."}
{"id": "lesson-e56ee6f7a69974d5e04b3578", "text": "Compute modular inverses with the extended Euclidean algorithm, not pow(a, m-2, m), whenever the modulus may be composite since Fermat's-little-theorem inverse only holds for prime moduli."}
{"id": "lesson-e592a5671f9ab58df71e295c", "text": "Prefer rebase to replay commits onto a new base for linear history; prefer merge to preserve true branch topology and both parents."}
{"id": "lesson-e5a3355c8846844a689a9430", "text": "Replace a correlated subquery per row with a window function (SUM(...) OVER, ROW_NUMBER() OVER) so the engine computes it in one pass instead of once per outer row."}
{"id": "lesson-e5c82da9b114e8514504d514", "text": "Swap two list elements in place with three XOR assignments (a^=b; b^=a; a^=b), but guard the same-index case or XOR-swap zeroes the value."}
{"id": "lesson-e60dc669aafecb299ce84814", "text": "Use string multiplication with boolean expressions to concisely build conditional strings in list comprehensions."}
{"id": "lesson-e6253d99576777b5533a2514", "text": "Use dataclasses.field(default_factory=list) for mutable defaults so each instance gets its own list, then use __post_init__ to validate/normalize constructor input."}
{"id": "lesson-e65245b732cd3bffe2ed5e0a", "text": "Before trusting a flaky-test fix, loop the test 100-1000x in isolation under a stress runner to confirm the timing window actually closed."}
{"id": "lesson-e6674e16c4f5304ba74ad039", "text": "Use `try-except EOFError` to handle end-of-input gracefully."}
{"id": "lesson-e68e331fd980a76b102712d3", "text": "Use `os.path.splitext` to extract file extensions efficiently."}
{"id": "lesson-e6ba41eb27c33b18a5625459", "text": "Use `for` loop with condition `i * i <= num` for efficient prime checking."}
{"id": "lesson-e6d074c3870660716d5c424d", "text": "Check loop and slice boundaries against a concrete tiny case (n=0, n=1, empty input) instead of trusting intuition — off-by-ones hide in < vs <= and inclusive ranges."}
{"id": "lesson-e7090bdd114b4e6b22d71d6d", "text": "Use a stack (list) to match opening and closing brackets in LIFO order for balanced bracket validation."}
{"id": "lesson-e71a7020474938cc13785b9a", "text": "Return GLM_ROUTE_OK directly."}
{"id": "lesson-e74d956d11170b4b421aaaad", "text": "Use explicit parent keys in `ProcessCreate` to avoid inference pitfalls."}
{"id": "lesson-e7655dd76e447f578f0a0cc0", "text": "Ensure transactional pattern correctly handles success and failure in `DecodeOne`."}
{"id": "lesson-e78c07cef8aaa59cf271e9c6", "text": "Tune chunksize in Pool.imap: chunksize=1 gives fine-grained load balancing but high IPC overhead; larger chunksize cuts overhead at the cost of balance."}
{"id": "lesson-e7e341c607480a38d5e69993", "text": "Use modular arithmetic with ASCII values to implement character shifting while preserving case and non-letter characters."}
{"id": "lesson-e7f42ef8122b7ead63922b0a", "text": "Use iterative multiplication with a loop to compute factorials efficiently in O(n) time."}
{"id": "lesson-e84d51d2921a48f0113e673d", "text": "Handle Windows symlink edge cases with `os.unlink()` and path normalization."}
{"id": "lesson-e89394f99d8882d68bc33bff", "text": "Assign each request a client-generated idempotency key and cache its result server-side, so a retried request returns the stored outcome instead of executing twice."}
{"id": "lesson-e8a336c925f72290717fcac8", "text": "Use `requests.get` for simple HTTP GET requests."}
{"id": "lesson-e8a7ddf5b0603286c66f8ced", "text": "Undo side effects of probes immediately to maintain accurate state."}
{"id": "lesson-e8a84a3ccf80ee84e754d2df", "text": "Use slicing for quick string reversal."}
{"id": "lesson-e8ef437bceae45d43319c815", "text": "Initialize buckets before appending to avoid key errors."}
{"id": "lesson-e9247f91cd2763a01cd76482", "text": "Use indexing to reverse strings in PowerShell."}
{"id": "lesson-e9436ea5ee6b067aaa0d5c6a", "text": "Use functools.cmp_to_key to plug a pairwise comparator (e.g. fuzzy-tie score bands with alphabetical tiebreak) into sorted() when no single key tuple can express the ordering."}
{"id": "lesson-e9544f4745c25d6a6bd692d4", "text": "Track sliding-window maxima in O(n) with a deque of indices kept decreasing, popping expired fronts and smaller tail values."}
{"id": "lesson-e9aeb556c6b04827904a4e09", "text": "Use modular arithmetic with `ord()` and `chr()` to efficiently shift characters while preserving case and non-letter characters."}
{"id": "lesson-e9ee4633ac1b1119fa7a45fe", "text": "For template engines, never pass user input as the template source itself; bind it only as data into a fixed template, since sandboxed engines still leak via attribute/method access chains (SSTI)."}
{"id": "lesson-ea179cb18a767c1bc6fb0ec6", "text": "Rely on the 'with open(...) as f' block to guarantee f.close() runs on any exit path; check f.closed after the block instead of manually tracking close calls."}
{"id": "lesson-ea34fa2ebe9d4cd5a8ef2aad", "text": "Use std::priority_queue with std::greater for Dijkstra's shortest path algorithm."}
{"id": "lesson-ea75de839d3a42b0cc440f74", "text": "Use bitwise shifts and masks for endian-safe serialization."}
{"id": "lesson-eab2599d13e5b32a600ec26a", "text": "Use `capitalize` method on each word after splitting string; join with spaces."}
{"id": "lesson-eb00decab7c77dc0aec99e6c", "text": "Prefer time.monotonic_ns() integer accumulation over summing many time.monotonic() float diffs -- sub-microsecond float additions compound binary rounding error over many iterations; integer nanosecond sums stay exact."}
{"id": "lesson-ebb3ffb1a3861c3288e1d6b9", "text": "For counting sort, iterate the input in reverse when placing elements from the cumulative-count array; forward iteration silently reverses same-key order and breaks stability."}
{"id": "lesson-ec1a60e100545002d98c8700", "text": "Use a loop with `i * i <= n` for prime checking to optimize performance."}
{"id": "lesson-ecab524f463255ec069ed79f", "text": "Factor primes by trial division: strip factors of 2 first, then test odd d from 3 while d*d<=n, appending any remaining n>1."}
{"id": "lesson-ecea4c4a95af8112af14589d", "text": "Use snprintf for escaping non-printable characters efficiently."}
{"id": "lesson-ed1632903bebd17c1a3899fe", "text": "Track last_seen[char]=index in a dict and jump the window start past a repeat to find the longest unique substring in O(n)."}
{"id": "lesson-ee1f234ff9755523aa1e8cb1", "text": "Use string concatenation with modulo and integer division to build binary representations from right to left."}
{"id": "lesson-ee649f92a6173e807df60cce", "text": "Use `zip(*matrix)` to transpose a 2D list by unpacking rows into columns."}
{"id": "lesson-eeb48942dfdbc30d46d591c6", "text": "Use Write-Output to print exact strings without additional formatting or processing."}
{"id": "lesson-ef189f1e59ecc1973ffbb6d4", "text": "Use for loop with Math.Floor for efficient prime number checking."}
{"id": "lesson-ef4f9bd404cf3b155bc1aa55", "text": "Capture and validate retail packets for integrity and security."}
{"id": "lesson-ef7faeb391a369551e4ff845", "text": "Treat exactly-once delivery as a myth over an unreliable network: build at-least-once delivery plus idempotent handlers to get exactly-once effects, not exactly-once transport."}
{"id": "lesson-ef923605cba9dd9a7c01cb73", "text": "Use `pygame.font.Font` for rendering text in games."}
{"id": "lesson-ef94b376f14d0c3511ba94ba", "text": "Review test cases to ensure correctness of logic not just output."}
{"id": "lesson-efb59d52fe78ce45abe2553e", "text": "Use `sizeof` to determine array length for loops."}
{"id": "lesson-efdea10820e1da5e3bf4ca22", "text": "Use a foreach loop to iterate through array elements and sum them."}
{"id": "lesson-efe5a0d5cff72fd76d985317", "text": "Use ALTER TABLE to add columns without dropping existing table."}
{"id": "lesson-f0067ce8d82ed9a55a7b0f3e", "text": "Add columns to large tables safely: add nullable first, backfill in small batched transactions, then apply NOT NULL as a separate later migration to avoid a long lock."}
{"id": "lesson-f07c62091b8d067da826f45a", "text": "Use recursive dictionary traversal with path concatenation to flatten nested structures into dotted-key mappings."}
{"id": "lesson-f0cc2ad1942ea1bae005c036", "text": "Use `random.randint()` for generating inclusive random integers within a specified range."}
{"id": "lesson-f0f17ef8a3f29aa3fa4e3427", "text": "Use collections.deque for efficient O(1) appends and pops when implementing BFS-based topological sort."}
{"id": "lesson-f174d5e6d4d9efbcc1b97c21", "text": "Add a foreign key to an existing large table with NOT VALID then VALIDATE CONSTRAINT so the initial add takes only a brief lock and validation runs without blocking writes."}
{"id": "lesson-f1d58e74582c56f8dfdb5ded", "text": "Build decimal.Decimal amounts from strings, never from float literals; Decimal(0.1) inherits binary imprecision but Decimal(\"0.1\") is exact."}
{"id": "lesson-f1d6eae6af4ea80085d6d244", "text": "Pick ThreadPool for IO-bound work (sleep/network releases the GIL) and Pool (processes) for CPU-bound work, since the GIL blocks true multi-core speedup in threads."}
{"id": "lesson-f23adb59d07d5da2c5d19dad", "text": "Use `std::isfinite` for vector validation to prevent NaN and overflow issues."}
{"id": "lesson-f2846ec97cd3a1cafaf94cc9", "text": "Use `str(abs(n))` to convert a number to its digit characters, then `int(d)` to convert each back for summation."}
{"id": "lesson-f2967a2995824d5c094569e1", "text": "Centralize object construction behind a factory method when the concrete type depends on runtime config, so callers depend on an interface, not a constructor name."}
{"id": "lesson-f2c0d34c1ae7cc51de5c6222", "text": "Guard recursive quicksort with a depth_limit (~2*log2(n)) that falls back to heapsort (introsort) once exceeded; without it, a last-element pivot on already-sorted input causes O(n) recursion depth and can raise RecursionError, not just run slow."}
{"id": "lesson-f32d2afb70f7899d65f60d59", "text": "In 0/1 knapsack's 1D dp array, loop capacity from high to low per item so each item is used at most once; looping low-to-high silently turns it into unbounded knapsack."}
{"id": "lesson-f3322908f0eaeb8ce6025065", "text": "Convert a string to a list and repeatedly call .pop() to reverse it via a stack instead of slicing."}
{"id": "lesson-f3678e2a4f07b4c45e296ff1", "text": "Define __eq__ comparing a tuple of fields and __hash__ returning hash of that same tuple so instances that are == also collapse correctly in sets and dict keys."}
{"id": "lesson-f36eaf9d485da37780a17872", "text": "Use functools.partial(func, *fixed_args, **fixed_kwargs) to freeze some parameters and get a specialized callable without a lambda or def."}
{"id": "lesson-f384cf2dc0d23126c82f9de5", "text": "In a patch-archive chain, search added-later archives first (back-to-front) so overrides win without special-casing lookup order."}
{"id": "lesson-f3f472c3bdb3ca0b8a3512ba", "text": "In a Generic[K, V] class with forward and reverse dicts, delete the stale reverse entry before overwriting a key or the old value leaks a phantom mapping."}
{"id": "lesson-f43fe5f8ac36279a172f8b8a", "text": "Reverse strings using manual swapping for O(n) complexity."}
{"id": "lesson-f4470faeaed2ffee83642e97", "text": "functools.lru_cache needs hashable args (tuple, not list) or it raises TypeError; with maxsize set, re-requesting an evicted key is a fresh miss, not a hit, so check cache_info() before assuming reuse."}
{"id": "lesson-f44bd158118a0651cfbd803c", "text": "Use a for loop with index-based iteration to process array elements sequentially."}
{"id": "lesson-f496a058a8b39871137aed94", "text": "In subset backtracking, append path[:] (a copy) to results before recursing, since the shared list mutates via later pop()."}
{"id": "lesson-f4a3e5e964597ce10e98955e", "text": "Use std::clamp for safe clamping and calculate SmoothStep efficiently using ternary."}
{"id": "lesson-f4d1b2246236f8a00472b463", "text": "Use `std::map` for counting and `std::max_element` to find the most frequent word."}
{"id": "lesson-f4dffd80680c1233506357a6", "text": "Ensure `range` includes end value by adding 1."}
{"id": "lesson-f553270400627c9bb3bf092e", "text": "Extract a varying algorithm into a one-method interface and inject the implementation, so new behaviors mean a new class, not an edited switch statement."}
{"id": "lesson-f5c9096d770171da16653b80", "text": "Use set() to get unique words then count occurrences with list.count() method."}
{"id": "lesson-f5ef57784047eaf301559052", "text": "Check `errorBlob` for null before accessing its methods to avoid crashes."}
{"id": "lesson-f62308a22b17f4e6a6a4fb35", "text": "Use `dict.copy()` to avoid modifying the original dictionary when creating a new one with default values."}
{"id": "lesson-f62d309d4390c34cf713a281", "text": "Use `dict.copy()` to avoid modifying input dictionaries."}
{"id": "lesson-f646327f0efc70afc46d94bf", "text": "Use a set to track seen elements for O(1) duplicate detection in linear time."}
{"id": "lesson-f6af4779766f65d7e332f12b", "text": "Use std::stack for bracket matching in balanced parentheses problems."}
{"id": "lesson-f6f7a77c0caa0b7605e2f4f9", "text": "Use a for-each loop to iterate through an array and accumulate values into a sum variable."}
{"id": "lesson-f7501dee7104098dfec664db", "text": "Use `Write-Host` for quick console output in PowerShell scripts."}
{"id": "lesson-f77248da2c2412e26cd63c5a", "text": "For a tool meant to drop next to another app's exe with no installer, statically link the compiler runtime so it needs zero extra DLLs on a bare machine."}
{"id": "lesson-f7981bb5a6d83f5b7dbbc546", "text": "Recover a commit lost to a bad reset or rebase via git reflog: HEAD@{n} still names the dangling commit until gc eventually prunes it."}
{"id": "lesson-f7dd244262c79bb33aaa6484", "text": "Run EXPLAIN ANALYZE, not just EXPLAIN: compare estimated vs actual row counts — a large gap means stale statistics driving a bad plan."}
{"id": "lesson-f82c5c948a3487aabfdd83a9", "text": "Patch time.time or os.environ via monkeypatch.setattr/setenv inside a test; pytest auto-reverts them at teardown so tests never leak global state."}
{"id": "lesson-f8600761f9b63b5d7c11a8dc", "text": "Discard the first N iterations of any microbenchmark as warmup: JIT compilation, branch predictors, and cold caches inflate early timings and skew the average."}
{"id": "lesson-f865f5caf47ae4526aebd88a", "text": "Group anagrams with a defaultdict(list) keyed by ''.join(sorted(word)) so every permutation of the same letters collapses to one bucket in O(n log k) per word."}
{"id": "lesson-f8e1a4619d14dee35da541b4", "text": "itertools.groupby only groups consecutive runs; sort by the key first, or use dict.setdefault(key, []).append(x) to group unsorted data correctly."}
{"id": "lesson-f912e3de7e1bf2b84c3aa756", "text": "Run Johnson's algorithm: add a zero-weight virtual source, get potentials h[] via Bellman-Ford, reweight edges w+h[u]-h[v] to non-negative, then Dijkstra per node and un-reweight results."}
{"id": "lesson-f925df0aceffe62fb1fa24e6", "text": "Iterate 'for line in f' to stream a huge text file line-by-line instead of f.read() or f.readlines(), since the loop never materializes the whole file in memory."}
{"id": "lesson-f96907c3e569638091d87e53", "text": "Schema-migrate an existing SQLite DB by adding nullable columns idempotently (check PRAGMA table_info first) so old rows default to NULL and legacy code paths keep working unchanged."}
{"id": "lesson-f9a8898fc4bfeac3bc7cc6dc", "text": "Use immutable validation functions to ensure state immutability and prevent side effects."}
{"id": "lesson-f9ced00ee7bf93f836b97793", "text": "Use try/except blocks to handle type conversion errors gracefully and return default values."}
{"id": "lesson-fa3e908438636cf7b70d0df5", "text": "Dedupe a sequence while preserving order with a seen=set() plus an output list, checking membership in the set not the list."}
{"id": "lesson-fa5fda6a4b20b497722644ad", "text": "Use `csv.DictReader` for CSV parsing to handle headers and rows efficiently without loading everything into memory."}
{"id": "lesson-fa92db01fadf857e2a46cc30", "text": "Degrade gracefully: serve a cached, stale, or default response when a non-critical dependency times out instead of failing the entire request for one slow feature."}
{"id": "lesson-fab1f9d26947069c3fe76bf3", "text": "Use a hash map to store previously seen values and their indices for O(1) lookup when solving two-sum problems."}
{"id": "lesson-face4dec46fd8499188f26b2", "text": "Track and propagate request cleanup authority consistently across all paths."}
{"id": "lesson-fafe77e6a9d65f1de7afc8b8", "text": "Use `iter()` and `all()` with generator expressions for concise subsequence checking."}
{"id": "lesson-fb02541602ef79d799261251", "text": "Adding timedelta to a zoneinfo-aware datetime does wall-clock arithmetic on naive fields, not real-elapsed-time; convert to UTC, add there, then astimezone() back across a DST transition."}
{"id": "lesson-fb43b91e978761498e72e2c0", "text": "Clamp a value in one line with max(lo, min(x, hi)) instead of writing nested if/elif range checks."}
{"id": "lesson-fb77d618d78fed7c244a21f8", "text": "Modify `std::expected` APIs to handle allocation failures gracefully."}
{"id": "lesson-fb8226a4b848109e45dc09c4", "text": "When crossing over two tree genomes of different arity, only graft donor subtrees whose variable references are valid in the receiver, or the offspring reads out of bounds."}
{"id": "lesson-fb85d1eb2d6df4c9bfd1c3f8", "text": "Use `re.search` to find matches efficiently and sum them with a generator expression."}
{"id": "lesson-fbcffcb0b09ad052dba68fa0", "text": "Use string indexing and concatenation for reversing strings in PowerShell."}
{"id": "lesson-fd53d025d5e27dfc9793470d", "text": "In cross-version wire serializers, distinguish fields present only in the canonical model (omit from wire) from fields present only on the wire (need default synthesis) — conflating the two corrupts the payload."}
{"id": "lesson-fd56d9e30111fee031994783", "text": "Use regular expressions with capture groups and backreferences to insert underscores between letter boundaries in camelCase strings."}
{"id": "lesson-fd715816c325663358b5bd9a", "text": "Use strptime with an explicit format string containing %z to parse offsets like '-0500' directly into an aware datetime, and strftime with the same format to round-trip."}
{"id": "lesson-fdd0bd31a8e99f3348337074", "text": "Use vector<pair<int,string>> for efficient mapping and iteration in C++ Roman numeral conversion."}
{"id": "lesson-fe03b514c30d2c8f8aeda5b1", "text": "Ensure `if` conditions are inclusive for edge cases."}
{"id": "lesson-fe4ccaf66abbbac9712f1238", "text": "Use `Console.WriteLine()` to output computed results directly without intermediate string formatting."}
{"id": "lesson-fea6b85f20c8d629fa5dcdd0", "text": "Mark only GET, HEAD, PUT, DELETE as idempotent and safe to blind-retry; never auto-retry a bare POST without a client-generated idempotency key"}
{"id": "lesson-fed57dc3c660506cb7f03ae4", "text": "When merging k sorted lists manually, push tuples (value, source_index, elem_index) not just value, so equal-priority ties break on source_index before ever comparing non-orderable payloads and raising TypeError."}
{"id": "lesson-feda7ec362bbece4dbd6f6cc", "text": "Order composite-index columns equality-filter first, range-filter last, so the engine can seek on equality then scan the range via the leftmost-prefix rule."}
{"id": "lesson-ff5913c9081823a28653ade7", "text": "Use struct.pack/unpack with an explicit byte-order prefix ('<' or '>') and calcsize() so fixed binary record layouts stay deterministic across platforms."}
{"id": "lesson-ff9fb9c03928cf39f008f3ab", "text": "Recognize pattern overkill: a Strategy, Factory, or Observer with exactly one production implementation and no test-double need is indirection with zero payoff; inline it until a second variant appears."}
{"id": "lesson-ffbece86e380e5b5b0fb58f1", "text": "Trace a TLS handshake failure by checking ClientHello cipher/SNI, ServerHello certificate chain, and clock skew before assuming the certificate itself is invalid"}
{"id": "lesson-ffe5a789d625d78d9c5d95cb", "text": "Use `@{}` for hash tables to store key-value pairs efficiently."}
{"id": "lesson-fffada3dd90627762fbda789", "text": "Check divisibility up to square root for primality testing."}
