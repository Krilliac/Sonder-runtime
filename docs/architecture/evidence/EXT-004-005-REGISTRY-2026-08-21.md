# EXT-004/005 extension registry evidence

Date: 2026-08-21

Added `sonder_runtime/application/extensions/registry.py`, a bounded registry
for project/global installation records, with SQLite-backed durable state in
`sonder_runtime/adapters/persistence/sqlite/extensions.py`. It supports
provenance health evaluation, quarantine and crash diagnostics, explicit
disable/enable state, update replacement, deterministic snapshots, and repair
diagnostics without performing filesystem, network, or process mutation. The
live bootstrap graph exposes the durable registry through the typed HTTP facade
and machine-readable CLI command, with explicit operator authority required.

Verification: `python -m pytest -q tests/test_extension_registry.py tests/test_extension_host.py tests/test_extension_experiments.py tests/test_selfmod_governance_reproducer.py tests/production/test_extension_composition.py --basetemp C:\\Users\\Nathan\\Documents\\Codex\\pytest-extension-registry-final` — **26 passed**; the production test covers HTTP health/disable, CLI health, and fresh-graph persistence. Compileall, architecture, evidence, documentation, and diff checks pass.

The formal EXT-004/005 rows remain unverified because this branch has no
validated Flutter/UI registry surface and does not implement artifact discovery,
download, or installation mutation. The CLI/HTTP and durable-state slice is
implemented and directly tested; UI and artifact lifecycle remain outside this
evidence.
