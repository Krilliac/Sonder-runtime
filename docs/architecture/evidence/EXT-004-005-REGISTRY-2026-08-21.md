# EXT-004/005 extension registry evidence

Date: 2026-08-21

Added `sonder_runtime/application/extensions/registry.py`, a bounded,
side-effect-free registry for project/global installation records. It supports
provenance health evaluation, quarantine and crash diagnostics, explicit
disable/enable state, update replacement, deterministic snapshots, and repair
diagnostics without performing filesystem, network, or process mutation.

Verification: `python -m pytest -q tests/test_extension_registry.py tests/test_extension_host.py tests/test_extension_experiments.py tests/test_selfmod_governance_reproducer.py --basetemp .pytest-ext-registry-final` — **20 passed**; compileall, architecture, evidence, documentation, and diff checks pass.

The formal EXT-004/005 rows remain unverified because CLI/API/UI exposure and
durable installation storage are not yet wired.
