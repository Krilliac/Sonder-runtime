# Sonder Runtime project skill library

Portable project skills authored by the Fable-5 skill forge at commit
99162cf9 (2026-08-22/23). Sixteen skills, one routing line each — open the
skill whose line matches the question.

| Skill | Route here for |
|---|---|
| `sonder-architecture-contract` | Why the rules exist: authority hierarchy, ADRs, `check_architecture` invariants |
| `sonder-server-decomposition-campaign` | The executable WP1 slice loop for decomposing `server.py` |
| `sonder-change-control` | Change tiers, CI gate order, ratchets, the never-commit list, git conventions |
| `sonder-build-and-env` | Venv creation, requirements files, pytest gates, environment traps |
| `sonder-run-and-operate` | Launchers, subcommands, start/stop/drain lifecycle, state home, backups |
| `sonder-diagnostics-and-tooling` | doctor/preflight/status anatomy, logs, the launcher health proof, instruments |
| `sonder-config-and-flags` | The SONDER_* catalog, config precedence, runtime policy, consent gates |
| `sonder-debugging-playbook` | Symptom-first triage plus the discriminating experiment for each failure mode |
| `sonder-failure-archaeology` | Chronological incident history and what is still open |
| `sonder-selfmod-lifecycle` | Selfmod phases, gates, the protected-path policy, recovery |
| `sonder-security-and-privacy` | Boundaries never to weaken, and the security review checklist |
| `sonder-memory-and-training` | Lesson memory, calibrated retrieval, outcome provenance, QLoRA training |
| `sonder-agents-and-fleets` | Autopilot, fleets, fanout, cancellation semantics, the worker pool |
| `sonder-validation-and-qa` | Evidence standards, the test-harness contract, verifiers, evals |
| `sonder-docs-and-writing` | Where each fact lives, doc templates, doc gates, house style |
| `sonder-external-positioning` | Public claims, per-claim proof obligations, release gates |
