# Sonder currently imports the MCP Python SDK 1.x FastMCP and ToolManager APIs.
# MCP 2.x removed mcp.server.fastmcp, so keep the breaking major out until the
# runtime is deliberately migrated and tested against its replacement API.
mcp>=1.28.1,<2
cryptography>=42,<46
