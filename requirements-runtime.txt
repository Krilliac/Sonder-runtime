# Bootstrap is unattended, so runtime dependencies must be the exact releases
# exercised by CI. Bump these deliberately with the FastMCP compatibility
# probe and the runtime test suite; do not turn this into a floating range.
# MCP 2.x removed mcp.server.fastmcp, which Sonder currently imports.
mcp==1.29.0
cryptography==50.0.0
# pydantic-settings 2.15 warns when MCP 1.x resolves FastMCP's generic
# lifespan setting, although the runtime remains usable. Pin the last
# warning-free release until the upstream forward reference is resolved.
pydantic-settings==2.14.0
