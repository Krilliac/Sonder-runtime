"""Platform layer (SPEC-3): cross-cutting infrastructure.

Until SPEC-3's later phases physically move the implementations, these
modules re-export the SPEC-2 root modules so package callers already use
the target import paths (``sonder_runtime.platform.config`` etc.).
"""
