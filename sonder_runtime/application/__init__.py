"""Application layer (SPEC-3): use cases, ports, and the operation context.

Application modules import only the domain layer, ports, and the standard
library — never adapters, platform, or bootstrap.
"""
