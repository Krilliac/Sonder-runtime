{"lesson": "Give every recursive function a base case checked before the recursive call, e.g. `if n == 0: return 1`, or it never terminates.", "difficulty": "basic", "task": "factorial", "grounded": true, "source": "seed:curriculum:recursion_dp:basic"}
{"lesson": "Wrap a naive recursive Fibonacci with @functools.lru_cache(maxsize=None) to turn exponential calls into linear memoized ones.", "difficulty": "basic", "task": "nth_fibonacci", "grounded": true, "source": "seed:curriculum:recursion_dp:basic"}
{"lesson": "Replace recursive Fibonacci with a bottom-up loop using tuple reassignment `a, b = b, a + b` for O(1) space, no recursion limit risk.", "difficulty": "intermediate", "task": "nth_fibonacci_iter", "grounded": true, "source": "seed:curriculum:recursion_dp:intermediate"}
{"lesson": "Kadane's algorithm: at each step set cur=max(x, cur+x) then best=max(best,cur) to find max subarray sum in one linear pass.", "difficulty": "intermediate", "task": "max_subarray_sum", "grounded": true, "source": "seed:curriculum:recursion_dp:intermediate"}
{"lesson": "Build a (n+1)x(m+1) DP grid where dp[i][j]=dp[i-1][j-1]+1 on a char match else max(dp[i-1][j],dp[i][j-1]) for LCS length.", "difficulty": "advanced", "task": "lcs_length", "grounded": true, "source": "seed:curriculum:recursion_dp:advanced"}
{"lesson": "Seed dp[i][0]=i and dp[0][j]=j as base rows/cols, then take 1+min(delete,insert,substitute) for edit distance.", "difficulty": "advanced", "task": "levenshtein_distance", "grounded": true, "source": "seed:curriculum:recursion_dp:advanced"}
{"lesson": "Init the first row/col of the grid DP to 1 (only one path along an edge), then dp[i][j]=dp[i-1][j]+dp[i][j-1] for path counts.", "difficulty": "advanced", "task": "unique_paths", "grounded": true, "source": "seed:curriculum:recursion_dp:advanced"}
