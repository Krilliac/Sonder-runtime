{"lesson": "Split \"key=value;...\" with str.partition('=') per segment, not str.split('='), so values may contain extra '=' signs.", "difficulty": "basic", "task": "parse_kv", "grounded": true, "source": "seed:curriculum:parsing_validation:basic"}
{"lesson": "Wrap int(str(s).strip()) in try/except (TypeError, ValueError) to return a caller-supplied default on bad input.", "difficulty": "basic", "task": "safe_int", "grounded": true, "source": "seed:curriculum:parsing_validation:basic"}
{"lesson": "Use regex alternation with the float branch first, r'-?\\d+\\.\\d+|-?\\d+', so findall grabs whole decimals instead of splitting on the dot.", "difficulty": "basic", "task": "extract_numbers", "grounded": true, "source": "seed:curriculum:parsing_validation:basic"}
{"lesson": "Build JSONL with json.dumps(record, sort_keys=True) per line and '\\n'.join, so key order never breaks byte-for-byte comparisons downstream.", "difficulty": "intermediate", "task": "to_jsonl", "grounded": true, "source": "seed:curriculum:parsing_validation:intermediate"}
{"lesson": "Validate IPv4 octets with str.isdigit() plus an explicit leading-zero check (len>1 and p[0]=='0'), since int('0.0.0.0'.split('.')) alone accepts '01' as 1.", "difficulty": "intermediate", "task": "is_valid_ipv4", "grounded": true, "source": "seed:curriculum:parsing_validation:intermediate"}
{"lesson": "Implement a no-eval arithmetic parser as three mutually recursive functions (expr/term/factor) so */ binds tighter than +- without a precedence table.", "difficulty": "advanced", "task": "eval_expr", "grounded": true, "source": "seed:curriculum:parsing_validation:advanced"}
{"lesson": "Flatten nested dict/list JSON by recursing with a growing prefix key ('a.b' for dicts, 'a[0]' for lists) and special-casing empty dict/list as leaf values.", "difficulty": "advanced", "task": "json_flatten", "grounded": true, "source": "seed:curriculum:parsing_validation:advanced"}
