{"lesson": "Group anagrams with a defaultdict(list) keyed by ''.join(sorted(word)) so every permutation of the same letters collapses to one bucket in O(n log k) per word.", "difficulty": "basic", "task": "anagram grouping via sorted-key", "grounded": true, "source": "seed:grounded:string_algorithms:basic"}
{"lesson": "Build a trie from nested dict nodes with an explicit is_end flag, not just leaf-presence, so 'car' inserted doesn't falsely make search('ca') true.", "difficulty": "basic", "task": "trie insert/search/startsWith", "grounded": true, "source": "seed:grounded:string_algorithms:basic"}
{"lesson": "Compute KMP's prefix table with the fallback loop 'while k>0 and pattern[i]!=pattern[k]: k=pi[k-1]' so the search loop reuses it for O(n+m) matching with zero backtracking in text.", "difficulty": "intermediate", "task": "KMP prefix-function pattern search", "grounded": true, "source": "seed:grounded:string_algorithms:intermediate"}
{"lesson": "In Rabin-Karp, always re-verify a rolling-hash hit with a direct text[i:i+m]==pattern slice comparison before accepting the match, since equal hashes under a fixed modulus can still collide.", "difficulty": "intermediate", "task": "Rabin-Karp rolling hash search", "grounded": true, "source": "seed:grounded:string_algorithms:intermediate"}
{"lesson": "To find all occurrences of a pattern in text with the Z-algorithm, concatenate pattern + sentinel + text (sentinel absent from both) and scan for z[i] >= len(pattern), avoiding false matches that could span the join.", "difficulty": "advanced", "task": "Z-algorithm pattern search via concatenation", "grounded": true, "source": "seed:grounded:string_algorithms:advanced"}
{"lesson": "For longest palindromic substring, expand around every one of the 2n-1 centers (n single chars plus n-1 adjacent pairs) so odd- and even-length palindromes are both covered in O(n^2) with O(1) extra space.", "difficulty": "advanced", "task": "longest palindromic substring via expand-around-center", "grounded": true, "source": "seed:grounded:string_algorithms:advanced"}
{"lesson": "Manacher's algorithm needs the string transformed with interleaved sentinels ('^#a#b#$') so every palindrome becomes odd-length, letting one radius array p[i] and a mirror-reuse of p[2*center-i] give true O(n) time.", "difficulty": "expert", "task": "Manacher's O(n) longest palindromic substring", "grounded": true, "source": "seed:grounded:string_algorithms:expert"}
{"lesson": "The KMP failure function doubles as a periodicity detector: n - pi[n-1] gives the smallest additive period, and n % period == 0 tells you if the string is an exact whole-number repetition of that period.", "difficulty": "expert", "task": "KMP failure-function period detection and border enumeration", "grounded": true, "source": "seed:grounded:string_algorithms:expert"}
