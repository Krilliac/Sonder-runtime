{"lesson": "Replace near-duplicate test functions with @pytest.mark.parametrize over a list of (input, expected) tuples for one table-driven test.", "difficulty": "basic", "task": "parametrize palindrome checks", "grounded": true, "source": "seed:grounded:testing_and_tdd:basic"}
{"lesson": "Structure each test as Arrange (build state), Act (one call), Assert (check outcomes only) so failures show a single cause.", "difficulty": "basic", "task": "AAA-structured Stack test", "grounded": true, "source": "seed:grounded:testing_and_tdd:basic"}
{"lesson": "Give a pytest yield-fixture scope='module' to share one setup/teardown across every test in the file instead of re-running it per test.", "difficulty": "intermediate", "task": "module-scoped db_connection fixture", "grounded": true, "source": "seed:grounded:testing_and_tdd:intermediate"}
{"lesson": "Patch time.time or os.environ via monkeypatch.setattr/setenv inside a test; pytest auto-reverts them at teardown so tests never leak global state.", "difficulty": "intermediate", "task": "monkeypatch time.time and env var", "grounded": true, "source": "seed:grounded:testing_and_tdd:intermediate"}
{"lesson": "Set Mock.side_effect to a list mixing exceptions and return values to script retry/error-path behavior across successive calls in one test.", "difficulty": "advanced", "task": "mock retry-then-succeed and exhaust-retries", "grounded": true, "source": "seed:grounded:testing_and_tdd:advanced"}
{"lesson": "Fuzz a function against a trusted oracle (e.g. sorted()) over many random.Random(seed)-generated inputs, asserting invariants instead of one fixed example.", "difficulty": "advanced", "task": "property-fuzz merge_sort vs sorted()", "grounded": true, "source": "seed:grounded:testing_and_tdd:advanced"}
{"lesson": "Run red-green-refactor as three real pytest passes: an incomplete implementation must fail before pytest.raises(...) passes, then keep tests green after restructuring internals.", "difficulty": "expert", "task": "TDD cycle for parse_kv error path", "grounded": true, "source": "seed:grounded:testing_and_tdd:expert"}
{"lesson": "Never monkeypatch.setattr(datetime.datetime, 'now', ...): it's an immutable C type. Instead subclass datetime and monkeypatch the module's imported name to the subclass.", "difficulty": "expert", "task": "patch datetime.now via subclass at import site", "grounded": true, "source": "seed:grounded:testing_and_tdd:expert"}
