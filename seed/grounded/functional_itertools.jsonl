{"lesson": "Compose map() then filter() with a helper that returns None on failure (not raise) so filter can drop bad-parse sentinels in one pass.", "difficulty": "basic", "task": "map/filter parse-and-filter strings to positive ints", "grounded": true, "source": "seed:grounded:functional_itertools:basic"}
{"lesson": "Use functools.reduce with an explicit initial value ([] or 1) to fold nested lists or compute a product, avoiding IndexError on empty input.", "difficulty": "basic", "task": "functools.reduce flatten-then-multiply", "grounded": true, "source": "seed:grounded:functional_itertools:basic"}
{"lesson": "Always sorted(data, key=fn) immediately before itertools.groupby(data, key=fn); groupby only merges equal-key items that are already contiguous, silently splitting/overwriting otherwise.", "difficulty": "intermediate", "task": "itertools.groupby unsorted-input pitfall", "grounded": true, "source": "seed:grounded:functional_itertools:intermediate"}
{"lesson": "Use itertools.product(range(...), repeat=n) plus a sum filter to brute-force bounded combination search instead of nested for-loops.", "difficulty": "intermediate", "task": "itertools.product coin-combination search", "grounded": true, "source": "seed:grounded:functional_itertools:intermediate"}
{"lesson": "Pass func=min (or max) and initial= to itertools.accumulate to get a running extreme (e.g. lowest balance reached), not just a running sum.", "difficulty": "advanced", "task": "itertools.accumulate running-min balance tracker", "grounded": true, "source": "seed:grounded:functional_itertools:advanced"}
{"lesson": "Use functools.cmp_to_key to plug a pairwise comparator (e.g. fuzzy-tie score bands with alphabetical tiebreak) into sorted() when no single key tuple can express the ordering.", "difficulty": "advanced", "task": "functools.cmp_to_key fuzzy-tie leaderboard sort", "grounded": true, "source": "seed:grounded:functional_itertools:advanced"}
{"lesson": "itertools.chain.from_iterable over an unbounded generator plus itertools.islice pulls only N items lazily; islice-ing the same chain object again resumes mid-stream instead of restarting.", "difficulty": "expert", "task": "lazy paginated stream flatten with chain+islice", "grounded": true, "source": "seed:grounded:functional_itertools:expert"}
{"lesson": "functools.lru_cache needs hashable args (tuple, not list) or it raises TypeError; with maxsize set, re-requesting an evicted key is a fresh miss, not a hit, so check cache_info() before assuming reuse.", "difficulty": "expert", "task": "lru_cache unhashable-args and LRU-eviction pitfall", "grounded": true, "source": "seed:grounded:functional_itertools:expert"}
