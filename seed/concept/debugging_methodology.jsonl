{"lesson": "Shrink a failing case by deleting or simplifying inputs/code while the bug still reproduces, until nothing more can be removed.", "difficulty": "basic", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Read a traceback from the bottom exception upward, not top-down — the root cause and failing line sit at the bottom, imports and dispatch fill the top.", "difficulty": "basic", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Form exactly one falsifiable hypothesis before adding a print or breakpoint, then design the check so only that hypothesis survives or dies — don't scattergun edits.", "difficulty": "intermediate", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Bisect a regression with git bisect run <script> so a scripted pass/fail check decides each step automatically instead of eyeballing diffs.", "difficulty": "intermediate", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Drop into pdb.set_trace()/breakpoint() at the failure site and inspect live locals with p/pp instead of guessing values by re-reading source.", "difficulty": "intermediate", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Tag disciplined print-debugging output with a unique marker string and the enclosing function name so grep can isolate one execution path from interleaved log noise.", "difficulty": "intermediate", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Bisect corrupted or drifting data (not just code) by binary-searching the dataset/config itself — comment out half the migrations, rows, or feature flags per step — when the regression isn't in a commit.", "difficulty": "advanced", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Pin a heisenbug for reproduction: fix the random seed, force single-threaded execution, and record/replay the exact input sequence — most 'random' failures are actually timing- or order-dependent.", "difficulty": "advanced", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
{"lesson": "Use conditional and watchpoint breakpoints (break if x==N, watch on a memory address/variable) to stop only at the Nth iteration or the exact write that corrupts state, instead of stepping through thousands of benign iterations.", "difficulty": "expert", "task": "debugging_methodology", "grounded": false, "source": "seed:concept:debugging_methodology"}
