{"lesson": "Assign each request a client-generated idempotency key and cache its result server-side, so a retried request returns the stored outcome instead of executing twice.", "difficulty": "basic", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Treat exactly-once delivery as a myth over an unreliable network: build at-least-once delivery plus idempotent handlers to get exactly-once effects, not exactly-once transport.", "difficulty": "basic", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Pair exponential backoff with a retry budget (token bucket per client) so retries drain gracefully instead of amplifying a downstream outage into a bigger overload spike.", "difficulty": "intermediate", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Use Lamport timestamps to derive a happened-before partial order across nodes without synchronized wall clocks; break ties with node ID for a total order.", "difficulty": "intermediate", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Use vector clocks, one counter per replica, to detect concurrent conflicting writes that wall-clock timestamps would falsely order or silently overwrite.", "difficulty": "advanced", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "On a timeout, treat the outcome as unknown, not failed: the request may have completed after the response was lost, so retries must be idempotent, never assume-and-resend blindly.", "difficulty": "advanced", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Enforce read/write quorums with R + W > N so every read set overlaps at least one up-to-date replica, trading some availability for consistency without full consensus.", "difficulty": "advanced", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Replicate a log via majority-quorum consensus like Raft or Paxos so committed entries survive any minority-node failure and split-brain dual-leader writes are structurally impossible.", "difficulty": "expert", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
{"lesson": "Inject real faults, dropped packets, added latency, reordered messages, in tests to expose code that silently assumes the fallacies: reliable network, zero latency, infinite bandwidth.", "difficulty": "expert", "task": "distributed_systems", "grounded": false, "source": "seed:concept:distributed_systems"}
