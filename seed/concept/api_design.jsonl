{"lesson": "Version APIs via the URL path or an Accept header, never a bare query parameter — proxies and CDNs cache and strip query params, silently breaking version routing.", "difficulty": "basic", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Use cursor-based pagination (opaque token of last sort key + id) instead of offset/limit — offsets skip or duplicate rows when items are inserted or deleted mid-scroll.", "difficulty": "basic", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Require an Idempotency-Key header on POST-create endpoints; store (key, request hash, response) so a retried request replays the cached response instead of double-creating the resource.", "difficulty": "intermediate", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Standardize every error response on one envelope, e.g. RFC 7807 (type, title, status, detail, instance), so clients write one error handler instead of one per route.", "difficulty": "intermediate", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Model non-CRUD operations as RPC-style action endpoints (POST /orders/{id}/cancel) instead of forcing them into PATCH — ambiguous partial-update semantics invite silent state corruption.", "difficulty": "intermediate", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Encode pagination cursors as opaque signed tokens of (sort_key, tiebreaker_id), not raw integer offsets — this survives concurrent writes and stops clients forging arbitrary offsets.", "difficulty": "advanced", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Treat every released response field as a permanent contract: add new fields as optional, and never remove or repurpose one without a Sunset header and a full deprecation window.", "difficulty": "advanced", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Enforce idempotency-key uniqueness with a database unique constraint on (client_id, key), not just a cache lookup — otherwise a check-then-insert race lets duplicate requests both succeed.", "difficulty": "advanced", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
{"lesson": "Rate-limit with a token bucket sharded per (API key, endpoint), returning Retry-After and X-RateLimit-Remaining headers, so one noisy client can't starve others on shared infrastructure.", "difficulty": "expert", "task": "api_design", "grounded": false, "source": "seed:concept:api_design"}
