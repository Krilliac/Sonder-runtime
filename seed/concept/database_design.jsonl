{"lesson": "Order composite-index columns equality-filter first, range-filter last, so the engine can seek on equality then scan the range via the leftmost-prefix rule.", "difficulty": "intermediate", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Denormalize only after measuring join cost with EXPLAIN; when you duplicate a column, add a trigger or dual-write path so the copy can't silently drift.", "difficulty": "advanced", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Catch the N+1 problem by asserting query counts in integration tests, then fix it with a JOIN or a single batched IN-list fetch instead of one query per row.", "difficulty": "basic", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "READ COMMITTED permits non-repeatable reads across a transaction; use SELECT ... FOR UPDATE or SERIALIZABLE isolation to stop lost updates in read-modify-write cycles.", "difficulty": "advanced", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Add columns to large tables safely: add nullable first, backfill in small batched transactions, then apply NOT NULL as a separate later migration to avoid a long lock.", "difficulty": "advanced", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Add a foreign key to an existing large table with NOT VALID then VALIDATE CONSTRAINT so the initial add takes only a brief lock and validation runs without blocking writes.", "difficulty": "expert", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Read the query plan for a seq scan on a filtered large table before adding an index; every new index adds write-amplification on INSERT/UPDATE/DELETE.", "difficulty": "intermediate", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Keep transactions short: never hold a transaction open across a network call or external I/O, since held row and table locks block every competing writer.", "difficulty": "basic", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
{"lesson": "Acquire locks and rows in a fixed, consistent order across every code path so the database's deadlock detector rarely fires; retry the killed transaction rather than hand-serializing call sites.", "difficulty": "expert", "task": "database_design", "grounded": false, "source": "seed:concept:database_design"}
