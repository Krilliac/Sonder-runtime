{"lesson": "Never SELECT * in production queries: fetch only needed columns so a covering index can satisfy the query without a heap/table lookup.", "difficulty": "basic", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "An index on a low-cardinality column (e.g. boolean, status flag) can make the planner slower via random I/O than a sequential full scan.", "difficulty": "basic", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Run EXPLAIN ANALYZE, not just EXPLAIN: compare estimated vs actual row counts — a large gap means stale statistics driving a bad plan.", "difficulty": "intermediate", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Order composite index columns to match equality predicates first, then range/sort columns, so the index also satisfies ORDER BY without an extra sort step.", "difficulty": "intermediate", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Prefer hash joins for large unsorted equi-joins and nested-loop joins only when one side is tiny or already indexed; force a plan hint if the optimizer picks nested-loop on a big table.", "difficulty": "advanced", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Replace OFFSET-based pagination with keyset (seek) pagination: WHERE (sort_col, id) > (last_val, last_id) LIMIT n, so deep pages don't scan and discard skipped rows.", "difficulty": "advanced", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Replace a correlated subquery per row with a window function (SUM(...) OVER, ROW_NUMBER() OVER) so the engine computes it in one pass instead of once per outer row.", "difficulty": "advanced", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "Watch for implicit type coercion in a WHERE clause (e.g. comparing an indexed int column to a string literal) silently disabling index use and forcing a full scan.", "difficulty": "expert", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
{"lesson": "After bulk loads or large deletes, re-run ANALYZE (or equivalent) manually — autovacuum/auto-stats lag can leave the planner using outdated cardinality estimates for hours.", "difficulty": "expert", "task": "sql_optimization", "grounded": false, "source": "seed:concept:sql_optimization"}
