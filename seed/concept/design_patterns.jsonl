{"lesson": "Composition over inheritance: prefer a field holding a swappable object over subclassing, so behavior changes without touching a class hierarchy.", "difficulty": "basic", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Extract a varying algorithm into a one-method interface and inject the implementation, so new behaviors mean a new class, not an edited switch statement.", "difficulty": "basic", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Decouple publisher from subscribers with an observer/event-bus callback list, but explicitly unregister on teardown or dangling references leak memory.", "difficulty": "intermediate", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Centralize object construction behind a factory method when the concrete type depends on runtime config, so callers depend on an interface, not a constructor name.", "difficulty": "intermediate", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Wrap an incompatible third-party interface in a thin adapter implementing your own interface, so the rest of the codebase never imports the vendor type directly.", "difficulty": "intermediate", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Stack cross-cutting behavior with decorators wrapping the same interface, but note wrapping order changes semantics: caching-then-logging differs from logging-then-caching.", "difficulty": "advanced", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Apply dependency inversion: have high-level modules depend on abstractions they own, not ones defined by the low-level implementation, so it stays swappable in tests.", "difficulty": "advanced", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Recognize pattern overkill: a Strategy, Factory, or Observer with exactly one production implementation and no test-double need is indirection with zero payoff; inline it until a second variant appears.", "difficulty": "expert", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
{"lesson": "Pair dependency inversion with a single composition root: wire all concrete implementations in one bootstrap file so business logic never calls 'new ConcreteClass' directly.", "difficulty": "expert", "task": "design_patterns", "grounded": false, "source": "seed:concept:design_patterns"}
