{"lesson": "Choose UDP over TCP when occasional packet loss is cheaper than head-of-line-blocked retransmit delay, e.g. real-time voice or game state", "difficulty": "basic", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Mark only GET, HEAD, PUT, DELETE as idempotent and safe to blind-retry; never auto-retry a bare POST without a client-generated idempotency key", "difficulty": "basic", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Set separate connect, TLS-handshake, and read timeouts per request; a single blanket timeout masks whether DNS, the socket, or a slow server stalled", "difficulty": "intermediate", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Add full jitter to exponential backoff (delay = random(0, base*2^n)) rather than fixed exponential, so retrying clients desynchronize instead of hammering in lockstep", "difficulty": "intermediate", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Cap retry attempts with a deadline budget, not just a count, so retries inside a timed request never outlive the caller's own timeout", "difficulty": "intermediate", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Reuse keep-alive connections via a pooled HTTP client instead of opening one socket per request, avoiding repeated TCP handshake plus TLS negotiation cost", "difficulty": "intermediate", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Diagnose HTTP/1.1 head-of-line blocking by counting stalled requests behind one slow response on a shared connection, then fix with pipelining-safe HTTP/2 multiplexed streams", "difficulty": "advanced", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Trace a TLS handshake failure by checking ClientHello cipher/SNI, ServerHello certificate chain, and clock skew before assuming the certificate itself is invalid", "difficulty": "advanced", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
{"lesson": "Switch to QUIC over UDP when TCP head-of-line blocking across multiplexed streams dominates latency, since QUIC gives each stream independent loss recovery", "difficulty": "expert", "task": "networking", "grounded": false, "source": "seed:concept:networking"}
