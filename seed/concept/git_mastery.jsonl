{"lesson": "Prefer rebase to replay commits onto a new base for linear history; prefer merge to preserve true branch topology and both parents.", "difficulty": "basic", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Never force-push a shared branch: it rewrites commits other clones already built on, orphaning their work and breaking their reflog recovery.", "difficulty": "basic", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Run git rebase -i HEAD~n to squash, reorder, reword, or drop commits before pushing, keeping shared history clean without altering the remote.", "difficulty": "intermediate", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Use git cherry-pick -x <sha> to port a single fix across branches; the -x flag records the origin SHA in the message for later traceability.", "difficulty": "intermediate", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Install a pre-commit hook running lint and fast tests so bad commits never leave the machine, but keep it fast or developers bypass it with --no-verify.", "difficulty": "intermediate", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Resolve conflicts with git checkout --ours/--theirs <path> when one side should win wholesale, or hand-edit <<<<<<< markers when both sides changed meaningfully.", "difficulty": "advanced", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Bisect a regression with git bisect run <test-script> so each step is decided automatically by exit code instead of manual eyeballing.", "difficulty": "advanced", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Recover a commit lost to a bad reset or rebase via git reflog: HEAD@{n} still names the dangling commit until gc eventually prunes it.", "difficulty": "expert", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
{"lesson": "Use git rebase --onto <newbase> <oldbase> <branch> to transplant only a range of commits when an upstream dependency branch moved beneath you.", "difficulty": "expert", "task": "git_mastery", "grounded": false, "source": "seed:concept:git_mastery"}
